# 修订方案与项目数据面影响说明

**对象制度**：《阳光人寿保险股份有限公司保险销售行为管理办法》（阳光人寿发〔2024〕102号）
**依据项目**：`regulatory_compliance_orchestrator`（唯一演进仓）
**编制日期**：2026-09-14　|　**落盘位置**：`modules/internal_policy_drafter/docs/2026-09-14_修订/`（随仓纳管）

> 本文是六件套的**前置说明件**，回答三项指定内容：**①修订条款的具体范围；②涉及的模块交互节点；③需同步调整的数据项及其上下游依赖关系**。
> 全部结论以项目**运行时实际数据**为准，每条事实均标注可核验的产物路径。

---

## 一、交付物清单与稿件性质

| 序 | 文件 | 说明 |
|:---:|---|---|
| 00 | `00_修订方案与项目数据面影响说明.md` | 本文件 |
| 01 | `01_制度正文_…_2026修订版.md` | 制度全文（封面/记录表/目录/正文六章60条/附件依据清单） |
| 02 | `02_条款修改对照表.md` | **两段式**对照：2024版（7章44条）→ 2026修订版（6章60条） |
| 03 | `03_核心流程简图.md` | 销售前→中→后→监督问责全流程简图 |
| 04 | `04_执行落地备忘录.md` | 关键角色 RACI / 首次实施时间表 / Q&A / 落地风险 |
| 05 | `05_制度与监管文件条款双向映射表.md` | 监管文件全景清单 + 正向 + 反向映射（含条款原文） |
| 06 | `06_监管要求覆盖完整性校验报告.md` | 逐项覆盖状态 + 缺口 + 量化指标 + 数据面一致性检视 |

> ⚠️ **稿件性质（用户指令，2026-09-15）**：本套交付物为**草拟稿**，**未写入公司制度库**；制度库中 `阳光人寿发〔2024〕102号`（7 章 44 条）**仍为公司当前有效版本**。草拟稿之入库须待公司确定拟发文字号并履行发文程序后再行执行。

---

## 二、已确认的决策

| 决策点 | 确认结论 | 影响 |
|---|---|---|
| 修订基线 | **以 2024 原版（7 章 44 条）为唯一「旧版」** | 02 对照表为两段式；V2.0 仅作过程溯源 |
| 交付落盘 | `modules/internal_policy_drafter/docs/2026-09-14_修订/`（**随仓纳管**） | 交付物入 git；须净化盘符字面量以过 `gate_hardcoded_paths` |
| 数据面回写 | **草拟稿不入制度库** | 已写入 `originals/` 的 2026 版 docx **已整体撤出**并归档 |
| 2024 版状态 | **保持 `active`**（当前有效版） | 不应用 `deprecated`；2024 版未被任何变更影响 |
| 封面必填项 | 沿用 `〔待填写〕` 占位 | 姓名/版本/生效日期/拟发文字号不臆造 |
| 制度正文体例 | **取消节级结构**（章→条），与 2024 原版一致 | 条文切分器仅识别章/条，节标题会被黏入前一条正文，污染 `clauses.json` |

---

## 三、事实源与数据基线（运行时实测）

| 编号 | 事实 | 实测值 | 事实源路径 |
|---|---|---|---|
| FACT-01 | 内部制度索引规模 | `count=878`；`stat={state_entries:878, deduped_duplicates:0}` | `modules/internal_policy_base/data/internal_policy_index.json` |
| FACT-02 | 本制度索引记录（**当前有效版**） | `ipn=IPN-48419cb3312f33eb`；`docno=阳光人寿发〔2024〕102号`；`title=阳光人寿保险股份有限公司保险销售行为管理办法`；`sha256=f02b39258c710eb08edfa4c00bb2defe14e9adae3750b27e2f92341d4cb249e4`；`size_bytes=212236`；`text_chars=7250`；`chapter_count=7`；`article_count=44`；`status=active`；`drafting_dept=董事会办公室 战略企划部 中介渠道管理部` | 同上 |
| FACT-03 | 本制度条款产物 | 7 章 44 条：总则(1–3)/基本要求(4–9)/销售前(10–19)/销售中(20–32)/销售后(33–39)/责任追究(40–42)/附则(43–44) | `…/data/processed/IPN-48419cb3312f33eb_clauses.json` |
| FACT-04 | 监管文件归属表 | **1155 行**；时效分布 `valid 686 / pending 226 / repealed 154 / amended 62 / expired 27` | `modules/regulatory_classifier/data/人身保险公司-文件归属表.csv` |
| FACT-05 | 主题归属表 | 1155 行；**T1销售行为与消费者保护 = 196 行**（valid 125 / pending 38 / repealed 30 / expired 3） | `…/人身保险公司-主题归属表.csv` |
| FACT-06 | 本制度关联监管依据 | `associated_rfns` **仅 1 条**：`RFN-946238a80cf9fab2`《保险销售行为管理办法》（matched_by=`title`，`valid`）；`primary_theme=""` | `modules/internal_policy_base/data/merged_view.json` |
| FACT-07 | merged_view 全局 | `total=878`；`with_rfn_refs=336`；`aligned_ratio=0.383` | 同上 |
| FACT-08 | 关系事实源 | `relations_index.jsonl` **2130 条**（basis 1708 / repeal 422；三类 1985/19/126）；本 IPN 作为源/目标均 **0 条** | `modules/regulatory_classifier/data/relations/relations_index.jsonl` |
| FACT-09 | 监管原版条文 | 《保险销售行为管理办法》（总局令〔2023〕2号）真实 **6 章 50 条**；产物 `article_count=53`（3 处跨页续行误切） | `…/data/clauses/nfra_clauses_20260912.jsonl` |
| FACT-10 | 43 份 R 依据命中率 | **43/43 全部命中归属表**；其中 **4 份实际非 valid**：R-21 `pending`、R-28 `amended`、R-39 `pending`、R-41 `pending` | 归属表实测 |
| FACT-11 | 主题归属纠偏 | R-07（令〔2022〕8号）、R-16（银保监规〔2022〕24号）实际属 **T2产品与精算制度** | 主题归属表 |
| FACT-12 | 废止状态纠偏 | 银保监办发〔2019〕179号 归属表口径为 **`repealed`**，非"部分废止或失效" | 归属表 `RFN-e5a60d918465f9ee` |
| FACT-13 | 交付库 / 门禁 | `analysis` 交付库 **17 项**；门禁 **16 道**，`gate_relations` 含**判据 8 产物新鲜度** | `docs/reports/_manifest.json`；`gates/__init__.py` |
| FACT-14 | **IPN 派生规则** | `IPN = md5(docno|title|extension)[:16]`，**不含内容 sha** → 仅改正文内容、不改文号与标题时 **IPN 稳定** | `modules/internal_policy_base/scan.py:78-91` |
| FACT-15 | 摄取幂等键 | `_ingest_state.json` 键 = **内容 sha256**；值含 `{ipn, sha256, path_key, ingested_at}` | `modules/internal_policy_base/indexer.py:247-338` |
| FACT-16 | 起草素材现状 | `data/draft_clause/` **1001 份 md**，索引 878 条 → **孤儿素材 148 份**、**25 条索引无素材**；本制度素材**结构陈旧**（缺 §2 关系段） | `…/draft_clause/IPN-48419cb3312f33eb_条款对照素材.md` |
| FACT-17 | 原件双份存储 | `originals/` 与 `regulatory_scrapers/data/corpus/` **硬链接共享 inode** → **覆盖 `originals/` 会同步破坏审计层原件** | 跨目录 inode 实测 |

---

## 四、修订条款的具体范围

草拟稿在 2024 原版（7 章 44 条）基础上重构为 **6 章 60 条**：继承改造 **43 条**、删除 **1 条**（原第十二条 合同条款制订）、新增 **17 条**。全部修订点均由实测数据或体例规范触发，无主观增删。

### 4.1 本轮数据面校准变更点（7 类，逐条详见《02》第三部分）

| 类型 | 变更 | 触发数据 |
|---|---|---|
| A. 依据时效纠偏（4 条） | R-21/R-28/R-39/R-41 由"现行有效"改判为 `pending`／`amended` | FACT-10 |
| B. 废止状态纠偏（1 条） | 银保监办发〔2019〕179号 → 已废止，不列为依据 | FACT-12 |
| C. 主题归属纠偏（2 条） | R-07、R-16 归入 T2 | FACT-11 |
| D. 新增依据 5 份 & 新增 1 条 | R-44～R-48；新增第五十六条"消费者权益保护监管评价衔接" | FACT-05 |
| E. 废止声明纠错 | 文号占位 → 实测值 `阳光人寿发〔2024〕102号` | FACT-02 |
| F. 事实基线更新 | 归属表 1059→**1155** 条；T1 195→**196** 份 | FACT-04/05 |
| G. 对照口径说明 | 附件声明条文级对照以 50 条为准 | FACT-09 |

### 4.2 明确排除的范围

- 不改动与本次数据校准无关的条款文字；
- 不新增公司内部承办部门/时限（未获授权信息，禁止臆造）；
- 不修补 2024 原版已存在的**条文抽取缺字**（DEFECT-IPB-01，见《02》第四部分）——该缺陷属项目抽取链问题，已登记建议单独立项。

---

## 五、涉及的模块交互节点

### 5.1 节点表（9 个节点，均为项目**唯一入口**）

| 节点 | 模块 | 唯一入口 | 输入 | 输出产物 |
|:---:|---|---|---|---|
| **N1** 原件落位 | `internal_policy_base` | `indexer.ingest()` ← `cli.py internal index` | `data/originals/` | `internal_policy_index.json`、`_ingest_state.json`、`processed/<ipn>.json` |
| **N2** 正文与条文抽取 | `internal_policy_base` | `extract.extract_file()` / `backfill_clauses()` | N1 落位件 | `processed/<ipn>_fulltext.json`、`_clauses.json`、`_clauses.md` |
| **N3** 身份精化 | `internal_policy_base` | `indexer.refine_identity_backfill()` ← `internal refine-identity` | `_fulltext.json` | 索引 `docno/title/ipn/identity_*` |
| **N4** 主题对齐 | `internal_policy_base` | `align.align_all()` ← `internal align` | 索引 + 归属表/主题归属表 | `align_result.json` |
| **N5** 关联监管依据 | `internal_policy_base` | `merged.build_merged_view()` ← `internal merged` | 索引 + `processed/` + 归属表 | `merged_view.json` |
| **N6** 依据/废止关系抽取 | `regulatory_classifier`（编排 `tools/`） | `cli.py relations gen` → `tools/extract_relations.py` | `processed/*_fulltext.json` + 五源 `cleaned/*.jsonl` | `relations/*`（4 产物） |
| **N7** 起草素材编排 | `internal_policy_drafter` | `cli.py draft --ipn <IPN>` | N5 + N2 + N6 | `data/draft_clause/<ipn>_条款对照素材.md` |
| **N8** 发布件与 FTS | `base_publish` | `cli.py base publish` | 内部索引/条文 + 外部五源 | `published/*` + SQLite/FTS5 + `publish_manifest.json` |
| **N9** 交付库与门禁 | `tools/gen_analysis_deliveries` + `gates` | `cli.py analysis gen` / `cli.py gates` | 底座 / clause_graph / 关系产物 / 索引 | `docs/reports/` **17 项** + `_manifest.json`；**16 道门禁** |

### 5.2 节点依赖与顺序约束

```
[制度稿落盘] → N1 → N2 → N3                                   （生产刷新链「阶段 1 internal」）
                 ├─→ N4 → N5 ──→ N7                          （阶段 4.2；draft 为 4.2 之后的独立步）
                 └─→ N6（阶段 2.6，cleaned 写后、下游消费者前；不带 --report）
                              ↓
                        N8（阶段 5.5） → N9（阶段 6.8 analysis） → gates（阶段 6）
```

| # | 顺序约束（违反即 FAIL，不静默） | 由谁守 |
|:---:|---|---|
| 1 | N6 必须在 N1/N2 之后（输入是 `processed/*_fulltext.json`） | — |
| 2 | N6 **不带 `--report`**（报告统一由 N9 单一写入，防同名双写分叉） | — |
| 3 | N5 必须在 N1 之后、N7 之前 | `cli.py draft` 缺失即报错 |
| 4 | 关系产物不得早于数据面输入 | `gate_relations` 判据 8 |
| 5 | `merged_view.inputs` 指纹不得陈旧；`associated_rfns` 的 RFN 必须在 classifier 索引中 | `gate_citations` |

---

## 六、需同步调整的数据项及其上下游依赖

### 6.1 数据项清单（逐产物）

#### ① 原件层 `modules/internal_policy_base/data/originals/`

| 数据项 | 说明 |
|---|---|
| 文件命名 | 建议**保留原文号命名**（`阳光人寿发〔2024〕102号_…`）以守 IPN 稳定；若改用新文号 → IPN 变更 → 全链引用漂移 |
| ⚠️ **硬链接约束** | `originals/` 与 `corpus/` 共享 inode（FACT-17）→ **禁止原地覆盖既有 PDF**（会同步破坏审计层原件）。**正确做法：新增文件而非覆盖** |

#### ② 索引 `internal_policy_index.json`

| 字段 | 变化 |
|---|---|
| `sha256` / `size_bytes` / `text_chars` | 内容变更必变 |
| `chapter_count` / `article_count` | 7→6；44→60 |
| `extracted_at` | 刷新 |
| `docno`/`title`/`file_name`/`relative_path` | 由内容精化决定（**标题中的"（2026修订版）"后缀会被并回**） |
| `primary_theme`/`secondary_themes`/`align_method` | 对齐后回写 |
| ⚠️ `drafting_dept` / `path_relocated_*` / `name_normalized_at` / `policy_kind` / `excluded_reason` | 属 `_AUX_CARRY_FIELDS`，**不在 `PROCESSED_FIELDS` 白名单**，重建时靠防冲机制保留 |

#### ③ 条文产物 `processed/`

| 产物 | 说明 |
|---|---|
| `<ipn>.json` | 与索引同 schema |
| `<ipn>_fulltext.json` | `{ipn, text}`；**N6 关系抽取的直接输入** |
| `<ipn>_clauses.json` | `{ipn, chapters[{no,title,article_index}], articles[{no,number,body}]}`；`article_index` 为**整数下标** |
| `<ipn>_clauses.md` | 渲染视图 |
| `<ipn>_rich.json` / `_images/` | 富内容（本制度为 0） |

#### ④ 对齐 `align_result.json`

| 数据项 | 变化 |
|---|---|
| 记录 `primary_theme` / `secondary_themes` / `align_method` | 按归属表重算 |
| `theme_stat` | T1 计数变化 |

#### ⑤ 关联视图 `merged_view.json`

| 数据项 | 当前 | 说明 |
|---|---|---|
| 记录 `associated_rfns` | 2024 版仅 **1 条** | **核心缺口**：草拟稿正文声明 48 份依据，视图仅 1 条 |
| 顶层 `inputs.*`（`attr_sha`/`theme_sha`/`index_sha`/`processed_signature`） | — | `gate_citations` 校验对象；索引/产物一变即需重跑 N5 |
| 顶层 `stat.with_rfn_refs` / `aligned_ratio` | 336 / 0.383 | 随语料变化 |

#### ⑥ 关系产物 `relations/`（4 项）

| 数据项 | 当前 | 说明 |
|---|---|---|
| 本 IPN 的 `src_kind=internal` 的 `basis` 边 | **0 条** | 主因：2024 原版仅第一条出现 `《…》`，其余为改写表述 |
| `dst_class` ∈ `entity/corpus/organ/generic/external` | — | **只有 `external` 计入"文件引用未定位"分子** |
| `unresolved` 行 | — | **`dst_key` 恒为空，禁止臆造** |
| `relations_stat.json` 的 `total`/`by_kind` | 2130 | 与实际不一致即 FAIL |

#### ⑦ 起草素材 `modules/internal_policy_drafter/data/draft_clause/`

| 数据项 | 当前 | 说明 |
|---|---|---|
| `<ipn>_条款对照素材.md` | 缺 §2 关系段；条文为旧 44 条结构 | 执行 `cli.py draft --ipn …` 刷新 |
| 目录一致性 | 孤儿素材 **148 份** / **25 条**索引无素材 | 属独立数据治理事项 |

#### ⑧ 发布件 `published/`

`internal_policies.jsonl`（878 行）、`internal_clauses.jsonl`（**10213** 行）、`internal_index.sqlite` + FTS5、`publish_manifest.json` — 均由 `cli.py base publish` 确定性重建。

#### ⑨ 交付库 `docs/reports/`（17 项 + `_manifest.json`）

`2.1.2.4 监管与制度依据废止关系图谱` / `2.1.2.5 RFN补登候选清单` 随关系产物重建（**零重抽取**，从已落盘产物重建）。

### 6.2 上下游依赖链（严格顺序）

```
originals/ → ① 索引 → ③ processed → ④ align → ⑤ merged → ⑦ 素材
                                 ├─→ ⑥ relations（不带 --report）
                                 └─→ ⑧ published → ⑨ 交付库 → gates（16 道）
```

### 6.3 ⚠️ 高危操作警示（本轮实测踩坑，务必遵守）

| # | 警示 | 后果 |
|:---:|---|---|
| **W1** | **`--only-unindexed` 在"未索引清单为空"时会退化为全库重扫**（`ingest` 的 `if only_paths:` 对空列表判假） | 实测触发 **855 份重摄取**、索引 878→861、IPN 漂移 643 条。**运行前必须先确认清单非空** |
| **W2** | **全量 `internal index` 会回退 `refine-identity` 成果**：`scan_directory` 按**文件名**派生 IPN，而 state 存的是**内容精化后** IPN，二者天然不等 → 既有记录被判 `changed` | 全库 IPN 漂移 + 重复记录 |
| **W3** | **禁止原地覆盖 `originals/` 中已有的制度正文文件**（与 `corpus/` 硬链接共享 inode） | 同步破坏归集/审计层原件 |
| **W4** | 改 IPN/文号前须先枚举全链引用（`relations.src_ref`、`merged.ipn`、`draft_clause/*.md`、`published/*`） | 引用失配 |

### 6.4 数据面字段级影响（若公司决定入库草拟稿）

若后续决定将草拟稿入库，需执行的最小序列：

```
1) 备份 index / state / merged / relations / processed
2) 新增制度正文文件至 originals/（禁止覆盖既有文件；建议保留原文号命名以守 IPN）
3) cli.py internal index --source-dir <originals> --only-unindexed   ← 运行前确认清单非空
4) cli.py internal refine-identity
5) cli.py internal align
6) cli.py internal merged
7) cli.py relations gen
8) cli.py draft --ipn <IPN>
9) cli.py base publish
10) cli.py analysis gen
11) cli.py gates                       ← 须 16/16 PASS
12) 复核 aligned_ratio / with_rfn_refs / associated_rfns / 关系条数
```

---

## 七、本轮执行记录（含数据面事故与恢复）

### 7.1 已执行

| 序 | 动作 | 结果 |
|:---:|---|---|
| 1 | 生成草拟稿 docx（公司体例排版） | `6 章 / 60 条 / 0 处句末缺字 / needs_ocr=False` |
| 2 | 写入 `originals/` 并定向摄取 | 新增 `IPN-ebf704c0cd3aef80`（docx，6章60条） |
| 3 | **（事故）** 误执行全量重摄取 | 索引 878→**861**，643 条 IPN 漂移 |
| 4 | **恢复**（经用户授权） | 以 `backups/naming_20260913_233001/` 快照复原 → **878 条、IPN 与 merged_view 差异 0** |
| 5 | **草拟稿撤出制度库**（用户指令） | 原件 + processed 移出并归档至 `backups/incident_20260914_215138/draft_2026_reverted/` |
| 6 | 路径重复修复 | 2 组同名冲突记录按 **sha256 精确配对**修复；`unindexed_originals()` → **0** |
| 7 | 重跑刷新链 | align → merged → relations gen → base publish → analysis gen |
| 8 | **门禁** | **PASS：全部门禁通过（16/16）** |

### 7.2 事故与恢复要点（留档）

| 项 | 内容 |
|---|---|
| 触发 | 我漏写 `--only-unindexed`（W1） |
| 损害面 | `internal_policy_index.json`（878→861）、643 条 IPN 漂移、`processed/` 增 2971 个漂移文件 |
| 恢复源 | `modules/internal_policy_base/backups/naming_20260913_233001/`（index + state 快照，其 878 个 IPN 与 `merged_view.json`、`published/internal_policies.jsonl` 完全一致） |
| 恢复结果 | `count=878`；`stat={state_entries:878, deduped_duplicates:0}`；**索引 IPN 与 merged_view 差异 = 0**；`unindexed_originals()=0`；`with_rfn_refs=336`；`aligned_ratio=0.383`；关系 **2130 条**（与事故前基线一致）；**门禁 16/16 PASS** |
| 归档位置 | `modules/internal_policy_base/backups/incident_20260914_215138/`（含事故态 index/state、damaged_ipns.json、orphan_processed/、draft_2026_reverted/、recovery_log.json） |
| 遗留（非阻断） | ① `drafting_dept` 521 条 vs 快照 522 条（差 1 条，辅助列，不影响任何门禁）；② `processed/IPN-045d756c1276371f_fulltext.json` 缺失（其 `_clauses.json` 亦缺，`base publish` 少计 69 行条文，属**事故前即存在**的口径差：快照索引条数合计 10297 vs 条文产物合计 10213）；③ 2 组同名冲突记录（内部控制指引、销售人员分类分级办法）在**事故前**即存在索引路径指向与实际不符，已按 sha256 修复 |

---

## 八、待公司确认事项

| # | 事项 | 当前处理 |
|:---:|---|---|
| 1 | 草拟稿是否入库、何时入库 | **未入库**；须待拟发文字号确定后按 §6.4 执行 |
| 2 | 封面编写/审核/签发人、版本号、生效日期、拟发文字号 | `〔待填写〕` |
| 3 | 正文中 `（依据清单 R-xx）` 内部标注是否在印发生效版中剥离 | 建议剥离 |
| 4 | 依据清单 4 份非现行有效文件的效力确认时限 | 建议 30 个工作日内 |
| 5 | DEFECT-IPB-01（`_drop_junk_lines` 误删短行）是否立项修复 | 建议立项（影响全库 878 条正文质量） |

---

**编制**：AI 助手（life-insurance-system-drafter 技能 · 六阶二十步流程）
**事实源**：`regulatory_compliance_orchestrator` 运行时产物（2026-09-14 实测；2026-09-15 恢复后复核）
**声明**：本文数值均来自项目实际产物，未作推测性填充；不确定项已显式标注。
