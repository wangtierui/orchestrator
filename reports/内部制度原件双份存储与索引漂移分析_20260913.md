# 内部制度原件「双份存储」与索引路径漂移分析

> **检视对象**
> - A：`modules/regulatory_scrapers/data/corpus/dept_policies`
> - B：`modules/internal_policy_base/data/originals`
>
> **检视日期**：2026-09-13　**方法**：目录实测 + sha256 内容级比对 + 代码链路溯源（全部结论附可复现命令，见 §7）
> **关联**：`reports/克隆可移植性检视报告_20260913.md`（同批发现的静默降级模式，本报告 §5.3 为其同类新实例）

---

## 0. 结论摘要

| 问题 | 结论 |
| :--- | :--- |
| 为何重复？ | **两条独立链路各存一份**：A 由 `tools/ingest_corpus.py`（F-L05 外部语料归集）从 `D:/Desktop/1.总公司各部门制度` 复制；B 由 `internal_policy_base.indexer.ingest()`（P6 制度摄取）从**同一源目录**复制。二者是同源的两份物理副本。 |
| 各自用途？ | A = **归集/审计层**（保留原始交付目录树 + 入库清单 `reports/corpus/*.manifest.json`，是"这份语料从哪来"的可审计入口）；B = **摄取/运行层**（IPN 索引锚点 + OCR/条文/富内容抽取的原件来源，被 `reocr` 等命令实际回读）。**职责不同，不是重复建设。** |
| 有冗余/同步问题？ | **有，且不止一类**：① A 的 967/968 个文档内容在 B 中**全部存在** → 约 **306 MB 纯物理冗余**；② B 内部 97 个文件是同一内容的重复拷贝；③ **B 的原件与主索引路径严重失配**：957 条索引中仅 443 条能在磁盘解析，514 条指向**已改名的旧目录**；④ `internal reocr` 对这 514 条**静默跳过**（无计数、无告警）。 |
| 是否需要合并？ | **A 与 B 不应合并**（会破坏 F-L05 归集审计链）；但必须 ①消除 A/B 的物理重复 ②修复 ingest 幂等键 ③修复 reocr 静默缺口 ④新增"索引↔原件可解析性"门禁。详见 §6。 |

一句话：**这不是"重复建设"，而是"同源双写 + 源目录重组导致索引漂移"，且漂移被静默容忍了。**

---

## 1. 事实基线（实测）

### 1.1 规模与结构

| 指标 | A `corpus/dept_policies` | B `internal_policy_base/data/originals` |
| :--- | ---: | ---: |
| 文件数（含隐藏） | 1,028 | 1,134 |
| 总大小 | 306.2 MB | 349.3 MB |
| 一级目录数 | 26（部门） + 根层散件 | 26（部门） + 根层散件 |
| 根层文件数 | 4 | 108 |
| 唯一内容数（sha256） | 968 | 1,037 |
| 内部内容重复 | 45 组 / 涉及 105 文件 | 75 组 / 涉及 172 文件 |
| 扩展名分布 | pdf 662 / docx 134 / xlsx 80 / doc 68 / xls 20 / png 8 / jpg 8 / zip 8 / db 31 … | pdf 725 / docx 151 / xlsx 84 / doc 90 / xls 21 / png 8 / jpg 8 / zip 8 / db 31 … |

> 附：同链路第三层 `internal_policy_base/data/processed` 为 **389.9 MB**（957 份 `*_fulltext/_clauses/_rich` 派生物），三者合计约 **1,045 MB** 本地占用。

### 1.2 两者的一级目录完全一致

```
corpus 一级目录数: 26 | originals 一级目录数: 26 | 差集: []
```

即 **A 与 B 来自同一份源快照**（目录命名逐字相同），不存在"不同批次不同部门集合"的情况。

### 1.3 内容级重合度（决定性）

| 交叉指标 | 结果 |
| :--- | ---: |
| A 唯一内容 ∩ B 唯一内容 | **967** |
| 仅 A 有（B 无） | **1**（即 A 自己的 `_ingest_manifest.json`） |
| 仅 B 有 | **70** |

**结论：A 的全部 967 个业务文档内容，B 中全都有。** 唯一例外是 A 的清单文件本身。

### 1.4 路径级比对

| 指标 | 结果 |
| :--- | ---: |
| A 相对路径数 / B 相对路径数 | 1,028 / 1,134 |
| A ∩ B 相对路径 | **1,027**（A 除清单外全部命中） |
| 仅 B 有的相对路径 | 107（即 B 根层那 108 个"扁平重命名"文件） |

---

## 2. 问题一：为什么两个目录存在重复存储

### 2.1 两条链路、同一个源目录

**A 的写入方** —— `tools/ingest_corpus.py`（F-L05 外部语料归集）：

```28:30:tools/ingest_corpus.py
CORPUS_DIR = os.path.join(_ROOT, "modules", "regulatory_scrapers", "data", "corpus")
# 归集清单落 reports/corpus/（reports 入库；语料本体 data/ 不入库——清单为唯一可审计入口）
MANIFEST_DIR = os.path.join(_ROOT, "reports", "corpus")
```

其清单记录了源目录（实测）：

```
domain      : dept_policies
source_dir  : D:/Desktop/1.总公司各部门制度
ingested_at : 2026-09-12 04:01:39
file_count  : 1019
total_bytes : 332138728
copied      : 1019          # 全部为新复制
```

**B 的写入方** —— `modules/internal_policy_base/indexer.py`（P6 摄取）：

```8:9:modules/internal_policy_base/indexer.py
  3) 新/变 → 复制原文件入仓 data/originals/<rel> + 抽取正文 → data/processed/<ipn>.json
  4) 汇总写 data/internal_policy_index.json（主索引）+ 侧写 data/_ingest_state.json
```

```145:145:modules/internal_policy_base/indexer.py
        orig = copy_original(f["source_path"], _ORIGINALS, f["relative_path"])
```

`copy_original` 是**物理复制**：

```107:112:modules/internal_policy_base/extract.py
def copy_original(src: str, dst_dir: str, rel_path: str) -> str:
    """原始件复制入仓（D-05 数据随仓），返回落盘绝对路径。同名冲突保留（按 rel 目录）。"""
    dst = os.path.join(dst_dir, rel_path)
    os.makedirs(os.path.dirname(dst), exist_ok=True)
    shutil.copy2(src, dst)
    return dst
```

→ **两条链路都从同一个"各部门制度"目录做全量物理复制**，于是同一份交付物在仓内落地两次。这与 `.gitignore` 无关（`data/` 不入 git），是**本地磁盘层面的重复**。

### 2.2 加剧因素：源目录在两次复制之间被重组

一级目录层面的实测：

```
索引 distinct 一级目录: 27 | 磁盘 distinct 一级目录: 27
索引有、磁盘无（源目录已重组/改名）: 3 个目录，涉及 97 条记录
    - 4.个险业务支持部（培训板块）      （索引 7 条）
    - 9.健康事业部                     （索引 25 条）
    - 制度检视-风险管理                 （索引 65 条）
磁盘有、索引无: 3 个
    + 26.风险管理部
    + 4.个险业务支持部
    + 9.健康保险事业部
```

即源目录发生过**部门改名/归并**（`4.个险业务支持部（培训板块）` → `4.个险业务支持部`、`9.健康事业部` → `9.健康保险事业部`、`制度检视-风险管理` → `26.风险管理部`），而**索引里留的仍是旧名**。深层子目录同理（例：索引记 `4.个险业务支持部（培训板块）\制度汇总--培训板块\...`，而该目录在当前 A/B 中均不存在）。

### 2.3 B 根层 108 个"扁平重命名"文件从何而来

B 的根层文件名形如 `阳光人寿办发〔2023〕58号_个险客经渠道团队套利处置管理办法.pdf`、`_振兴计划规划师基本管理办法（2022版）.pdf` —— 即**"文号_标题"内容权威命名 + 无文号时前置 `_`**（与 `reports` 中"内部制度命名内容权威"约定一致）。

内容比对显示，这批根层文件**大部分是既有部门目录文件的重复拷贝**：

```
[已索引] _振兴计划规划师基本管理办法（2022版）.pdf
      || 2.机构振兴改革事业部\1-振兴计划规划师管理办法\振兴计划规划师基本管理办法（2022版）.pdf
[已索引] 阳光人寿办发〔2023〕58号_个险客经渠道团队套利处置管理办法.pdf
      || 3.客经部\1.《个险客经渠道团队套利处置管理办法》.pdf
[已索引] 阳光人寿发〔2021〕70号_融客渠道坐席提单管理办法（2021版）.pdf
      || 19.核保核赔部\56.融客渠道坐席提单管理办法（2021版）.pdf
```

B 内部 75 组重复、172 个文件，基本都由"部门目录副本 + 根层重命名副本"构成。

---

## 3. 问题二：各自的用途与设计意图

| 维度 | A `corpus/dept_policies` | B `originals` |
| :--- | :--- | :--- |
| 所属层 | regulatory_scrapers（**归集层**，F-L05） | internal_policy_base（**摄取层**，P6） |
| 设计意图 | 把"未归集数据域"（EAST2.0 / 部门制度 / 保单登记…）**按域归集进仓**，保留**原始交付目录树**，使"数据在哪、从哪来、有多少、校验值是什么"可审计 | 把制度原件**复制入仓并建立 IPN 主索引**，供抽取/OCR/条文/富内容/条款对照消费（D-05 数据随仓） |
| 目录形态 | 保留源目录结构与原文件名（`1.营销部\1-附件2.个险营销星荣誉会章程（2025版）.pdf`） | 同上 + 根层追加"文号_标题"重命名批次 |
| 可审计产物 | `_ingest_manifest.json`（域内）+ **`reports/corpus/dept_policies.manifest.json`（入库）** + `reports/corpus/_index.json` | `data/internal_policy_index.json`（957 权威记录）+ `data/_ingest_state.json`（内容游标）+ `data/processed/*` |
| 幂等机制 | `_iter_files` + "目标已存在且 sha256 相同则跳过"（`ingest_corpus.py:128`） | `state[sha256] = {ipn,...}` 内容寻址（`indexer.py:133-137`） |
| 被谁消费 | **没有任何链路读取**（见下） | `extract.reocr_backfill`（`extract.py:293`）、`merged._load_text`（经 processed）、`backfill_clauses` 等 |
| 入 git？ | 否（`data/`）；仅清单入库 | 否（`data/`） |

**"A 无人消费"的实测证据**（全仓检索 `corpus`）：

```
gates/gate_flat_layout.py:27    "regulatory_scrapers": {..., "corpus"},   # 目录白名单
gates/gate_secret_scan.py:30    ... os.sep + "corpus" + os.sep ...       # 密钥扫描排除
tools/ingest_corpus.py:28,30,...                                        # 写入器自身
```

除"门禁白名单/排除项"与写入器自身外，**没有任何业务代码读取 A**。A 的价值在于**审计与可追溯**（清单入库），而非运行期数据源。

---

## 4. 问题三：是否存在数据同步或冗余问题

**存在，共 4 类，且第 3、4 类是真实缺陷。**

### 4.1 【冗余】A 与 B 的内容重复 —— 约 306 MB 可消除

A 的 967 个业务文档内容在 B 中 100% 存在（§1.3），A 总大小 306.2 MB → **这 306.2 MB 是纯物理冗余**。因为二者都不入 git，影响面是本地磁盘与备份体积，而非仓库体积。

### 4.2 【冗余】B 内部同一内容多份拷贝 —— 97 个冗余文件

B：1,134 文件 / 1,037 唯一内容 → **97 个文件是同一内容的重复拷贝**（75 组）。主因是"部门目录副本 + 根层重命名副本"并存（§2.3）。

### 4.3 【缺陷】主索引与原件路径严重失配

| 指标 | 实测 |
| :--- | ---: |
| `internal_policy_index.json` 记录数 | 957 |
| 索引 `relative_path` 能在 `originals/` 解析的 | **443**（46.3%） |
| 索引 `relative_path` **解析失败** | **514**（53.7%） |
| `originals/` 磁盘文件未被任何索引记录引用 | **691** |
| 索引 basename 能在磁盘找到（去路径后） | **933 / 957**（97.5%） |
| 索引内容（sha256）能在磁盘找到 | **933 / 957**（97.5%） |
| 磁盘内容未被索引覆盖 | 104 组 / 108 文件 |

**判定：这不是"文件丢失"，而是"路径漂移"。** 97.5% 的内容都在，只是索引记的路径（旧部门名/旧子目录名）与磁盘实际布局（新名）不一致。

**根因（代码级）** —— 摄取幂等键是**内容**而非**路径**：

```132:141:modules/internal_policy_base/indexer.py
    for f in files:
        key = f["sha256"]
        prev = state.get(key)
        if prev and prev.get("ipn") == f["ipn"]:
            skipped.append(f["file_name"])
            continue
        if prev:
            changed_items.append(f["file_name"])
        else:
            new_items.append(f["file_name"])
```

而主索引汇总**按 IPN 去重，只保留 `extracted_at` 最新一条**：

```211:217:modules/internal_policy_base/indexer.py
        # 同 IPN 多 sha（同名同文号同介质的不同内容版本）→ 主索引保留 extracted_at 最新一条
        # （2026-09-12：与 merged/policies 同口径；重复数透明登记于 stat）
        ipn = r.get("ipn", "")
        if ipn not in seen_ipn or (r.get("extracted_at") or "") >= ext_map.get(ipn, ""):
            seen_ipn[ipn] = {k: r.get(k, "") for k in PROCESSED_FIELDS}
            ext_map[ipn] = r.get("extracted_at") or ""
```

**机制合成（因果链）**：

1. 首轮摄取（源目录为**旧布局**）→ `originals` 落盘布局 A + 索引写路径 A；
2. 源目录被改名/重组（§2.2 的 3 个一级目录 + 若干深层目录）；
3. 再次摄取（新布局）：文件内容已在 `state` 中 →
   - 若 IPN 未变 → **skip**：不复制、**不更新路径** → 索引停留在旧路径；
   - 若 IPN 因"内容权威"重算而变化（`indexer.py:154-156`）→ 视为 changed → **再次 `copy_original` 到新路径** → B 出现"旧布局 + 新布局"两套副本；
4. 汇总按 IPN 去重 → 每个 IPN 只留 1 条记录，路径可能落在旧布局上 → **514 条路径不可解析、691 个磁盘文件无索引引用**。

即：**B 的目录是"两代布局的叠加"，而索引是"按 IPN 去重后的单代视图"**，两者天然对不上。

### 4.4 【缺陷】`internal reocr` 对不可解析路径**静默跳过**

```293:295:modules/internal_policy_base/extract.py
        orig = os.path.join(data_dir, str(rec.get("original_path", "")))
        if not os.path.exists(orig):
            continue
```

`original_path` 是摄取时按 `relative_path` 写入的相对路径（`indexer.py:165`），因此上述 514 条记录的 `original_path` **必然解析失败** → 被静默 `continue`：**不计入 `stats["total"]`、不输出告警**。

后果：`orchestrator internal reocr [--force]`（扫描件提质重建）**最多只能触达 443/957 = 46% 的记录**，而命令输出看起来"正常完成"。这与本批克隆检视中发现的"静默降级/假成功"属**同一类缺陷模式**（能力缺失被伪装成正常）。

### 4.5 【次要】A 的清单与磁盘存在口径差

`dept_policies/_ingest_manifest.json` 记 `file_count: 1019`，磁盘现存 1,028 个文件（其中 1 个为清单自身）→ **净 8 个文件未被清单登记**（清单生成后新增）。清单是"唯一可审计入口"，故该差值应被纳入核验，否则审计口径失真。

### 4.6 一个"反向"发现：B 有 108 个文件内容不在任何索引中

磁盘内容未被索引覆盖者 104 组 / 108 文件，构成：

- 非制度类散件：`制度清单.xlsx`、各口径 `附件1：2025年制度检视自查情况表-*.xls`、`*.png`（部门自己把制度截图存放）、`附件2 产品开发手册附件.zip`、`Thumbs.db`；
- 少量**较新制度**（如 `阳光人寿发〔2025〕88号_阳光人寿客户信息安全管理办法.doc`）——疑似清单生成后补入目录、尚未摄取。

其中 `.png/.zip/Thumbs.db` 属**不应进入"制度原件库"的噪声**；`制度清单/自查表` 则是"检视工作台账"而非制度正文。这两类宜在摄取侧按扩展名/名称规则排除并显式登记。

---

## 5. 与既有纪律的对照

| 既有纪律 | 实际情况 | 判定 |
| :--- | :--- | :--- |
| `data/` 不入 git（数据与代码分离） | A/B/processed 均被 `.gitignore` 覆盖 | ✅ 符合 |
| 可审计：清单入库（`reports/corpus/*.manifest.json`） | 已入库 3 个清单文件 | ✅ 符合（但存在 §4.5 口径差） |
| "同名冲突保留（按 rel 目录）"（`copy_original` docstring） | 同内容异路径会重复落盘，且无去重/无上限 | ⚠️ 设计如此，但未设冗余治理 |
| 门禁覆盖"数据一致性" | 无任何门禁校验 `internal_policy_index.relative_path ↔ originals` 可解析性 | ❌ **缺口**（§4.3 长期无人察觉的直接原因） |
| 降级须显性（不留静默 fallback） | `reocr_backfill` 对缺原件静默 `continue` | ❌ **违反**（§4.4） |

---

## 6. 问题四：是否需要合并的建议

### 6.1 不建议合并 A 与 B

理由：

1. **职责不同**：A 是"归集 + 审计"（其清单 `reports/corpus/*.manifest.json` 已入 git，是"语料从哪来"的唯一可审计凭证，且与 F-L05 的域归集语义一一对应）；B 是"摄取 + 运行"（IPN 索引锚点、`reocr` 的原件来源）。合并会使"审计凭证"与"运行原件"耦合，任一侧变更都会破坏另一侧的语义。
2. **粒度不同**：A 按**域**（domain）组织，一个域可含非制度语料（如 `east2_m20`）；B 按**制度身份**（IPN）组织。二者的分区键不同，物理合并会导致目录语义混杂。
3. **删除 A 的代价**：`reports/corpus/dept_policies.manifest.json`（入库）将失去对应本体，审计链断裂。

### 6.2 应做的是"消除物理重复 + 修复漂移"，分四步

| 优先级 | 动作 | 预期收益 | 风险/前置 |
| :--- | :--- | :--- | :--- |
| **P0** | 修复 `indexer.ingest` 幂等键：`state` 由 `sha256` 单键改为 **`sha256 + relative_path`** 双键（或新增 `path_key` 维度），使"同内容不同路径"能被登记为新路径而非静默 skip；同时**不再因路径变化重复复制** | 根除 §4.3 漂移的产生机制 | 需迁移既有 `_ingest_state.json`（966 条）；建议先备份 state 与 index |
| **P0** | 修复 `reocr_backfill` 静默跳过：不可解析的 `original_path` 计入 `stats["missing_original"]` 并输出清单/告警（而非 `continue`） | 消除 §4.4 的静默能力缺口 | 低 |
| **P0** | 新增门禁 `gate_original_resolvable`（或并入 `gate_contract`）：校验 `internal_policy_index.json` 每条 `relative_path` 在 `originals/` 下可解析，覆盖率须达阈值（建议 100%），否则 FAIL | 使 §4.3 类问题**不可能再静默存在** | 低；需先修数据，否则门禁初始即红 |
| **P1** | 对 B 执行**单次对账合并**：以索引为准，重建 `originals/` 为"索引可解析路径"集合，把仅存在于磁盘的冗余副本移入 `backups/`（不直接删除），把索引指向但磁盘缺失的 24 条（制度清单/自查表类）显式标记为 `excluded/non_policy` | 消除 §4.2 的 97 个冗余文件；使索引与磁盘一致 | 中；须在 P0 修复后进行，且先备份 |
| **P2** | 消除 A/B 的 306 MB 重复：`copy_original` 改为**硬链接优先、复制兜底**（同盘同 NTFS 可行；不可用时回退 `shutil.copy2`） | 省 ~306 MB 且不改变目录语义与路径可读性 | 中；NTFS 硬链接需同卷，跨卷自动回退 |
| **P2** | 摄取侧增加**非制度件过滤**：排除 `*.png/*.jpg/*.zip/Thumbs.db` 与 `制度清单/自查情况表` 类台账，并显式登记"已排除"清单 | 消除 §4.6 的 108 个噪声/未覆盖率 | 低；需与用户确认排除口径（台账是否需要对账用） |

### 6.3 不建议的做法

- ❌ **直接删除 A**：会切断 F-L05 归集审计链与入库清单的对应本体。
- ❌ **直接删除 B 中"索引未引用"的 691 个文件**：其中相当部分是内容的**唯一可读副本**（索引路径失效但内容在盘），删了会真丢内容。**必须先按"内容"而非"路径"判定**（本报告 §1.3 即按内容判定）。
- ❌ **放任不改**：`reocr` 只能覆盖 46% 的记录，会直接削弱扫描件提质重建（这正是 2026-09-12 OCR 提速工作的下游）。

---

## 7. 复现命令附录

```powershell
# 0) 变量
$O = "modules\internal_policy_base\data\originals"
$C = "modules\regulatory_scrapers\data\corpus\dept_policies"

# 1) 规模与结构
Get-ChildItem $C -Recurse -File | Measure-Object Length -Sum
Get-ChildItem $O -Recurse -File | Measure-Object Length -Sum
Get-ChildItem $O -File | Measure-Object            # 根层 108
Get-ChildItem $O -Directory | Select-Object Name    # 26 个部门

# 2) 内容级重合（sha256）
$hA=@{}; Get-ChildItem $C -Recurse -File | ForEach-Object { $hA[(Get-FileHash $_ -Algorithm SHA256).Hash]=$true }
$hB=@{}; Get-ChildItem $O -Recurse -File | ForEach-Object { $hB[(Get-FileHash $_ -Algorithm SHA256).Hash]=$true }
"corpus 唯一内容 $($hA.Count) / originals 唯一内容 $($hB.Count) / 共有 $(@($hA.Keys|?{$hB.ContainsKey($_)}).Count)"

# 3) A 的清单与源目录
Get-Content "$C\_ingest_manifest.json" -Raw -Encoding UTF8 | ConvertFrom-Json |
  Select-Object domain,source_dir,ingested_at,file_count,total_bytes,copied

# 4) 索引 ↔ 磁盘 路径可解析率（Python）
py -c "import json,os;d=json.load(open('modules/internal_policy_base/data/internal_policy_index.json',encoding='utf-8'))['records'];O='modules/internal_policy_base/data/originals';ok=sum(1 for r in d if os.path.exists(os.path.join(O,r['relative_path'])));print('可解析',ok,'/',len(d))"

# 5) 一级目录漂移（Python）
py -c "import json,os,collections;d=json.load(open('modules/internal_policy_base/data/internal_policy_index.json',encoding='utf-8'))['records'];t=collections.Counter((r['relative_path'].split(chr(92))[0]) for r in d);disk=set(os.listdir('modules/internal_policy_base/data/originals'));print([x for x in t if x not in disk])"

# 6) 消费方核查（确认 A 无业务读取）
git grep -n "corpus" -- "*.py" "*.yaml" "*.toml"
git grep -n "original_path" -- "*.py"
```

---

## 8. 附：本次分析涉及的代码位置索引

| 位置 | 作用 |
| :--- | :--- |
| `tools/ingest_corpus.py:28-30, 113-131, 146-154` | A 的写入；清单与 `_index.json` 落地 |
| `modules/internal_policy_base/indexer.py:126-196` | B 的写入（复制 + 抽取 + processed + state） |
| `modules/internal_policy_base/indexer.py:132-141` | **幂等键 = sha256（漂移根因之一）** |
| `modules/internal_policy_base/indexer.py:145` | `copy_original` 物理复制调用 |
| `modules/internal_policy_base/indexer.py:154-156` | IPN 随"内容权威"重算（漂移根因之二） |
| `modules/internal_policy_base/indexer.py:211-217` | **主索引按 IPN 去重（漂移根因之三）** |
| `modules/internal_policy_base/extract.py:107-112` | `copy_original`（物理复制，无去重/无链接） |
| `modules/internal_policy_base/extract.py:293-295` | **`reocr` 对缺原件静默 continue** |
| `gates/gate_flat_layout.py:27`、`gates/gate_secret_scan.py:30` | A 仅被门禁"白名单/排除"引用（无业务消费） |

---

## 9. P0 修复执行记录（2026-09-13）

> §6.2 的 P0 三项已全部落地，并按"门禁可绿"的需要补做了**按内容重定位**（见下"配套"）。

| 项 | 动作 | 落点 | 结果 |
| :--- | :--- | :--- | :--- |
| **P0-A** | ingest 幂等键加 `path_key` 维度；同内容换路径走**「路径跟随」= 移动既有原件**（不再复制副本），同步 processed 路径字段与 `state.path_key` | `modules/internal_policy_base/indexer.py`（新增 `_path_key` / `_follow_path` / `_sha_match` / `_update_processed_path`） | `state` schema 升至 **2.0**（键空间不变，`_rebuild_index`/`refine_identity_backfill` 无需改动）；summary 新增 `relocated` |
| **P0-B** | 原件不可解析→计入 `missing_original` + `missing_details` 并**打印告警与处置入口**（原静默 `continue`） | `modules/internal_policy_base/extract.py`（`reocr_backfill`，并新增可注入 `data_dir` 便于单测） | 覆盖率缺口不再静默 |
| **P0-C** | 新增门禁 `gate_original_resolvable`：制度正文类 **100% 可解析**；非正文表格类失效台账 ≤ **登记基线 24**（只减不增）；索引缺失→FAIL（对齐 A-07） | `gates/gate_original_resolvable.py` + `gates/__init__.py` | ALL_GATES **14 → 15 道** |
| **配套** | 新增 `tools/reconcile_original_paths.py`：按**内容 sha256** 对账重定位（`--apply` 前自动备份；每条写 `path_relocated_from` 可回滚；幂等） | `tools/reconcile_original_paths.py` | 见下 |

### 9.1 对账执行结果（关键数据修复）

```
修复前：索引 957 条 → 可解析 443 / 不可解析 514（漂移 490 + 表格类 24）
        `internal reocr` 实际只能触达 443/957 = 46.3%
执行  ：python tools/reconcile_original_paths.py --apply
        → 重定位 490 条；state path_key 迁移 966 条
        备份 → modules/internal_policy_base/backups/original_paths_20260913_181519/
修复后：可解析 933 / 仍漂移 0 / 表格类 24 / 真缺失 0
        `internal reocr` 可触达 933/957 = 97.5%（余 24 条为已失效的制度检视台账）
幂等性：再次 dry-run → 待重定位 0（无变化）
```

**重定位选优规则**（27 条为多重匹配，已在记录中打 `path_relocated_ambiguous=true`）：
① 与索引原路径**同部门前缀**优先 → ② 层级最浅 → ③ 字典序。

**做了什么 / 没做什么**：

- ✅ 只改写**路径字段**（`internal_policy_index.json` 957 条中的 490 条 + 对应
  `processed/<ipn>.json` 的 `relative_path`/`original_path`）；
- ✅ 每条加 `path_relocated_from` / `path_relocated_at`（可审计、可回滚）；
- ✅ 备份索引 + state + 490 个 processed 主记录；
- ❌ **未改任何文件内容、未删除任何文件**；
- ❌ 未收窄 `known_non_policy_baseline`（仍 24，待 P2 摄取侧过滤后下调）。

### 9.2 修复后的连带动作与验证

- `internal merged` 重建（`gate_citations` 正确检测到 `index_sha` / `processed_signature` 变化并阻断，
  按提示重建后通过——**门禁按设计工作**）；
- 新增回归测试 `tests/test_internal_original_paths.py`（**10 例**）：门禁四分支（缺失/正文不可解析/
  可解析通过/表格类超基线）、reocr 缺失计数与告警、`path_key` 归一、`_follow_path` 移动+审计痕迹+
  旧件缺失回退；
- 全量回归：`cli.py gates` **15/15 PASS**、`pytest tests` **236 全绿**（`-m "not data"` 223 全绿）、
  `ruff` 0；`BENCHMARK.md` 重生成（gates 15 / 用例文件 23）；README 数字同步（门禁 14→15、
  数据门禁 6→7、用例 224→236）。

### 9.3 仍未闭环（P1/P2，见 §6.2）

| 项 | 状态 | 说明 |
| :--- | :--- | :--- |
| P1 原件库对账合并（消除 97 个冗余副本）+ 24 条表格台账显式排除 | **未做** | 属数据治理动作，建议单独授权后执行（需按**内容**判定，勿按路径删） |
| P2 消除 A/B 间 ~306 MB 物理重复（`copy_original` 改硬链接优先） | **未做** | NTFS 硬链接需同卷，跨卷须回退复制 |
| P2 摄取侧过滤非制度件（png/zip/Thumbs.db/台账）+ 基线收紧至 0 | **未做** | 落地后应把 `KNOWN_NON_POLICY_BASELINE` 下调 |
| 源目录 `D:/Desktop/1.总公司各部门制度` | **已不存在** | 故"重跑 ingest 收敛漂移"不可行——本次改为**按内容重定位**实现收敛（不依赖源目录） |

---

*本报告全部结论基于本机实测（目录遍历 + sha256 内容比对 + 代码链路检索）；§9 的 P0 修复仅改写索引/processed 的**路径字段**，未改动原件内容、未删除文件，并保留备份与审计痕迹。分析用临时脚本与输出文件已清理。*
