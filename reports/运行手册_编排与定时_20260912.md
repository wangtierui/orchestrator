# 运行手册 · 编排与定时（2026-09-12）

> ⚠️ **本表为生成物**（v2 §3.9 / P2-2，2026-09-26 起）：定时任务的**唯一事实源**是
> `config/schedule.yaml`；本文档「二、定时任务清单」的表格与 crontab 片段由
> `tools/gen_schedule_doc.py` **反向生成** —— **请勿手改**（`gate_config_integrity` 判据 S
> 断言「手册自动段 == yaml 渲染结果」，手改即 FAIL）。
> 变更流程：改 `config/schedule.yaml` → `python tools/gen_schedule_doc.py` →
> `python cli.py schedule verify`（比对已安装的 Windows 计划任务）。

---

## 一、编排主链（唯一编排 = `tools/run_production_refresh.py`）

```
阶段 0   抓取（--collect gov,mof,nfra,pbc,supp / nfra-weekly）
阶段 1   clean 五源（run_clean_pipeline --project <src>；尾部自动 clause + clean_index 重建）
阶段 2   时效回写（consolidate --use-state → apply_timeliness_to_cleaned 五源；含 status 派生）
阶段 2.5 classify --all（主题底座/明细/upper 强序重建，断点幂等）
阶段 3   reconcile_clean_drift --apply（RFN↔clean 漂移）
阶段 4   recall（clean/validity/contract/schema 四门禁；FAIL → rc=1）
阶段 4.2 internal merged（内部制度 × RFN 引用视图）
阶段 4.5 reports（全景/主题分类报告）
阶段 5.5 base publish（双底座发布件 + SQLite/FTS5；Base Contract v1）
阶段 6   gates（13 道交付门禁）
阶段 6.5 变更监听基线（cli.py source diff --record → data/watch_baseline.jsonl）（F-O02）
阶段 6.8 分析交付库刷新（cli.py analysis gen → docs/reports/ 15 项）（F-L01；classify 成功亦自动触发，--no-analysis 可跳过）
```

常用入口：

```bash
python tools/run_production_refresh.py --no-scrape          # 日常：raw 已抓取，仅重清洗+全链
python tools/run_production_refresh.py --collect all        # 全量抓取（长时，分段跑）
python tools/run_production_refresh.py --collect nfra-weekly  # nfra 周度增量链（F-O04）
python tools/run_production_refresh.py --no-scrape --supp-batch <backlog.json>  # 含 supp 摄取
```

## 二、定时任务清单（建议调度）

| 频率 | 时间 | 命令 | 说明 |
|---|---|---|---|
<!-- SCHED:AUTO -->
| **每周二 01:00** | `0 1 * * 2` | `cli.py run --collect nfra-weekly` | nfra 周报增量链（单实例锁 / 顶部窗口 / 断点续跑） |
| **每日 06:00** | `0 6 * * *` | `cli.py run --no-scrape --resume` | 全链刷新（raw 已抓取，仅重清洗 + 全链；当日无 raw 变化时各阶段快跳）（漏跑策略：run_at_next_boot） |
| **每周一 07:00** | `0 7 * * 1` | `cli.py run --only 6.9` | 效力核验批次（有 Token 时；断点续跑，配额耗尽自动停） |
| **每周一 08:00** | `0 8 * * 1` | `cli.py run --only 21.5` | 发布件刷新 + llm_wiki 源同步（SHA256 增量；条件触发 wiki_sync，见 triggers.yaml） |
| **每月 1 日 09:00** | `0 9 1 * *` | `cli.py gates` | 月度巡检：门禁（另需人工跑 `cli.py source diff` / `cli.py timeliness summary` / check_llm_wiki_upstream） |
| **事件驱动（after: refresh）** | — | `cli.py run --collect gov` | gov 源增量（含 zhengceku 子源）——**已并入 refresh**，此处仅登记语义，不重复调度 |
| **事件驱动（after: verify）** | — | `cli.py timeliness sync` | 核验后：台账 → 归属表时效同步（F-C03）；已由 run 主链 6.9 之后的阶段承接 |
<!-- SCHED:END -->

**Windows 计划任务样例**（Task Scheduler → 创建基本任务 → 启动程序）：

```
程序:   <python.exe 绝对路径>
参数:   tools\run_production_refresh.py --no-scrape
起始于: D:\WorkBuddy\regulatory_compliance_orchestrator
```

**cron 样例**（同语义；生成物）：

<!-- CRON:AUTO -->
```cron
0 6 * * *   cd /path/to/repo && python cli.py run --no-scrape --resume >> logs/cron_refresh.log 2>&1
0 1 * * 2   cd /path/to/repo && python cli.py run --collect nfra-weekly >> logs/cron_nfra_weekly.log 2>&1
0 7 * * 1   cd /path/to/repo && python cli.py run --only 6.9 >> logs/cron_verify.log 2>&1
0 8 * * 1   cd /path/to/repo && python cli.py run --only 21.5 >> logs/cron_publish_wiki.log 2>&1
0 9 1 * *   cd /path/to/repo && python cli.py gates >> logs/cron_monthly_check.log 2>&1
```
<!-- CRON:END -->

### 二·补　gov 源子源与 zhengceku 全量收敛（2026-09-15 新增）

gov 源现有**两个子源**，由 `gov_collector.py --source {xzfgk|zhengceku|all}` 分发，
产物合并进同一主库 `data/raw/gov_laws.json`，统一由 `run_clean_pipeline --project gov` 消费：

| 子源 | 栏目 | 规模 | 说明 |
|---|---|---|---|
| `xzfgk` | 行政法规库（`www.gov.cn/zhengce/xzfgk`，详情落 `xzfg.moj.gov.cn`）| 约 605 条 | 原有子源 |
| `zhengceku` | 国务院政策文件库·**国务院部门文件**（`www.gov.cn/zhengce/zhengceku/bmwj/home.htm`）| **629 页 × 28 ≈ 1.7 万条** | 2026-09-15 纳入 |

**增量语义**：`--source all`（编排默认）下 `--resume` 默认开启——基于 `detail_url` 跳过主库已有
且**已有正文**的条目、与主库按 `detail_url` 合并（新优先），故可反复调用而不会重复抓取。

**全量收敛**：zhengceku 历史存量约 1.7 万条，单条平均约 6 秒（无附件条目约 1.2 秒；
带 PDF 附件的条目可达 20 秒以上，PDF 抽取已用 `pymupdf→pypdf→pdfplumber` 优选链，无可压缩空间），
**全量约需 30 小时**。日常由「每日 `refresh --collect gov`」增量反复调用逐步收敛；
需要一次性推进时可手工执行下列任一种：

```bash
# ① 持续后台收敛（可随时中断，断点自动续跑）
python modules/regulatory_scrapers/collectors/gov_collector.py --source zhengceku
# ② 分批推进（每批 N 条后即写主库，天然断点）
python modules/regulatory_scrapers/collectors/gov_collector.py --source zhengceku --max-items 500
# ③ 彻底全量重抓（忽略主库）
python modules/regulatory_scrapers/collectors/gov_collector.py --source zhengceku --full
```

> **断点续跑（必须依赖）**：主库 `gov_laws.json` 只在全部结束后写一次，故详情阶段按
> `--checkpoint-every`（默认 200 条）原子落盘暂存 `data/raw/_zhengceku_partial.json`；
> 下次运行先载入暂存并入 `seen` 再续抓，最后统一并入主库。**中断不会丢失已抓数据。**

## 三、日常巡检与查询

```bash
python cli.py source diff                     # 快照变更监听（F-O02：日期/记录数/内容 sha）
python cli.py source list                     # 源清单与 collector 就绪度
python cli.py base query --rfn RFN-xxx        # 发布件精确查询（Base Contract v1）
python cli.py base search "银行代理保险"      # 全文检索（FTS5 trigram）
python cli.py base query --chain <文号>       # 同文号版本链（F-K08）
python cli.py base query --view active        # 现行有效视图
python cli.py rfn lookup --docno <文号>       # 归属表查询（含部分键回退）
python cli.py rfn register --theme T1 --title "..." # 登记（唯一写口）
python cli.py internal merged                  # 内部制度引用视图
```

## 四、告警与状态接点（rc 语义表）

| 入口 | rc/信号 | 含义 |
|---|---|---|
| `run_production_refresh.py` | rc=2 + `summary.failed` | 任一步失败（含 clean 尾部固定节点 rc=3）|
| `run_retrieval_after_checks.py` | rc=1 | 四门禁任一 FAIL（F-S01；报告见 `recall_audit/output/重跑执行报告.json`）|
| `cli.py gates` | rc=1 | 13 道门禁任一 FAIL（含空跑门禁改 FAIL 后，未实检≠通过）|
| 核验覆盖率门禁 | detail.warnings | < 0.70 拦截（当前 2975/4043=0.7358，未覆盖清单透明化）|
| `cli.py timeliness summary` | — | verify_summary 最近结论（overall/各源缺失）|
| `source diff` | — | 快照变更信号（同日改写可见）|

## 五、归集（外部语料）

```bash
python tools/ingest_corpus.py --src <目录> --domain <域>   # manifest 入 reports/corpus/，语料入 data/corpus/
python modules/regulatory_scrapers/collectors/supp_ingest_local_dir.py \
    --src-dir <目录> --task-index <ID> --doc-number <文号> --date <YYYYMMDD> [--run-clean]  # 法规类收录
```

> 维护约定：本表为调度唯一清单；新增/变更定时任务 → 更新本表并提交（防"不可见调度"复发）。
