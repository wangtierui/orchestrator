# 遗留项完成报告（2026-09-12）

> 指令：执行遗留项（第 2/3/4 项）+ 最后归集（第 1 项）；制定任务列表、自动推进。
> 前置：《修复批次完成报告_20260912》（阶段 1 已完成）→ 本批次完成 收敛清零 → 存量重刷 → 阶段 2/3 → 归集。

---

## 一、第 2 项 · 收敛专项（C-10 / C-11 / C-12）✅

| 项 | 修复 | 证据 |
|---|---|---|
| **C-12 回写 3 份 → 单点** | `apply_timeliness_to_cleaned.writeback_source()` 成为**全仓唯一回写实现**（jsonl+csv 双轨 + 原子写 + 备份 + status 派生）；`apply_source` 改薄壳（匹配逻辑留层）；`sync_three_modules.stage_scrapers` 与 `classifier_pkulaw_verify` 回写段改调单点（原三份分叉：字段集/双轨性/原子性/备份各不同） | 统一单点调用；apply dry-run 五源统计正常 |
| **C-11 核验 4 脚本 → 家族** | `supp_authority_verify.py` **删除**（被 `authority_backfill_verify --source supp` 完全覆盖：候选筛选/台账名/断点名一致）；无命中处置**统一"写核验痕"**（authority_backfill 原"不动"→ 与 verify_missing/classifier_pkulaw 同款，防重复查询）；三脚本头注加家族全景（各自场景+共享工具链+回写单点） | 家族 3 脚本并存且语义一致 |
| **C-10 sync 双实现分叉** | 由 C-12 收敛自然消解（两处回写不再各自实现写盘）；`A-08` 标注 sync 为**辅助编排**（生产唯一编排=run_production_refresh） | — |

## 二、第 3 项 · A-10 副本清零 + 存量重刷 ✅

### 2.1 A-10 归一 SSOT 分层（16 文件，提交 `a32a150`）
- `std_lib/common_lib/norm.py` 扩容为**三层 SSOT**：`norm_docno`（标准）/ `norm_title`（激进）/ `norm_title_strict`（保守精确匹配），并文档化豁免清单。
- **12 处标准层副本 → import**：merged / verify_regulatory_citations / build_detail_tables / classifier_pkulaw_verify / sync_three_modules / verification_state / gate_citations（死函数清除）/ gate_timeliness_ssot / reconcile（title）。
- **10 处有意特化 → `# norm-specialization` 豁免标记**：registry / rfn / scanner / build_outputs / consolidate / build_upper_laws / apply / reconcile（宽容预处理）。
- `gate_no_duplicate_libs` 升级：**未豁免副本 → FAIL**（防增量）；实测 `private_norm_unmarked = {}`（清零达标）。

### 2.2 存量重刷（提交 `1554d9c`）
| 阶段 | 结果 |
|---|---|
| 五源 clean | 0912 快照（gov 15s / mof 27s / pbc 13s / nfra 30s / supp 9s）|
| 时效回写 | 单点回写 **3267 条** + status 全量派生（**ts≠status 不一致 = 0**）|
| **F-S05 OCR** | **1335 条** cleaned 正文应用 OCR 校正（`_metadata.ocr_corrected`，原零调用）|
| classify --all | 10 主题强序重跑（幂等）|
| reconcile / recall / gates | C1 applied；四门禁 PASS；gates 13 PASS |
| 附带 | 备份清洁（释放 971MB）；F-C05 钩子补 `status_derived` 触发（修第二轮派生漏 rebuild）|

## 三、第 4 项 · 阶段 2（P1）与阶段 3（P2）✅（核心落地 + 快赢批次）

### 3.1 Base Contract v1（阶段 2 核心，提交 `1554d9c`）
- 新增 `modules/base_publish/`：`build_external`（**external_records 4043 / external_clauses 61433 / external_attachments 2329** + manifest）/ `build_internal`（**policies 107 / clauses 1272**）/ `build_fts`（**SQLite + FTS5 trigram** 双库）。
- `interfaces/base_api.py`：三模块**唯一底座访问面**（query_external / search_external / get_external / query_internal / search_internal / get_policy / manifest / version_chain / view_active）。
- `cli.py base publish|query|search`（F-K03/F-O09 统一查询入口）；refresh 阶段 5.5 接线；`tests/test_base_publish.py` 验收。
- 实测：主题过滤 / "银行代理保险" 全文 / 条款级 / 内部制度 全部命中。

### 3.2 阶段 2 配套快赢（提交 `0086f67`）
F-S08（clean 尾部失败不再伪装成功 → rc=3）· F-S11（报告动态日期）· F-O10（`rfn.ranges()` 三元对齐，`--list-ranges` 修复）· **F-D11 契约单源化**（contract re-export unified_schema；registry re-export contract；recall Gate4 键集 import contract——4 处字面量副本清零）· F-O06/O07（refresh 接 internal merged + reports）· **F-C08**（merged_view 陈旧校验：inputs 指纹重算比对，陈旧即 FAIL）· F-C03（`cli timeliness sync` 显式入口）· F-K07（`cli timeliness summary` 告警接点）· F-R03（classifier backups 57 份清理）。

### 3.3 阶段 3 快赢 + llm_wiki 接入层（提交 `fab2983`）
- `tools/sync_wiki_sources.py`：发布件 → llm_wiki 监控源导出（`<rfn>__<title>.md` + YAML frontmatter 溯源 + 正文截断控 token + **SHA256 增量** `.wiki_sync_manifest.json`）；llm_wiki 未装亦可独立运行。
- `base_api.version_chain()` + `view_active()`（F-K08 版本链/现行视图最小实现）+ `cli base query --chain/--view active`。

## 四、第 1 项 · 最后归集 ✅（提交见 `tools/ingest_corpus.py`）

新增 `tools/ingest_corpus.py`（manifest + 幂等物理归集；清单入 `reports/corpus/`，本土入 `data/corpus/` 不入库）：

| 数据域 | 文件数 | 体量 | 说明 |
|---|---|---|---|
| **EAST2.0 全域**（east2_m20）| 1990 | 1807.1 MB | 含报送文档/需求文档/修订通知全套/相关法规/保单登记三期（9 文件/32.8MB）/培训答疑等 |
| **公司各部门制度**（dept_policies）| 1019 | 316.8 MB | 26 部门制度（财务 187/投资 136/制度检视 93…）|
| **合计** | **3009** | **2123.8 MB** | 总索引 `reports/corpus/_index.json` |

- 配套：`gate_flat_layout` 白名单加 `corpus`（归集根合法子目录）；归集工具示例去盘符（R4）。
- 归集后：ruff 0 / gates 13 PASS / pytest 32 全过。

---

## 五、遗留登记（下一批建议）

| # | 项 | 说明 |
|---|---|---|
| 1 | **llm_wiki PoC** | 需安装桌面应用（GPL v3）；同步脚本已就绪（`--out` 指向其监控夹）|
| 2 | F-D14 状态文件 schema_version | classify_state / clause state 等 12 个状态文件补齐（本轮未做）|
| 3 | F-L01/L02/L03 交付补齐 | 规划 2.1 五级交付 14 项、3.2 六交付生成器、法宝核验联入分析层 |
| 4 | F-D06/D07/D09/D10 字段契约深化 | 五源字段归一收口、ATTACHMENT_FIELDS 契约、双轨权威声明、条款行维度 |
| 5 | F-O02/O03/O04/O08 | 变更监听器、定时任务可见化、nfra_weekly 消费、match 内存优化（335MB 驻留）|
| 6 | F-R06 重复实现群 | HTTP4/附件5/保存4/归一19/标题11/枚举5 → 维护点 50→12（收敛批次）|
| 7 | 归集后续 | 语料结构化（EAST 报送文档/需求文档入监管统计域解析、部门制度入 IPB 摄取 → 1019→全部）|
| 8 | 阶段 3 剩余 | F-L04/L06/L08（关系图/时间线/截断声明）|

> 编制：CodeBuddy · 2026-09-12 · 批次提交：`a32a150` / `1554d9c` / `0086f67` / `fab2983` + 本报告
