# regulatory_compliance_orchestrator — 最终实施蓝图 v1.1（含检视修订 R1~R24）

> 状态：**实施基线（已批准分阶段执行）**。v1.1 = v1（合并终稿）＋《蓝图检视报告》R1~R24 修订并入。
> 日期：2026-09-08 ｜ 根目录：`D:\WorkBuddy\regulatory_compliance_orchestrator`
> 本文档为**分阶段实施唯一基准**；v1/检视报告留档 reports/ 供追溯。

---

## 0. 决策与修订总账（现行有效）

| 编号 | 决策/修订 | 结论 |
|---|---|---|
| D-01/D-04 | 原四目录零改动只读 | 复用一律"复制进新仓后修改"；活跃数据按 manifest 复制 |
| D-02 | modules/ 命名沿用现名 | `modules/{regulatory_scrapers,regulatory_classifier,internal_policy_base,internal_policy_drafter}` |
| D-03 | 内部制度独立 `IPN-<hex>` | 不进 RFN 归属表；classifier 仅主题对齐 |
| D-05 | 不设统一 data 根 | 数据随仓；manifest 追踪 |
| D-06 | compliance-closure/query-report/monitor-daemon | 二期预留；merged_view schema 版本化冻结 |
| D-07 | **启用 git** | 本仓 git；数据不入库，复制快照+manifest |
| Q1 | RFN↔clean 桥 | 独立 `rfn_clean_bridge.csv`（归属表 8 列零变更） |
| Q2 | C1 自动刷新归属表（RFN 不变） | C2 实体变化仍人工确认 |
| Q3 | 接受同仓 import 取代跨仓 sys.path | 全仓禁止 sys.path 盘符 |
| Q4 | nfra weekly 保留内部确定性编排 | 仅入口挂统一 CLI |
| N1 | raw 仅 json | 删 raw csv |
| N2 | clean sanitize 内置 pipeline | 五源统一；jsonl 保留 \n |
| N3 | 附件文本统一入 `attachment_content` | gov/mof/nfra/pbc 改 supp 同款 |
| N4 | supp 入时效核验；map_supp 去默认 valid | 保空+needs_review |
| N5 | 全量时效清单补 RFN/dedup_key | |
| N6 | 时效 SSOT=verification_state.json | 单路传播，双路直写关停 |
| N7 | 命名/类型统一 | real_year→str；空串契约；source+source_origin 收敛 |
| R1 | 明细表 SSOT = `scripts/build_detail_tables.py` | node2 v2 生成器不迁 |
| R2 | 主题归属唯一写接口 `theme_api.set_theme` | 3 写者（merge_final_theme/merge_review107/registry）收敛 |
| R3 | 效力单路传播 | classifier_pkulaw_verify 直写收编为 sync 内部步骤；diff_timeliness 只读比对 |
| R4 | 路径唯一入口 = 根 `paths.py` | 不设 common_lib/paths_api |
| R5 | scraper_std 枚举 re-export from config.enums | + assert_enum_bindings 防漂移 |
| R6 | 时效四层一致性断言入 gate_timeliness_ssot | |
| R7 | 桥表写者=reconcile 后处理（source_url 主锚 upsert） | |
| R8 | classify 内强序子步 a..e + hash 断点 | |
| R9 | report_builders 输出/中间产物全改 paths 派生 | 消除旧一次性目录硬编码 |
| R10 | 底座/明细/桥表补 provenance 字段 | generated_by/at |
| R11 | 调度矩阵 + 命令级 fs_lock 互斥 | 单一 automation |
| R12 | retrieval 数据门禁 vs gates 交付门禁语义界定 | |
| R13 | timeliness verify 三态 + 执行摘要 | |
| R14 | C1 自动刷新联动 needs_rebuild | |
| R15 | sources.yaml 唯一源目录 + source add checklist | |
| R16 | 主题按 THEME_MAP 遍历 | EXPECT 动态生成，去 T1-T10 硬编码 |
| R17 | OCR 路径 config/ocr.yaml 化 | gate_hardcoded_paths 含 tess 黑名单 |
| R18 | internal 枚举只增不删 + UNALIGNED 桶 | |
| R19 | 旧仓 std_lib FROZEN.md + automation 全切新仓 | 双库漂移防制 |
| R20 | common_lib 与 scraper_std.common 边界文档化 | common_lib 不 import scraper_std |
| R21 | document_structure 入 scraper_std | 双条文正则消除 |
| R22 | 对照基线 sha256 清单 + 迁移不变性自动验收 | manifest + baseline |
| R23 | gates 类别化，数量以 ALL_GATES 为准 | 文本禁写死数量 |
| R24 | SSOT/门禁/调度/契约落盘为程序可读清单 | config/schema + manifest |

---

## 1. 目录结构（v1.1 生效版）

以 v1 §2 为准，追加 v1.1 修订：不设 `common_lib/paths_api`；`modules/regulatory_classifier/scripts/` 只保留 SSOT 生成器（build_detail_tables），node2 v2 不迁；`std_lib/scraper_std` 增加 `document_structure.py`（R21）；reports 外新增 `data_migration_manifest.json` 与 `reports/baseline_<date>.json`（R22）。

## 2. SSOT 总账（v1.1 修订版）

| 事项 | 唯一权威 | 唯一写者（v1.1） | 读取口 | 派生方 |
|---|---|---|---|---|
| 路径 | paths.py | 维护者 | import | — |
| 受控枚举 | config/enums.py | 维护者 | import | scraper_std re-export(R5) |
| cleaned 位置 | clean_index/index.json | clean_index 重建 | clean_index_api | — |
| 监管归属/RFN | 归属表(8列) | rfn_api.register_doc | rfn_api | 索引/镜像 |
| RFN↔clean 桥 | rfn_clean_bridge.csv | reconcile 后处理(R7) | rfn_api | — |
| 主题 | 主题归属表(3列) | **theme_api.set_theme(R2)** | theme_api | 底座 theme 视图 |
| 明细表 | 11 csv | **build_detail_tables(R1)** | contract | 底座/报告 |
| 数据底座 | 40 JSON | build 脚本族 | classifier | — |
| 内部制度索引 | internal_policy_index.json | internal indexer + drafter 发布 | internal_policy_api | 版本链 |
| merged_view | merged_view.json | aligner | interfaces | 版本化(D-06) |
| 效力状态 | verification_state.json | verify 脚本 mark_checked | mirror | cleaned/归属表/mirror/eff_status/清单（单路 R3） |
| 审计 | audit_log.json | io_atomic.audit | — | — |

## 3. pipeline 阶段（v1.1 修订版）

| 阶段 | 命令 | 关键修订 |
|---|---|---|
| P1 collect | `orchestrator collect --source {s}` | sources.yaml 派生(R15)；nfra weekly 子模块(Q4) |
| P2 clean | `orchestrator clean --project {s}` | sanitize 内置(N2)；raw 仅 json(N1) |
| P3 index | `orchestrator index --refresh` | — |
| P4 retrieval | `orchestrator retrieval` | 数据门禁(R12) |
| P5 classify | `orchestrator classify` | 强序子步 a..e + hash 断点(R8)；THEME_MAP 遍历(R16)；reconcile_clean_drift(R7) |
| P5b report | `orchestrator report --theme {T}` | report_builders paths 派生(R9) |
| P6 internal | `orchestrator internal scan` | scanner/ocr/parser/normalizer/indexer |
| P7 align | `orchestrator internal align` | merged_view；UNALIGNED 桶(R18) |
| P8 draft | `orchestrator draft` | drafter 读 merged_view |
| P9 gates | `orchestrator gates` | 12 道 ALL_GATES(R23) |
| T0 timeliness | `orchestrator timeliness {verify,sync,...}` | 自动传播(R3/R6/R13/R14) |

调度矩阵（R11）见检视报告 §四；实施中落地为 `config/schedule.yaml`（R24）。

## 4. 门禁（v1.1：ALL_GATES 12 道）
SSOT-junction / rfn_sync / citations / hardcoded_snapshots / flat_layout / pipeline_contract / enum_values / **rfn_drift** / **no_duplicate_libs** / **hardcoded_paths** / **timeliness_ssot** / contract —— 实现以 `gates/__init__.py ALL_GATES` 为准。

## 5. 阶段实施顺序与 git 提交节奏
P0（已完成，commit ee3e535）→ P1…P7，每阶段：复制→改造→测试→gates 绿→`git commit`。R22 基线在各阶段启动前对比。

## 6. 最终交付要求
- 完整标准 README：运行说明 + 架构 + 全部数据表（含字段级数据结构字典：表名/字段/类型/含义/来源/消费者/枚举约束）。
- 最终 README 由 P7 汇总生成（程序可读契约 + 人工校对）。
