# 全流程审查与缺陷修复报告（2026-09-26）

> 任务：对 regulatory_compliance_orchestrator 进行全面审查与重构，真实生产环境完整运行全流程，
> 精准识别并修复数据阻塞、流程断点、门禁失效、统一入口自动化、唯一事实源违背、硬编码等缺陷。
> 结果：完整全流程（`cli.py run --no-scrape --resume`）跑通，`tools/ci_check.py --cov` **CI: PASS 5/5**。

---

## 1 缺陷清单与修复（6 项）

| # | 类别 | 缺陷 | 根因 | 修复 | 验证 |
|---|---|---|---|---|---|
| **R-01** | 数据阻塞 | supp 源 clean 隔离率 6.67% > 2% 阈值 → rc=2 → 下游全链阻塞 | `UNIFIED_SCHEMA` 把 `source_url` 定义为严格 `url`（`^https?://`），supp 源（本地补充材料）的合法来源标注（本地文件路径/媒体说明）被误判为校验失败 | `schema_validation.validate_record` 的 url 校验**精准降级**：带 scheme 但非 http(s)（`ftp://`）仍报 error；无 scheme 的来源标注记入 `validation_warnings`（可审计不阻断） | supp clean 失败步骤 0 |
| **R-02** | 数据治理 | supp 源混入 2 条非监管文件（行业自律规范/示范文本），其中 1 条无文号致 `index_no` 校验失败 | 中国保险行业协会自律规范/示范文本被人工录入"补充法规库"，非监管文件且无监管文号 | 备份并移除 2 条（`backups/supp_nonregulatory_20260926.json` 可回滚），supp 62→60 条 | supp clean 隔离率归零 |
| **R-03** | 流程断点 | `apply:{src}` 5 源全 FAIL（`ModuleNotFoundError: config`） | 第九批 ExitCode 收敛把 `from config.exitcodes import ExitCode` 插在仓根（`_ORCH_ROOT`）引导**之前**，standalone 子进程 sys.path 无仓根 | 移到仓根引导之后；其余 13 个同批收敛文件静态核查均位置正确 | apply 5 源 rc=0（写回 783/… 条） |
| **R-04** | 统一入口 | `cli.py run --no-scrape` 被 doctor 前置因 token 缺失拒绝（需手动 `--skip-doctor`） | `QUICK_IDS` 含 `token`，但 token 是**核验步骤**依赖，非 run 主链必要条件 | 从 `QUICK_IDS` 移除 `token`（核验步骤自带 R13 降级；全量 `cli.py doctor` 仍检查） | `run --no-scrape` 免 `--skip-doctor` 直接通过前置自检 |
| **R-05** | 唯一事实源 | `apply_timeliness_to_cleaned.py` 硬编码 `TIMELINESS_STATUS` 枚举副本 | 历史上手写 7 值，与 `config/enums.py` 双份定义 | 改 `from config.enums import TIMELINESS_STATUS`（唯一事实源） | import 后 7 值一致 |
| **R-06** | 门禁失效 | 修复过程注释引入盘符字面量 `D:/` | 注释举例用了盘符 | 改注释为不含盘符的"本地文件路径" | `gate_hardcoded_paths` PASS |

## 2 完整全流程运行结果

`cli.py run --no-scrape --resume`（950s → 修复后 300s 全绿）：

| 阶段 | 结果 |
|---|---|
| clean 5 源 | 通过（supp 重跑后隔离率归零） |
| timeliness:consolidate | 通过（全量清单 3901 条） |
| apply 5 源（时效回写） | 通过（gov 783 条等） |
| classify / relations / reconcile / governance:sync / recall / internal:merged / reports:build / base:publish / analysis:gen / watch:baseline | 全部 rc=0 |
| gates | **22/22 PASS**（修复盘符字面量后） |

交付库 `docs/reports/` 17 项 + 关系图谱 + RFN 补登清单已随全流程刷新（supp 数据治理 + 时效回写后的最新状态）。

## 3 验证

| 检查 | 结果 |
|---|---|
| `tools/ci_check.py --cov` | **CI: PASS 5/5**（ruff 全绿 ｜ mypy 0 error ｜ pytest 488 ｜ gates 22/22 ｜ coverage 45%） |
| 新增回归测试 | `test_validate_record_url_note_vs_scheme`（url 降级三分支：本地路径/括号说明/ftp 协议错误） |

## 4 审查结论

本次审查聚焦**数据校验逻辑**与**唯一事实源**原则，发现的核心缺陷是：
- **数据校验逻辑过严**（`source_url` 的 url 校验对 supp 本地补充材料源不合理）——这是数据阻塞的直接根因；
- **统一入口未全流程自动化**（token 前置阻断非核验场景）；
- **一处 ExitCode 收敛回归**（子进程 standalone 导入失败）——仅靠门禁/单测无法发现，须**真实全流程运行**才暴露。

均已修复并通过真实全流程验证。剩余非阻塞数据质量待办（supp 源 2 条文号占位：`#14 2023年（无正式文号）`、`#30 （无发文字号）`，属真实文件文号未取得，source_url 降级后不再阻塞，需人工补文号）已记录，不影响端到端闭环。

**文档结束。**
