# 重构执行报告 · 第四批（2026-09-26）

> 依据：`reports/全链路重构方案_修订版_v2_20260926.md` §3.5（P2-1）、§3.9/§3.13（P2-2/P2-6）、
> §3.12.4-§3.12.6（P2-3b）、§3.14.3/§3.14.5（P2-5）、§3.11（P2-4 部分）
> 范围：**P2-1 / P2-2 / P2-3b / P2-5 / P2-6 五项闭环**；累计路线图进度 **20/22**；工作树残留 **0**。
> 验证：`tools/ci_check.py` **CI: PASS 3/3**（ruff 全绿 ｜ pytest **401 通过** ｜ gates **22/22 PASS**）

---

## 1 任务完成清单

| ID | 任务 | 交付物 | 验收证据 |
|---|---|---|---|
| **P2-2** | 调度事实源唯一化 | `config/schedule.yaml`（新）、`tools/gen_schedule_doc.py`（新）、手册自动段、判据 **S** | 7 个作业（5 cron + 2 事件驱动）× `notify` 段；手册定时表由 yaml 反向生成（首次消除了手册/实测的 01:00 vs 01:30、06:30 vs 06:00 二义，记为**决策 D-12**）；判据 S1–S4 全绿，且**手改手册即 FAIL**（测试含负例） |
| **P2-1** | 条件触发链 | `config/triggers.yaml`（新）、`std_lib/common_lib/triggers.py`（新）、`commands/triggers.py`（新）、判据 **T** | 5 个触发项（timeliness_verify / internal_update / supp_ingest / inbox_drop / wiki_sync）；**6 类可机器判定条件**；决策表实测输出证据（如 `unindexed_originals → 满足（原件 879 份 / 索引 878 条）`）；**条件无实现 → 不放行**（纪律②，含负例测试） |
| **P2-3b** | 投放区与扫描器 | `config/inbox_registry.yaml`（新）、`tools/inbox_scan.py`（新）、`data/inbox/{internal,regulatory_stats}/`、`inbox_drop` 触发项 | 两域（dept_policies / regulatory_stats）+ 准入策略 `new_and_unhashed_only`；sha256 去重（命中归集本体或 `originals/` → `duplicate` 跳过）；不可识别扩展名 → `needs_review` + worklist（含测试） |
| **P2-5** | 六类决策项接入 worklist | `rfn_backlog.py`(D1)、`align.py`(D2)、`pkulaw_cli.py`(配额阻断)、`inbox_scan.py`(投放区) | **9 类 kind 全部有产生方**，J7 双向闭合 pass；`_PENDING_PRODUCERS` 仅剩 D6（见 N-27）；新增 `trigger_manual_breakpoint`（触发链 `manual_breakpoint` 的落点） |
| **P2-6** | 统一执行入口 | `commands/{run,doctor,status,schedule}.py`（新）、`tools/install_schedule.py`（新）、`std_lib/common_lib/notify.py`（新）、`run_step` 台账（`_SCHEMA_INT` 4→5） | `cli.py` 命令数 **13 → 18**；`run` 含 `--resume/--from/--only/--dry-run/--json-logs/--list-steps/--triggers`；`doctor` **18 项**（quick 子集 10 项，被 `run` 作为前置门）；`status` 四段披露 + `--json/--exit-code`；`schedule print/list/install/verify/remove` |
| **P2-4**（部分） | 测试补齐 | `tests/test_p2_governance_layer.py`（新，13 例） | 用例总数 **388 → 401**；含判据 S4 负例、条件缺实现的负例、投放区不可识别扩展名的正例 |

**命令数口径变更**：v2 §3.13.2 计划 12 → 17；实际 12 → **18**（额外 +1 = `triggers`）——
P2-1 的触发链需要**可验证的驱动面**（`triggers table/run`），否则「条件触发」只能靠读 yaml 推断。
已在 `cli.py` 帮助文本与本报告登记。

---

## 2 变更摘要

### 2.1 新增文件（7）

| 文件 | 作用 |
|---|---|
| `config/schedule.yaml` | 调度唯一事实源（作业/漏跑策略/告警通道） |
| `config/triggers.yaml` | 条件触发唯一事实源（条件/步骤/失败语义） |
| `config/inbox_registry.yaml` | 投放区「域 → 管线」映射唯一事实源 |
| `tools/gen_schedule_doc.py` | yaml → 手册定时表 + crontab（`--check` 供判据 S4） |
| `tools/install_schedule.py` | yaml → Task Scheduler XML / crontab，`--print/install/verify/remove` |
| `tools/inbox_scan.py` | 投放区扫描与路由（sha256 去重 / needs_review / manifest） |
| `std_lib/common_lib/notify.py` | 告警通道（file 默认 / webhook 可选，URL 只从 env 读） |
| `commands/{run,doctor,status,schedule,triggers}.py` | 统一执行入口五命令 |
| `tests/test_p2_governance_layer.py` | 13 例回归 |

### 2.2 行为级变更（B1–B7）

| # | 变更 | 影响 |
|---|---|---|
| B1 | `cli.py run` 成为**唯一主链入口** | `tools/run_production_refresh.py` 降级为库 + legacy 直调（`__main__` 打印弃用提示，保留一个版本，R14） |
| B2 | 步骤级运行台账 `run_step`（`_SCHEMA_INT` 4→5） | `status` 可披露"本轮哪些步骤失败"；`run --resume` 的跳过集有了事实源 |
| B3 | `run --resume` 的跳过判据**与水位同源**（v2 R13） | skip ⟺ 上次该步 rc=0（且非跳过）**且**全库水位无 stale/未登记；否则**全量重跑**（宁重跑不跳） |
| B4 | 失败自动告警 | 汇总失败 → `reports/_alerts/<ts>_failed_step.{json,md}`（无人值守的硬缺口） |
| B5 | `doctor` 成为 `run` 的前置门 | 环境 FAIL → `run` 拒绝执行并返回 `ExitCode.ENV(3)`（`--skip-doctor` 可绕过，排障用） |
| B6 | 手册定时表改为**生成物** | 判据 S4 断言一致；`reports/运行手册_编排与定时_20260912.md` 顶部已加"请勿手改"提示 |
| B7 | 触发链失败 → worklist | `on_fail: manual_breakpoint` 必写队列（v2 §3.5 纪律④；新 kind `trigger_manual_breakpoint`） |

---

## 3 执行中发现并处置的问题项（6 项）

| # | 问题 | 处置 |
|---|---|---|
| **N-25** | **方案 §3.13.3 的 `--from/--only` 按"阶段 id"表述，与实装不符**：主链步骤名为 `<name>:<sub>`（`gates`/`base:publish`/`governance:sync`），且含**动态族** `collect:{src}`/`clean:{src}`/`apply:{src}`；若按方案引入「阶段 id ↔ 步骤名」映射表，将新增**第三处清单**（违反 §3.1.1 SSOT 与 v2 R13"不得自建第二套判据"） | 改为**以步骤名为准** + `--list-steps` 发现 + `STEP_ORDER` 单一清单（含 `{src}` 动态族，前缀匹配）；测试守住清单与调用点一致（`test_run_step_order_matches_call_sites`） |
| **N-26** | **方案 §3.12.4 的落点与仓内纪律冲突**：`data/inbox/_registry.yaml`，但仓根 `.gitignore:11` 为 `data/`（**整目录**排除，且 git 不允许在被排除目录内反选 `!`）→ 声明无法入库、新克隆会缺文件 | 声明落 `config/inbox_registry.yaml`（与 schedule/triggers 同类）；`data/inbox/_registry.yaml` 保留为**运行期覆盖**（两者不并存，避免 §3.12.7 F2 的"双副本"教训） |
| **N-27** | **D6（`relevance_boundary`）本批未接**：`filter_clean_relevance.py` 当前是"批量分层导出（INCLUDE/BOUNDARY/EXCLUDE）+ `--decisions` 过滤"，逐条登机需先定"逐条裁决"与过滤语义的关系（否则一次全量导出会把数千条 BOUNDARY 灌进队列） | 保留在 `_PENDING_PRODUCERS` 并在 J7 detail 中披露；**P2-5 剩余项**（见 §5 遗留风险 R3） |
| **N-28** | 冒烟暴露 3 处自身缺陷：① 三个命令文件误写 `from common_lib import …`（应为 `std_lib.common_lib`）→ 命令直接崩；② `commands/doctor.py`/`status.py` 内 `sys.path.insert` **触碰 `gate_import_bootstrap` 的 commands/ 硬零层**；③ 新表 `run_step` 在旧库未迁移时读侧抛 `OperationalError` | ① 改全限定导入；② 改经 `bootstrap(include_tools=True)`；③ 读侧容错（`sqlite3.Error` → 空表 + 提示跑 `governance init`），并已对有库执行迁移（11 张表就位） |
| **N-29** | **模块 docstring 的阶段编号与实装顺序漂移**：docstring 写「6 gates → 6.5 watch:baseline → 6.8 analysis:gen」，实装为 `gates → analysis:gen → watch:baseline` | **以实装为准**记入 `STEP_ORDER` 注释与报告；未改 docstring（编号体系属旧口径，待 P2-7/文档轮统一） |
| **N-30** | `config/inbox_registry.yaml` 的 YAML **双引号串内含双引号**（`"…仅"真法规"子集…"`）→ 解析失败 | 改中文引号并留注释（YAML 陷阱）；`inbox_scan` 解析恢复 |
| **N-31** | **N-26 的同心问题**：既然 `data/**` 不入库，`data/inbox/<域>/` 目录也**不入库** → 新克隆缺该目录，`doctor` 的 ⑭ 会 FAIL，且扫描器无目录可扫 | ① `inbox_scan --apply` 按注册表**幂等创建**域目录（与 `governance init` 建库同思路）；② `doctor` 把"投放区未初始化"从 FAIL 降为 **warn + 修复命令**（判据 ⑭ 的本意是**自指环防护**（inbox ≠ corpus），"缺失"是待初始化而非环境缺陷） |

---

## 4 验证结果

| 检查 | 结果 |
|---|---|
| `tools/ci_check.py` | **CI: PASS 3/3** |
| `ruff check .` | All checks passed |
| `pytest tests -m "not data"` | **401 通过**（上批 388 + 新增 13） |
| `cli.py doctor`（N-31 修复后） | `inbox_corpus` = **OK**（`tools/inbox_scan.py --apply` 已幂等创建域目录） |
| `python cli.py gates` | **22/22 PASS**（含新判据 S/T、J7 双向闭合） |
| `cli.py doctor` | 18 项：**ok=12 warn=5 fail=1**；唯一 FAIL = `.pkulaw_token` 缺失（**环境事实**，非缺陷：效力核验触发项因此 `enabled=False`，条件判定正确） |
| `cli.py status` | 本轮 RUN-20260922-…ok；水位 22 条 / 0 异常；待办 0 open |
| `cli.py triggers` | 5 项决策表（含证据与 `on_skip`）；`internal_update` 实测满足（原件 879 / 索引 878 → 未索引 1） |
| `cli.py schedule list` | 5 个任务名与 cron 表达式（`REG_ORCH_*`） |
| `cli.py run --dry-run` | 逐步 `SKIP（--dry-run）`，无副作用（步骤标记 `skipped=1`，**不会**被 `--resume` 误当作已完成） |
| `BENCHMARK.md` | 已重跑：`gates=22 tests=31 attr=1162 base=1073 final=1073 detail_rows=1162 bridge=943 state=14797 merged=878` |
| `git status --porcelain` | 0 条 |

---

## 5 遗留风险说明

### 5.1 高

| # | 风险 | 说明 | 建议 |
|---|---|---|---|
| **R1** | 路线图 **20/22** | 未做：**P2-3**（`retention.py` + `archive/`）、**P2-4 剩余**（用例 ≥450、coverage `fail_under=20`、ci_check 加 mypy）、**P2-7**（退役收尾 + `gate_repo_hygiene` J6 转严）、**P3-1~P3-4**、**P3-5**（未决） | 下一批：P2-4（纯测试/CI，风险最低）→ P2-7 → P2-3；P3-5 需先定顺序解耦方案 |
| **R2** | **P3-5 未决**（gates 阶段 6 / `analysis:gen` 阶段 6.8 顺序耦合） | `analysis:manifest` 仍无法声明依赖边 → 水位覆盖度真实缺口；本批的 `STEP_ORDER` 反而**暴露**了该漂移（N-29） | 建议随 P2-7 一并定案：把 gates 后移到链尾或把 analysis 前移，二选一需评估 `gate_result` 归档与 BENCHMARK 基线 |
| **R3** | **P2-5 剩 D6**（`relevance_boundary`） | BOUNDARY 逐条登记会灌爆队列（数千条）；需先定"只登记人工已介入的子集"或"抽样"口径 | 与 P3-3（监管统计域下游）一并处理；当前 J7 会持续披露该项待接 |
| **R4** | `gate_runtime_hygiene` 判据①②仍只披露 | 裸整数退出码、`except-pass` 存量未收敛 | 需 R3 类型的存量收敛批（本批新增代码已全用 `ExitCode`/LOG） |

### 5.2 中

| # | 风险 | 说明 |
|---|---|---|
| **R5** | **计划任务未安装** | `cli.py schedule verify` 报 5 项"未安装"——安装会改**本机系统状态**（`schtasks /create`），故**未自动执行**；请人工确认后跑 `python cli.py schedule install`（`--print` 可先审 crontab/XML） |
| **R6** | `run --resume` 的跳过面**偏保守** | 只有当"全库水位无 stale/未登记"时才跳过任何步骤；水位整体异常时全量重跑（**这是刻意的**：正确性优先，见 R13；代价是单点水位异常会让续跑退化为全跑） |
| **R7** | `run` 默认**不执行**触发链 | 主链既有阶段已覆盖多数链外节点（`internal:merged`/`base:publish`），自动追加会双写；故 `--triggers` 为显式开关（P2-1 的"链内条件执行"接入留待后续） |
| **R8** | 判据 **R（步骤清单一致性）未接** | `STEP_ORDER` 与 `_run("<step>")` 调用点的一致性目前由 **测试**守住（`test_run_step_order_matches_call_sites`），尚未升为门禁判据（已写入 `gate_config_integrity` docstring 的"待接"位） |
| **R9** | 交付件数据面仍为 09-22 快照 | `docs/reports/*`（19 份）未重跑；`gate_clean_schema` 的 `LEGACY_WITH_ERRORS={nfra:5, supp:4}` 待全链 clean 归零 |

### 5.3 低

| # | 风险 |
|---|---|
| **R10** | `notify` 的 `webhook` 分支**未实测**（无 URL）；file 通道已实测可用 |
| **R11** | `install_schedule.build_xml` 仅支持"具体分/时 + 日/周/月"形态，列表/步进表达式会**显式报错**（不猜）——当前 yaml 全部落在支持范围内 |
| **R12** | `doctor` 的 `token_fresh` 恒为 `warn`（**无法本地判定 token 有效性**，不假装能判） |
| **R13** | `paths` 仍有 7 个预留守位常量；`data/inbox` 目录为空（尚未有真实投递） |

---

## 6 下一批建议

1. **P2-4**（测试与 CI 收口）：用例 401 → ≥450、coverage `fail_under=20`、`ci_check` 加 mypy（非阻断）、`T3` 脆弱断言改造（`== 40`/`== 11`/`== 17` 改为派生）。
2. **P2-7**（退役与仓根洁净）：`gate_repo_hygiene`（J6 转严）、处置 `file_list_watcher_bak.py`/`_p.log`。
3. **P2-3**（数据生命周期）：`tools/retention.py` + `archive/` 加入门禁排除集（256 项 backups → 10）。
4. **判据 R**（步骤清单一致性）升为门禁 + **P3-5** 顺序解耦定案。
5. **R3 存量收敛**（裸整数退出码 / `except-pass`）为判据①②转严铺路。

**文档结束。**
