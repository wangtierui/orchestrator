# 三项落地完成报告（OCR / EAST 排除 / Obsidian，2026-09-12）

> 依据用户指令：①本机已装 Obsidian；②本机已部署 PaddleOCR-3.7.0 + tesseract-ocr；
> ③EAST 报送文档中的「00.报送文档、核查手册、立项申请、需求文档、邮件宣导、相关法规」不纳入。
> 提交：`e854c93`（OCR）/ `6b68518`（EAST）/ 本报告（Obsidian PoC）。

---

## 一、OCR 通路接通 + 扫描件回填（`e854c93`）

### 1.1 本机部署对齐（R17 预期布局）
| 项 | 处理 |
|---|---|
| tesseract v5.4（`C:\Program Files\Tesseract-OCR`，含 chi_sim/eng/osd） | 仓内 `external/tesseract` **junction** 指向系统安装（R17 默认值即此路径，零硬编码） |
| chi_sim 语言包 | 系统内为 **旧格式（10.9MB）tesseract5 拒载**（实测 `Error opening data file`）→ 下载 **tessdata_fast v5**（2.4MB）落仓内 `tessdata/`（实体目录）+ eng/osd 复用系统 |
| python 依赖 | 装 `pytesseract` + `numpy`（workbuddy 3.13.12 环境） |
| PaddleOCR 3.7.0 | 当前环境未探测到 python 包（`import paddleocr` 失败）→ **OCRConfig.engine=auto 降级链自动走 tesseract**；Paddle 就绪后无需改动（优先级已内置） |

### 1.2 代码修复（3 处真实缺陷）
1. **`config/loader` 占位符嵌套**：`${A:-${B}}` 语法原正则提前截断 → `tesseract_bin` 展开残留未生效 → 支持一层嵌套 + 迭代展开；
2. **`OCRConfig.from_config()`**：原 `OCRConfig()` 只用环境变量默认值，与 `config/ocr.yaml` **脱节**（R17"yaml 化"实际未接通）→ 新增 from_config（取值：环境变量 > ocr.yaml > 内置默认），`UnifiedOCR` 默认走它；
3. **`extract.reocr_backfill` + `cli internal reocr`**：存量扫描件回填工具（目标 `text_chars==0`）——文本层优先 → OCR 引擎链；**质量闸门 min_cjk=20**（防乱码污染底座）；回写主记录/fulltext/clauses(md,json)/index；逐文件落盘可断点续跑。

### 1.3 实测（回填成果）
| 指标 | 前 | 后 |
|---|---|---|
| 零文本制度 | 31 | **1**（Word 临时锁文件 `~$*.docx`，已从底座清理） |
| text_chars 合计 | 9,744,601 | **9,837,050**（+92,449 字） |
| clauses 条文数 | 11,328 | **11,514**（+186） |
| merged with_rfn_refs | 320 | **324**（OCR 新文本识别出 4 份制度的法规引用） |
| 引擎探针 | — | `health = {'paddle': False, 'tesseract': True}`；单件 ~5-25s |

---

## 二、EAST 报送文档排除（`6b68518`）

### 2.1 用户指定不纳入的六目录（源 `D:\0数据治理\00.EAST2.0#M20\`，源目录不动）
| 目录 | 剔除文件 |
|---|---|
| 00.报送文档 | 582 |
| 核查手册 | 25 |
| 立项申请 | 18 |
| 需求文档 | 727 |
| 邮件宣导 | 132 |
| 相关法规 | 193 |
| **合计** | **1677** |

**保留**：`00.中国银保信…培训与答疑`(61) / `00.关于修订…口径的通知`(175) / `保单登记平台…`(9) / `地方局通报`(67) / 一表通 xlsx(1) = **313**。

### 2.2 工具增强（`ingest_corpus.py`）
- **`--exclude-top`**：顶层目录**精确**排除（剪枝）；原 `--exclude` 片段语义**实测误伤 59 个**（如 `00.关于修订…/2.0需求文档/` 被"需求文档"片段命中）——已修正为顶层语义并保留片段参数（标注慎用）；
- **`_update_index()`**：`reports/corpus/_index.json` 同步落工具（原文件**无生成器**，靠手工）。

### 2.3 实测（三向一致）
- corpus `east2_m20`：1990 → **313 文件 / 204.2MB**（manifest 313 = 物理 313 ✓ = `_index` 313 ✓）；
- 全量语料：3009 → **1332 文件**（= 3009 − 1677，精确闭合）；`exclude_tops` 已写入 manifest（可审计）。

---

## 三、Obsidian 知识层 PoC（`reports/Obsidian_PoC_20260912.md`）

- vault 探测：`%APPDATA%\obsidian\obsidian.json` → `D:\DeMon KB`（已打开）；
- **全量同步 36s**：发布件 → **4985 md**（外部 4043 + 内部 957，SHA256 增量）→ `D:\DeMon KB\监管法规库`；
- 每文件 YAML frontmatter（RFN/IPN 溯源 + 文号/日期/时效/来源/url + `body_len_full`/`body_truncated` 截断声明）；
- vault 内 `00_使用说明.md`（命名约定/Dataview 示例/边界声明/增量复跑命令）；
- 后续登记：主题 MOC 页（concepts/）、条款级分发（可选）、llm_wiki 原路径（装后指向同目录即可）。

---

## 终验（全绿）

**ruff 0 / pytest 32 / gates 13 / recall 四门禁 rc=0**

## 遗留登记（下一批）

1. 主题 MOC 生成器（T1-T10 concepts 页）；2. llm_wiki 安装后接入（指向 `监管法规库`）；
3. PaddleOCR python 包（可选增强，当前 tesseract 已覆盖）；4. 归集排除规则随新域沿用 `--exclude-top` 登记纪律。

> 编制：CodeBuddy · 2026-09-12
