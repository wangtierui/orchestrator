# 三项任务完成报告（PaddleOCR 优先 / 制度命名权威化 / 遗留推进，2026-09-12）

> 依据用户指令：①PaddleOCR-3.7.0 存储于 `D:\WorkBuddy\PaddleOCR-3.7.0`，OCR **优先 PaddleOCR**、tesseract 仅降级；
> ②内部制度**文件名称及文号以文档内容为准**；③继续推进未完成事项。
> 提交：`7394893`（P1）/ `90aa98f`（P2）/ `b2edc11`（P3）。

---

## P1 · PaddleOCR 优先接入 + 46 扫描件提质重建 ✅

### 部署接入（R17 布局，零硬编码）
| 项 | 处理 |
|---|---|
| PaddleOCR-3.7.0 源码仓 | `D:\WorkBuddy\PaddleOCR-3.7.0` → 仓内 `external/PaddleOCR-3.7.0` **junction**；`OCRConfig.paddleocr_root`（读自 ocr.yaml）在 `UnifiedOCR` 构造时注入 `sys.path`（绕过 editable 安装失败） |
| 依赖 | `pip install paddlex[ocr]==3.7.2 + paddlepaddle 3.3.1 + aiohttp` 等全依赖（清华源） |
| 模型 | 用户已预置 `~/.paddlex/official_models`（PP-OCRv6 medium det/rec 等），直接命中缓存 |

### 速度与质量实测
- **144s/页 → 23s/页（6 倍）**：关闭 doc_ori/UVDoc 子管线 + `text_det_limit_side_len=1280` + 渲染 DPI 300→200（质量不变）；
- **质量对比**（同一 374 号扫描件）：tesseract"阳光人霸委扯股份有限公司…"（多处错字）→ **PaddleOCR 近乎逐字正确**；
- **`cli internal reocr --force`**：46 个扫描件全量 Paddle 重建（**32 个提质**，含 20+ 页大文档完整识别 cjk 29436）；
- 侧修：`_run_ocr_detail` 透出实际引擎；`ocr_engine/reocr_force_done` 幂等标记（修"边缘件无限重跑"实测缺陷）；
- 成效：clauses 11514 → **11584**（结构更完整）。

## P2 · 内部制度命名内容权威化 ✅（大工程）

### 实现
1. **`scan.parse_content_identity`**（正文提取，覆盖优先级）：
   - 文号：`〔年〕号` 首个命中（（）→〔〕归一；**弃**"文件编号：SLOC-xx"非发文号形态）；
   - 标题：a) 公文式标题（"关于…的通知/函/决定"）→ b) 首部标题段（截"一、/第一条/第N章"前，关键词**结尾**校验 + 页眉词黑名单 + 编号前缀清洗）；
   - **`_overlaps` 交叉校验**：候选与文件名 title 须实质重叠（**防引用他文误提**——如首部引《保险法》被误当标题，实测暴露后修复）；
2. **`clean_title_noise`**：编号/档案号前缀（"23."/"4："/"L042"）、版本盖章噪声（"-清洁版V3/_盖章/（含水印）"）、去外层书名号；
3. **`parse_filename` 增强**：兼容"标题（文号）"尾括号文件名形态；
4. **主链接入**：indexer 提取后即 refine（IPN 随身份重算、写回新路径）；**存量工具** `cli internal refine-identity`（断点幂等；冲突保留 IPN + 更新 title）。

### 实测（1011 份）
- **refine 3 轮收敛**：changed 521 → 44 → **4**（unchanged **1007/1011**）；
- 途中暴露并修复 3 类实现缺陷（撕裂 suffix 缺失/写回旧路径 → v2 自检修复 9 个 → v3 脏判重置 138 个）；
- **state 从 processed 重建**：悬空指针 95 / 影子记录 170 清零、失配 **0**（修复"862 假性收缩"，**index 恢复 957**）；
- 最终：merged 957（with_rfn 322）/ publish **957 policies + 11584 clauses**；vault prune 607 旧名（**4985 个 md 全对齐**）。

## P3 · 遗留推进 ✅

- **主题 MOC 生成器** `tools/gen_theme_moc.py`：发布件 → **每主题一页 MOC**（`[[文件名]]` 双链聚合 Obsidian 图谱）；**双体系归一**（外部全名 ↔ 内部代号按 T 前缀合并）；实测生成 **13 页** → `D:\DeMon KB\监管法规库\主题索引`（T0-T10 + 未分类 + UNALIGNED）；
- 登记未做（下批）：F-R06 剩余 4 群收�敛执行、EAST 报送文档结构化、llm_wiki 安装接入（目录已备）。

## 终验（全绿）

**ruff 0 / pytest 32 / gates 13 / recall 四门禁 rc=0**；vault 监管法规库 **4985 md + 13 主题页** 全量对齐。

> 编制：CodeBuddy · 2026-09-12
