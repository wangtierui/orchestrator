# 重构执行报告 · 第三批（2026-09-26）

> 依据：`reports/全链路重构方案_修订版_v2_20260926.md` §3.1.3（P1-3 I-1~I-7）、§3.6（P1-5 X3/X4）
> 提交：`e8fe732 feat(v2 P1-3/P1-5): interfaces 收口 + 日志与失败语义`
> 范围：**P1-3（7 个子项）、P1-5（2 个子项）全部闭环**；累计路线图进度 **15/22**；工作树残留 **0**。
> 验证：`tools/ci_check.py` **CI: PASS 3/3**（ruff 全绿 ｜ pytest **388 通过** ｜ gates **22/22 PASS**）

---

## 1 任务完成清单

| ID | 任务 | 交付物 | 验收证据 |
|---|---|---|---|
| **P1-3 I-1** | `theme_api` 实装 + `THEME_MAP_P0` 改 re-export | `interfaces/theme_api.py`（重写）、`modules/regulatory_classifier/rfn/registry.py`（新增 `set_theme`） | `interfaces/**` 的 `raise NotImplementedError` **由 3 → 0**；`THEME_MAP_P0 == rfn_api.theme_map()`（11 项）；`by_theme("T1")` 实测 196 行；未知 RFN → `LookupError`、未知主题 → `ValueError` |
| **P1-3 I-2** | `timeliness_review` 三处改经接口 | `verification_state.py`（新增 `classifier_csv()`）、`classifier_pkulaw_verify.py`、`sync_three_modules.py` | 三处归属表路径均经 `interfaces.rfn_api.registry_paths()["attr_csv"]`；实测与接口返回值**逐字符一致** |
| **P1-3 I-3** | `base_publish` 三脚本改经 interfaces 读输入 | `build_external.py`、`build_fts.py`、`build_internal.py` + 接口新增 5 个访问器 | 6 处拼接清零；新增 `clean_index_api.published_dir()/index_path()`、`clause_index_api.clauses_dir()`、`rfn_api.data_dir()`、`internal_policy_api.data_dir()/published_dir()` |
| **P1-3 I-4** | 新增 `interfaces/protocols.py`（4 协议）+ 消除治理层对模块内部的依赖 | `interfaces/protocols.py`（新）、`gates/gate_contract.py` | 协议：`CleanIndexProvider`/`RfnProvider`/`InternalPolicyProvider`/`RelationsProvider` + `assert_provider()`；`gate_contract` 不再自行 `sys.path` 注入与 `from rfn import THEME_MAP`（**gates/ 注入净减 1**） |
| **P1-3 I-5** | 判据 D：字符串路径指向兄弟模块目录 | `gates/gate_no_cross_module_import.py`（+`_scan_sibling_paths`） | 首轮扫描**命中 10 处真实违规**（`base_publish`×6、`clause_index`、`classify`、`recall_audit`、`timeliness_review` 族）→ 全部修复后 **findings=0** |
| **P1-3 I-6** | 结构清单派生一致性 | 既有 `gate_config_integrity` 判据 B2（本次**验证通过**，代码未改） | 5 处派生点（`paths.module_dir` / `gate_no_cross_module_import.MODULES+PKG_OWNER` / `gate_flat_layout` / `tools._manifest` / `ruff per-file-ignores`）与 `MODULE_PKGS` 一致 |
| **P1-3 I-7** | `gate_interface_stubs` 判据 | `gates/gate_runtime_hygiene.py` | `_STRICT_IFACE_STUBS = True` + 白名单机制（`_IFACE_STUB_ALLOW`，须注明期次）；当前空壳 **0** 且**阻断生效** |
| **P1-5 X3** | 日志设施 + 生产脚本日志化 | `std_lib/common_lib/logging.py`（新）、三脚本改造 | re-export `setup_logging/JsonFormatter/LogContext/snapshot_failure` + `get_logger`/`setup_cli_logging`/`json_logs_enabled`/`fatal`；**24 处状态行 print → LOG**（9 处机器可读载荷/表格行保留 print） |
| **P1-5 X4** | `_run()` 失败语义化字段 | `tools/run_production_refresh.py` | 新增 `exit_code`（`ExitCode` 成员名）/`stderr_tail_len`/`stdout_len`；超时→`TIMEOUT`、异常→`EXC:<类名>`；汇总 JSON 与 `run_log.note` 均带语义码（原为裸数字，`2` 的四义无法区分） |
| **P1-5 X3′** | 新增判据④（生产脚本日志化，阻断） | `gates/gate_runtime_hygiene.py` | 三脚本 LOG 下限 8 / print 上限 2·2·8（**只减不增**），纳入门禁阻断 |

**判据 D 命中的 10 处违规（全部修复）**

| # | 文件:行 | 原写法 | 修复 |
|---|---|---|---|
| 1 | `base_publish/build_external.py:39-44` | `os.path.join(_MODULES, "regulatory_scrapers", "published")` 等 3 处 + 3 个 CSV | 经 `clean_index_api/clause_index_api/rfn_api` 访问器；`CLASSIFIER_DATA` 由 `attr_csv` 父目录派生 |
| 2 | `base_publish/build_fts.py:26-27` | scrapers/published、ipb/published | `clean_index_api.published_dir()`、`internal_policy_api.published_dir()` |
| 3 | `base_publish/build_internal.py:28-29` | ipb/data、ipb/published | `internal_policy_api.data_dir()/published_dir()` |
| 4 | `regulatory_scrapers/clause_index/__init__.py:78` | 桥表 `rfn_clean_bridge.csv` | `rfn_api.registry_paths()["bridge_csv"]` |
| 5 | `regulatory_classifier/scripts/classify.py:46` | clean_index/index.json | `clean_index_api.index_path()` |
| 6 | `regulatory_classifier/recall_audit/run_retrieval_after_checks.py:63` | `_SCRAPERS_MOD`（**死常量**，仅注释提及） | 删除 |
| 7 | `timeliness_review/verification_state.py:36` | 归属表路径 | I-2 一并收口（回退分支改用 `paths.module_dir`） |

---

## 2 变更摘要

### 2.1 行为级变更（B1–B8）

| # | 变更 | 影响 |
|---|---|---|
| B1 | `theme_api.set_theme/by_theme/align_record` **实装** | 三个 `NotImplementedError` 消失；`align_record` 只读（不落盘、不登记 RFN） |
| B2 | `THEME_MAP_P0` 改为 re-export（浅拷贝） | 主题清单**不再双份维护**；改 `rfn.THEME_MAP` 即全局生效 |
| B3 | 新增 `rfn.registry.set_theme()` 为**主题归属唯一写接口** | `register_doc`（首次登记，`insert=True`）与 `re_theme`（改判，薄封装）**全部收口**；`_save_theme_rows` 只剩一个调用方 |
| B4 | `bootstrap()` **补注入 `modules/` 父目录** | 「包形态」导入（`import internal_policy_base.*`）自此可用；`interfaces/internal_policy_api.register()` 等从死代码恢复可用 |
| B5 | `gate_import_bootstrap` **只统计代码行** | 文档/注释里的文字提及不再被计为注入；6 层基线按新口径重标（`gates 17→9`、`interfaces 15→11`、`modules 128→127`、`tests 35→33`、`tools 16→14`） |
| B6 | `gate_no_cross_module_import` 增判据 D | `modules/**` 出现「路径构造 + 兄弟模块名字面量」即 FAIL；经 `paths.module_dir()` / `interfaces.*` 的写法豁免 |
| B7 | `gate_runtime_hygiene` 判据③**转阻断** + 新增判据④ | 接口空壳、生产脚本日志化自此可阻断；①退出码 ②静默异常仍只披露 |
| B8 | `_run` 记录语义化退出码 | `run_log.note` 由 `rc=2` → `rc=2(DATA)`；每步的 `exit_code`/`stderr_tail_len` 落汇总 JSON |

### 2.2 新增文件（3）

`interfaces/protocols.py`、`std_lib/common_lib/logging.py`、`tests/test_interfaces_protocols.py`（16 用例）

---

## 3 执行中发现并处置的问题项（6 项）

| # | 问题 | 处置 |
|---|---|---|
| **N-19** | **`bootstrap("all")` 只注入 `modules/<pkg>` 子目录，未注入 `modules/` 父目录** → 「包形态」导入（`import internal_policy_base.align`）**全仓不可用**；`interfaces/internal_policy_api.register()`（`from internal_policy_base.scan import ipn_of`）等属**从未走通的死代码**。症状极隐蔽：目录确实在 `sys.path` 上，缺的是它的**父目录** | `bootstrap()` 补 `_add(ROOT / "modules")` 并写明两类注入的差别；`align_record` 实测从 `ModuleNotFoundError` → 正常返回（`primary=T8, method=body`） |
| **N-20** | `gate_import_bootstrap` 原按**全文正则**计数，把文档/注释里的文字提及（「禁止业务代码 sys.path.insert」「A 越权引导：sys.path.insert/append」）也算成"注入"——实测 gates/ 的 18 里有 **7 处是散文**，且**新增一句说明性注释即顶破基线**（本批亲历） | 改为 `_code_hits()`：跳过注释行与文档字符串，只数代码行；6 层基线按新口径重标 |
| **N-21** | 判据 D 的 `_PATH_CALL_MARKS` 常量内含完整 `sys.path.insert(` 字面量 → 被上一条门禁计成 gates 的注入（**自反假阳性**，17→19） | 拆成 `"sys.path." + "insert("` 并注明原因（语义不变、扫描不命中） |
| **N-22** | `interfaces/protocols` 初版与**实装形状不符**：`CleanIndexAPI`/`InternalPolicyAPI` 类缺协议声明的方法（`source_ids`/`latest`/`published_dir`/`index_path`/`data_dir`），`rfn_api` 的模块级函数名（`attr_rows`）与协议（`load_attr_rows`，仅类上有）不一致 | 统一到「协议描述 **provider 对象**」口径：补齐两个类的方法、测试改为断言 `get_*_api()` 实例（`relations_api` 为模块级函数面，按模块断言） |
| **N-23** | 判据 D 的首版断言过宽：`"NotImplementedError" not in text` 会把**文档里的说明**也判失败（`theme_api` 头部注释含该词） | 收紧为 `raise NotImplementedError`（与 `gate_runtime_hygiene` 判据一致） |
| **N-24** | 承接上批 N-15（写入链路二次转义反斜杠）：本批判据 D 的解析器同样**零正则实现**（`str` 成员判定 + 引号/空白自管），避免同类静默失效 | 已固化；`_scan_sibling_paths` 的豁免规则以 `module_dir(` 字符串判定，不需正则 |

---

## 4 验证结果

| 检查 | 结果 |
|---|---|
| `tools/ci_check.py` | **CI: PASS 3/3** |
| `ruff check .` | **All checks passed** |
| `pytest tests -m "not data"` | **388 通过**（上批 372 + 新增 16） |
| `python cli.py gates` | **22/22 PASS** |
| 判据 D 扫描 | `modules/**` findings **0** |
| 接口空壳 | `interfaces/**` `raise NotImplementedError` = **0**（判据③阻断生效） |
| 生产脚本日志化 | 三脚本 LOG ≥ 8、print ≤ 上限（判据④阻断生效） |
| `BENCHMARK.md` | 已重跑：`gates=22 tests=30 attr=1162 base=1073 final=1073 detail_rows=1162 bridge=943 state=14797 merged=878` |
| `git status --porcelain` | **0 条** |

---

## 5 遗留风险说明

### 5.1 高

| # | 风险 | 说明 | 建议 |
|---|---|---|---|
| **R1** | 路线图 **15/22** | 未做：P2-1（triggers）、P2-2（schedule）、P2-3b（inbox）、P2-4（测试策略）、P2-5（5 个 worklist kind）、P2-6（统一执行入口）、P2-7（退役收尾）、P3-1..P3-4 | 下一批首选 **P2-5 + P2-6**（P1-6 的队列机制与 P1-5 的日志设施刚好是它们的前置，边际成本最低） |
| **R2** | **P3-5 未决**（gates 阶段 6 / `analysis:gen` 阶段 6.8 顺序耦合） | `analysis:manifest` 仍**无法**声明依赖边 → 水位覆盖度真实缺口 | 需先评估 `gate_result` 归档、BENCHMARK 基线、人工处置习惯的连带影响，再二选一 |
| **R3** | `gate_runtime_hygiene` 判据①②仍只披露 | 裸整数退出码与 `except-pass` 存量未收敛（`_STRICT_EXITCODES`/`_STRICT_BARE_PASS = False`） | 需先做存量收敛（`tools/`、`modules/` 的裸整数 `return N`），再置 True |

### 5.2 中

| # | 风险 | 说明 |
|---|---|---|
| **R4** | `interfaces` 仍有 11 处 `sys.path` 引导 | `*_api.py` 为访问具体模块仍需少量注入；I-4 后已可逐步改为「经 `bootstrap()` + 协议」，但需先决定 `*_api` 是否统一要求调用方引导（会改变独立可用性） |
| **R5** | `modules` 仍有 127 处引导 | 依赖 P2-x 逐步收敛；判据 D 已封住"拼路径"这条旁路，`import` 与引导两条仍靠基线冻结 |
| **R6** | 判据④的 print 上限含**人工判断** | 三脚本残余 print 中，`governance_sync` 的双列表头/行为表格输出、`extract_relations` 的 `json.dumps` 汇总为**机器可读载荷**；若后续要把它们也日志化，须先定 stdout 契约（编排器 `_run` 会 tail stdout） |
| **R7** | 交付件数据面仍为 09-22 快照 | `docs/reports/*`（19 份）未重跑；`gate_clean_schema` 的 `LEGACY_WITH_ERRORS={nfra:5, supp:4}` 仍待一次全链 clean 归零 |

### 5.3 低

| # | 风险 |
|---|---|
| **R8** | `paths.py` 仍有 7 个预留守位常量（`INBOX_DIR` 等）；P2-3b/P2-1 接入后回收 |
| **R9** | `_IFACE_STUB_ALLOW` 白名单当前为空——若将来引入抽象基类需登记并注明期次（判据会在报错信息中提示） |
| **R10** | `common_lib/logging.py` 的 `setup_cli_logging()` 已提供但**主链尚未调用**（X3 的"主链 `--json-logs`"仅具备能力，未接到 `run_production_refresh` 的 CLI 参数）——留在 P2-6（统一执行入口）一并接 |

---

## 6 下一批建议

1. **P2-6 统一执行入口**（`cli.py run/doctor/status/schedule` + `notify`）：把 `setup_cli_logging(json_logs=...)` 接到 CLI（`--json-logs`），并把 `worklist_stats`/水位状态汇入 `status`——P1-2/P1-5/P1-6 的成果在此汇合。
2. **P2-5** 接入 D1/D2/D6 + `corpus_needs_review` + `ingest_quota_blocked`（worklist 机制已就绪；J7 会要求同步移除 `_PENDING_PRODUCERS`）。
3. **P2-3b** `data/inbox/` 投放区 + `inbox_scan.py`（`paths.INBOX_DIR` 已预留；判据 D/J8 已就位）。
4. **R3** 存量收敛（裸整数退出码 → `ExitCode`；`except-pass` 补原因注释或 LOG），为判据①②转严铺路。
5. **P3-5** 顺序解耦（解 R2），使 `analysis:manifest` 可声明依赖边。

**文档结束。**
