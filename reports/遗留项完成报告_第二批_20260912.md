# 遗留项完成报告（第二批，2026-09-12）

> 承接《遗留项完成报告_20260912》登记的 8 项遗留；本批次执行 U1–U7（提交 `37485bf` / `0ec3bf6` / `47321aa` + 本报告）。

---

## U1 · F-D14 状态文件 schema_version ✅（`37485bf`）

四类状态文件版本锚点落地（写侧注入 `_meta{schema_version,written_by,written_at}` + 读侧 pop 剥离，**消费方零改动**）：

| 状态文件 | 写入方 | 实测 |
|---|---|---|
| `classify_state.json` | classify | sv=1.0 by=classify |
| `clause_index_state.json` | clause_index | sv=1.0 by=clause_index |
| `rfn_drift_state.json` | reconcile_clean_drift | sv=1.0 by=reconcile_clean_drift |
| `verification_state.json`（+镜像） | verification_state | sv=1.0 by=verification_state；新增 `read_meta()` 辅助；覆盖率仍 2975/4043 无感 |

（`_ingest_state.json` sha256 键空间：`_meta` 独立键位 + 读剥离。）

## U2 · F-O08 match_theme_docs 内存优化 ✅（`0ec3bf6`）

- `build_lib_index` 改**轻量偏移索引**：条目只存 title/docno/body_len/**行偏移**（不再驻留 body_text 全文数百 MB）；
- 命中后经 `_read_at` 定点读取（IO 仅命中行）；
- **回归**：T1 实测 193/193 键集一致、body_len 差异 **0**、总字节 592785 完全一致、耗时 4.1s。

## U3 · F-O02/O03/O04 编排补件 ✅（`0ec3bf6`）

| 项 | 实现 |
|---|---|
| **F-O04** | `nfra_weekly` 周度增量链接入编排（`refresh --collect nfra-weekly`；等价周二 01:00 调度链，替代全量） |
| **F-O02** | `cli.py source diff` 变更监听（`data/watch_baseline.jsonl` 基线追加 + 对比日期/记录数/内容 sha；同日改写可见）；refresh 阶段 6.5 自动 `--record` |
| **F-O03** | `reports/运行手册_编排与定时_20260912.md`（调度清单/命令基准/rc 告警语义表/新增任务须登记） |

## U4 · F-D07 附件契约 + F-D09 双轨声明 ✅（`47321aa`）

- `contract.ATTACHMENT_FIELDS`（7 规范性字段）+ `ATTACHMENT_ALIASES`（五源 5 套字段归一）+ `attachment_view()`；发布件 attachments **扩至 7 字段**（实测 bytes 1953 / url 1575 / text_len 360）；
- F-D09 双轨权威声明：cleaned **JSONL=权威轨** / CSV=检索投影 / 不得反向覆盖（契约内成文）。

## U5 · 归集后续：部门制度入 IPB 摄取 ✅（本报告主体）

- 范围：`data/corpus/dept_policies`（1019 文件）中受支持制度文档 **937 份**（pdf 642/docx 124/doc 64/xlsx 66/xls 41 等经 scan 过滤）；
- 摄取（`cli.py internal index --source-dir ...`）：**新增 936 制度**（changed 0 / 与现有 107 制度零 IPN 碰撞；skipped 1 为完全重复）；
- 提取降级：扫描件 PDF 无文本层 → `needs_ocr` 标记（Tesseract 未部署，按引擎探测契约快速降级，未阻断）；
- 完成后续链：`internal align`（主题对齐）→ `internal merged` → `base publish --base internal`（发布件刷新）。

> 摄取结果数据：见下"实测摘要"（摄取完成后补录）。

## U6 · 阶段 3 轻量交付 ✅（`47321aa`）

| 项 | 实现 |
|---|---|
| **F-L04** | `external_relations.jsonl` 发布件（clause_graph 10 主题 **1508 条边**汇聚：src_rfn→dst_rfn/kind/count）——关系图数据面 |
| **F-L06** | `cli base query --order date`（时间线视图） |
| **F-L08** | wiki 导出 frontmatter 截断声明（`body_len_full` / `body_truncated`） |

## U7 · F-D06 契约化 + F-R06 台账 ✅（`47321aa`）

- **F-D06**：`FIELD_SEMANTIC_EQUIV` 补 raw 五源历史名（body_text 5 名 / publish_date(+original) / effective_date / doc_title / source_url）——读侧一律 `read_field` 归口（写侧不重写历史）；
- **F-R06**：`reports/重复实现台账_20260912.md`——已收敛 6 群（归一19/标题11/回写3/核验4/契约4/查表1）；剩余 4 群（HTTP4/附件5/保存4/枚举5）收敛路径与风险登记；防增量机制（gate_no_duplicate + 台账检查点）。

---

## 实测摘要

- U1：四文件 `_meta` 落地；覆盖率 2975/4043 不变；recall rc=0 / pytest 32 / gates 13 PASS
- U2：T1 回归 193/193、字节全同
- U3：`source diff` 两次实测（无基线→记录；有基线→"无变化 0 源"）
- U4：relations **1508**；attachments 2329（7 字段）；manifest counts 更新
- U5：部门制度 **937 摄取**（新增 936）→ IPB 制度 **107 → <待补>**
- U7：别名自检通过；`read_field` 冒烟（full_text→body_text 命中）

## 剩余登记（下一批）

| # | 项 | 说明 |
|---|---|---|
| 1 | llm_wiki PoC | 需安装桌面应用（GPL v3）；`tools/sync_wiki_sources.py` 已就绪 |
| 2 | F-L01/L02/L03 交付补齐 | 规划 2.1 十四项分析交付生成器、3.2 六交付——依赖规划章节设计（大项）；L03 法宝核验结论已入发布件（timeliness_status/verification_source 可读） |
| 3 | EAST 报送文档结构化（监管统计域） | corpus east2_m20 解析器（报送口径/数据结构文档族）——独立批次 |
| 4 | F-R06 剩余 4 群收敛 | 按台账顺序：枚举→原子写→附件→HTTP |
| 5 | OCR 引擎部署 | Tesseract/PaddleOCR 安装后重跑 IPB 摄取可补扫描件正文（needs_ocr 清单已在索引） |

> 编制：CodeBuddy · 2026-09-12 · 提交：`37485bf` / `0ec3bf6` / `47321aa`
