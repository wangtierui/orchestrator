# 数据结构枚举缺口审查报告（遗留问题 10）

> 日期：2026-09-08。审查范围：全项目数据结构定义（config/enums、std_lib/scraper_std 受控表、
> classifier data 契约、internal_policy_base 字段）。原则：**取值有限且稳定 → 应受控枚举；
> 自由文本/开放标注 → 合理自由**。

## 一、已受控（合规）字段对照

| 字段 | 所在表 | 受控源 | 值域 | 状态 |
|---|---|---|---|---|
| timeliness_status | cleaned 39列 / 归属表「时效状态」/ 底座 eff_status / 明细 | config.enums.TIMELINESS_STATUS | 7 值 | ✅ R5 上收 + gate_enum_values 断言 |
| source / 文件来源 | cleaned / 归属表 / 桥表 | config.enums.SOURCE_SET | 5 值 | ✅ registry LEGAL_SOURCES 校验 |
| body_source | cleaned | config.enums.BODY_SOURCE | 3 值 | ✅ |
| 主题（主题归属表） | 主题归属表 3列 | rfn.THEME_MAP | T0 + T1–T10 | ✅ 全库唯一定义 |
| internal_status | internal index | config.enums.INTERNAL_STATUS | 5 值 | ✅ |
| file_type | internal index | config.enums.INTERNAL_FILE_TYPE | 5 值 | ✅ classify_file_type 受控输出 |
| category（效力位阶） | cleaned | category_classifier.CATEGORY_SET | 13 级（AUTHORITY_RANK） | ⚠️ 受控但定义于 std_lib 分类器，未上收 config.enums（R5 覆盖不完整） |
| doc_type（文种） | cleaned | doc_type_cleaner.FILE_TYPES | 52 文种 + 别名归一 | ⚠️ 同上（受控但源在 std_lib，未在 config.enums 登记） |
| RFN / IPN 编号 | 各表 | rfn.RFN_PAT / scan.ipn_of | RFN-/IPN-+16hex | ✅ |

## 二、2026-09-08 新收口（本报告新增 config.enums 4 组）

| 新枚举 | 值域 | 用途/消费方 |
|---|---|---|
| `INTERNAL_EXT` | pdf/doc/docx/xlsx/xls/xlsm | internal 源文件扩展名（scan 支持集） |
| `ALIGN_METHOD` | title/body/unaligned | align_result.align_method |
| `REF_MATCH_METHOD` | docno_sig/title | merged_view.associated_rfns[].matched_by |
| `BRIDGE_RELATION` | self/refresh/supersede | rfn_clean_bridge.relation |

（已同步 assert_enum_bindings 自检；内部实现已按受控值写入。）

## 三、缺口清单（本应枚举但当前自由写入 / 源分散）

| # | 表/字段 | 现状 | 建议处置 | 优先级 |
|---|---|---|---|---|
| G1 | cleaned `doc_type` | 受控于 doc_type_cleaner.FILE_TYPES，但 **config.enums 无登记**、无跨模块校验 | 上收 config.enums.DOC_TYPE（FILE_TYPES+别名），doc_type_cleaner re-export（R5 同款）；gate_enum_values 扩展 | P1 |
| G2 | cleaned `category` | 受控于 category_classifier.CATEGORY_SET（13 级），源未上收 | 上收 config.enums.CATEGORY_RANK/SET；分类器 re-export | P1 |
| G3 | cleaned `verification_source` | 半自由（北大法宝/规则判断/数据源标注/总局清理通知（金规[…]号）——机构清理文号附注破坏纯枚举） | 归一为主枚举 + 附注迁 `_raw_fields`（N 系已有方向）；当前以 `startswith("北大法宝")` 判定为已知例外 | P2 |
| G4 | cleaned `category` 中文存量（法律法规/政策规章规范性文件等） | 出现在 cleaned 的 category 列原始值（非英文 13 级） | 确认五源清洗后 category 是否统一为英文位阶码（历史中文应已映射）；未映射属遗留 | P2 |
| G5 | internal `status` | 写死 "active"（draft/active/… 未按文件来源判定） | 摄取时按 源目录上下文 或 保留默认 active + INTERNAL_STATUS 校验 | P3 |
| G6 | `file_type` 粗分类（indexer.classify_file_type） | 关键词启发（policy/process/…）可能误判 | 人工复核接口预留；枚举输出本身已受控 | P3 |
| G7 | 桥/明细 中 `relation`/`matched_by` 等新字段 | 本轮已上收 BRIDGE_RELATION/REF_MATCH_METHOD | ✅ 已办 | — |

## 四、合理自由（不建议枚举）

- title/文件名称/body_text/summary/正文：自由文本 ✅
- 发文字号/document_number：半结构化（机关代字+年+序号）——保持格式校验（DOC_RE/长度），不做值枚举 ✅
- index_no/dedup_key/sha256/日期：标识/时间戳 ✅

## 五、落实建议

1. G1/G2 收口 = 复制两分类器常量表至 config/enums + re-export + doc_type_cleaner/category_classifier 改 `from config.enums import ...`（镜像 unified_schema R5 改法）。因涉及 scraper_std 两模块 200+ 行常量迁移，建议单列一期执行并重跑五源清洗回归。
2. G4 先抽样 cleaned 实际 category 取值分布再定。
3. G5/G6 为运行期增强，非结构性缺口。

> 本报告为审查结论与处置建议；P1 项（G1/G2 上收）是否本期执行请确认。
