# 主题分类脚本重构核查报告（遗留问题 6/9）

> 日期：2026-09-08。核查对象：regulatory_classifier 主题分类/归属脚本是否完成重构（原 P4 迁移时标 theme_analysis 多数脚本为"死路径/历史协议版"）。

## 核查结论：✅ 已完成收敛

### 1. 主题枚举唯一事实源
- 全仓唯一 THEME_MAP 定义：`modules/regulatory_classifier/rfn/__init__.py:34`（T0 上位法锚点 + T1–T10 全称）。
- 新仓 classifier 范围内无任何第二处 `THEME_MAP=` / T1-T10 全名重复定义（grep 实证）。
- internal 对齐（`internal_policy_base/align.py`）经 `from rfn import THEME_MAP` 引用同一来源；本地 THEME_TITLE_KW 仅关键词启发表，非枚举定义。

### 2. 主题归属表写者收敛（R2）
| 写者 | 状态 |
|---|---|
| `rfn/registry.py register_doc`（239 行 主题归属表同步写入） | ✅ 新仓唯一代码写者（查重复用/新建均经此） |
| theme_analysis/node2_rebuild `merge_final_theme.py` 等（旧 3 写者之一） | 未迁入新仓（P4 按 pyproject 死路径纪律排除） |
| theme_analysis 根目录 classify/adjudicate/split/blind_rejudge/perspective 等旧引擎 | 未迁入新仓（历史一次性作业） |
| `interfaces/theme_api.set_theme`（R2 建议单写接口壳） | 保留为 P0 占位；实际写由 registry 承担（registry 即代码层唯一事实写者） |

### 3. 调用链（现行）
```
register_doc(theme, ...) → 校验 theme ∈ THEME_MAP → 追加 文件归属表 + 主题归属表(3列) + 指纹 → rebuild_index
```
- 归属表 8 列不含主题列（拆表已落地）；主题唯一承载于主题归属表 3 列。
- RFN 索引 6 列由 rebuild_index 合并主题列派生（只读视图）。

### 4. 遗留待办（非阻断）
- `interfaces/theme_api.py` 若未来要求"外部仅经 theme_api 写主题"（R2 理想态），可将 register_doc 的主题写收口到 theme_api.set_theme 委托——当前无该需求（internal base 不写 classifier 主题，仅读对齐），暂缓。

### 5. 实证依据
- 关键词"T1销售行为与消费者保护 / T2产品与精算制度 / THEME_MAP="全仓 grep 仅命 rfn/__init__.py（定义）与 registry（写入）。
- data/主题归属表 3 列 1059 行与归属表 RFN 集一致（gate_rfn_sync 全量核验 0 问题）。
