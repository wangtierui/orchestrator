# 重构执行报告（2026-09-26）

> 依据：`reports/全链路重构方案_修订版_v2_20260926.md`
> 提交：`16e3c4f refactor(v2 P0/P1-1): 清单与引导 SSOT 收敛 + 校验假成功修复 + 门禁 18→22`（post-commit 已自动 push）
> 范围：本轮完成 **P0-1~P0-6 + P1-1 共 7 项**（路线图 22 行）；其余期次未执行，见 §5。

---

## 1 任务完成清单

| ID | 任务 | 状态 | 交付物 | 验收证据 |
|---|---|---|---|---|
| **P0-1** | 清单与退出码 SSOT | ✅ 完成 | `config/constants.py`（111 行：`MODULE_SPECS` 5 模块 / `PIPELINE_STAGES` 15 阶段 / `assert_registry_consistent()`）、`config/exitcodes.py`（59 行） | `gate_config_integrity` B1/C1 通过 |
| **P0-2** | 唯一 sys.path 引导点 | ✅ 完成 | `bootstrap.py`（支持按需模块 / `"all"` / `extra` 子目录 / `include_tools`）、`tests/conftest.py`；`commands/` **9 个文件注入清零** | `gate_import_bootstrap`：`commands` = 0（硬断言） |
| **P0-3** | paths 接入 constants | ✅ 完成 | `paths.py`：`module_dir()` 覆盖 **5 模块**（含 `base_publish`）并接受包名/短键；标注消费状况（9 已消费 / 6 预留守位） | `gate_config_integrity` B2 通过 |
| **P0-4** | 依赖声明完整性 | ✅ 完成 | `pyproject.toml`：补 `Pillow>=10.0`、`urllib3>=2.0`，`PyMuPDF` 从 `[ocr]` 上移 base；`supp_fix_raw_factual.py` 顶层导入加保护 + 调用期显式报错；`supp_ocr_pdf.py` 回退链降级返回空文本 | 全仓 `ruff check` 本次文件 0 报错；`pytest` 340 通过 |
| **P0-5** | tools 清单 + 隔离层 | ✅ 完成 | `tools/_manifest.json`（24 条：22 active + 2 retired；`root_py_allowlist`）、`tools/retired/`（迁入 2 个一次性脚本）、`tools/retired/README.md`（含退役原因/日期/替代物）；**删除仓根 `file_list_watcher_bak.py`** | `gate_config_integrity` J1–J5 通过 |
| **P0-6** | 新增 4 道门禁 | ✅ 完成 | `gate_config_integrity`（J1–J6 + 结构清单 + 退出码）、`gate_import_bootstrap`（分层基线冻结）、`gate_runtime_hygiene`（三项只披露）、`gate_clean_schema`；`ALL_GATES` **18 → 22** | `python cli.py gates` → **22/22 PASS** |
| **P1-1** | **校验假成功修复** | ✅ 完成 | `std_lib/scraper_std/pipeline.py`：失败记录转 `quarantined` 且**不进交付**，落 `{src}_cleaned_{date}.quarantine.jsonl`；`summary` 增 `quarantined_total`/`quarantine_path`/`schema_failed_rate`；`run_clean_pipeline.py` 增 `--allow-schema-errors`（逃生阀）与 `--schema-fail-rate-max`（默认 2%）+ 超阈值 `rc=2` | 门禁 `gate_clean_schema` 已能检出历史脏记录（见 §3 N-6） |

**未完成（路线图其余 15 行）**：P1-2（水位 round/stage）、P1-3（interfaces 收口）、P1-4（契约清单消费）、P1-5（日志/失败语义）、P1-6（worklist）、P2-1..P2-7、P3-1..P3-4。原因：本轮按 v2「P0 → P1-1」分批推进，避免一次改动面过大破坏生产链（v2 §4 顺序约束）。

---

## 2 变更摘要

**提交规模**：36 个文件，`+4340 / -64`。

### 2.1 新增（12）

| 文件 | 作用 |
|---|---|
| `config/constants.py` | 模块/阶段清单唯一事实源（替代原 4 份互不一致的硬编码副本） |
| `config/exitcodes.py` | `ExitCode` 枚举（**描述性建模 + 不重编号**，见 §3 N-3） |
| `bootstrap.py` | 唯一 `sys.path` 引导点 |
| `tests/conftest.py` | 测试侧唯一引导点（替代 26 个测试文件各自注入的历史模式） |
| `tools/_manifest.json` | tools 脚本语义分区清单 + 仓根 `.py` 白名单 |
| `tools/retired/README.md` | 退役登记（原因/日期/替代物） |
| `gates/gate_config_integrity.py` | J1–J6（tools 清单）+ B（结构清单）+ C（退出码）+ D（待落地披露） |
| `gates/gate_import_bootstrap.py` | 分层注入基线（`commands` 硬零） |
| `gates/gate_runtime_hygiene.py` | 退出码/静默异常/接口空壳三项指标披露 |
| `gates/gate_clean_schema.py` | 失败记录不得进入交付（含历史基线过渡） |
| `reports/全链路重构方案_修订版_v2_20260926.md` | 方案入库（P0-1 的一部分） |
| `reports/全链路数据流链路图_v2_20260926.md` | 5 张 Mermaid 图 + 4 张清单表 |

### 2.2 修改（关键行为变更）

| 文件 | 行为变更 |
|---|---|
| `std_lib/scraper_std/pipeline.py` | **校验失败记录不再进入交付**（原为照常交付 = 假成功）；新增隔离落盘与 3 个统计字段 |
| `modules/.../clean/run_clean_pipeline.py` | 新增 2 个 CLI 参数（`--allow-schema-errors` / `--schema-fail-rate-max`）+ 常量 `SCHEMA_FAIL_RATE_MAX = 0.02` + 阈值 `rc=2` |
| `paths.py` | `module_dir()` 改为派生（覆盖 5 模块）；docstring 标注常量消费状况 |
| `commands/{base,analysis,governance,timeliness,classify,source,rfn,internal,relations}.py` | 各自 `sys.path.insert` → `bootstrap(...)`；顺手清理 18 处因改造失效的未使用导入（ruff `--fix`） |
| `gates/__init__.py` | `ALL_GATES` 18 → 22，并写明门禁计数口径修正 |
| `gates/{gate_hardcoded_paths,gate_hardcoded_snapshots,gate_no_duplicate_libs}.py` | `EXCLUDE_DIRS` 增 `retired`；`gate_hardcoded_paths.EXCLUDE_RELPATHS` 增 `FILE_LIST.md`；移除已冗余的 `migrate_collectors_p3b.py` basename 豁免 |
| `file_list_watcher.py` | **生成物改为仓库相对路径**（原写绝对路径 → 生成物必触 R4）；顺带修 3 处风格问题 |
| `.gitignore` | 增 `FILE_LIST.md`（派生产物）与 `_p.log` |
| `pyproject.toml` | 依赖声明修正 + ruff `extend-exclude` 增 `tools/retired` |

### 2.3 移动 / 删除

| 对象 | 动作 |
|---|---|
| `tools/{flatten_collectors,migrate_collectors_p3b}.py` → `tools/retired/` | **同卷 rename**（git 识别为 100% rename，内容零变化） |
| `file_list_watcher_bak.py`（仓根，UNTRACKED、零引用） | 删除 |

---

## 3 执行中发现并处置的方案前期未识别问题（9 项）

| # | 问题 | 处置 |
|---|---|---|
| **N-1** | 方案门禁计数口径不一致：§3.8 称"18 → 24"，但表 A 的 `gate_cross_module_data` 又被表 B 声明为"并入既有门禁的判据"（判据扩展不增门禁数） | 定为 **18 → 22**（新增 4 道）；已在 `gates/__init__.py` 注释与方案中记录修正 |
| **N-2** | `gate_exit_codes` 既被列为独立门禁、又被 `gate_runtime_hygiene` 判据 ① 覆盖（重复计数） | 并入 `gate_runtime_hygiene`，不单独计数（同 N-1） |
| **N-3** | 方案 §3.6 规定 `ENV=3 / GATE=4`，与既存契约冲突：`cli.py` 用 **rc=4 = 非源码树**（对外文档/记忆已引用）；`2`/`3` 在多处含义不同 | 改为**描述性枚举 + 别名 + 不重编号**（`FAIL=USAGE=GATE=1`、`DATA=CONFIG=DEPENDENCY=2`、`ENV=LOCKED=PRECONDITION=3`、`NOT_SOURCE_TREE=4`），既统一语义又不破坏对外契约 |
| **N-4** | **`FILE_LIST.md:4` 的 R4 FAIL 自 2026-09-24 起长期存在**（`gate_hardcoded_paths` 命中盘符字面量），且根因是 `file_list_watcher.py` 主动写绝对路径 | 双重修复：① 生成物按派生产物加 `EXCLUDE_RELPATHS`（该 FAIL 消失）；② **改根因**（生成器改写仓库相对路径），使"将来入库"也不再违规 |
| **N-5** | J3"退役脚本零引用"被**历史注释标记**污染：`migrate_collectors_p3b.py` 当年把引导注释写入 24 个 `collectors/*.py` | 判据收紧为「**只计可执行引用**」；注释提及记入 `detail.retired_comment_mentions` 披露（总数需在后续批次顺手清理） |
| **N-6** | `gate_clean_schema` 首跑即检出**真实数据问题**：`nfra_cleaned_20260922.jsonl` 有 **5 条**、`supp_cleaned_20260922.jsonl` 有 **4 条** 记录带 `validation_errors` 却已进入交付 —— 正是"假成功"的实证 | 以 `LEGACY_WITH_ERRORS = {"nfra": 5, "supp": 4}` 冻结为历史基线（只减不增）；**下次 clean 重跑后应归零，届时删除该表并转严格** |
| **N-7** | `sys.path` 注入基线需按层冻结（方案只给了"17 → 2"的总数口径） | 实测并冻结：`gates 17 / interfaces 15 / modules 127 / std_lib 9 / tests 35 / tools 16`，`commands = 0`（硬断言） |
| **N-8** | 仓根 watcher 三件套（`file_list_watcher.py` / `update_list.bat` / `FILE_LIST.md`）**全部 UNTRACKED**，而 `update_list.bat` 调用其中脚本 → 新克隆无法运行 | 生成物 gitignore + 生成器改相对路径 + watcher/bat 纳入库（本提交已收录 `file_list_watcher.py`） |
| **N-9** | `_p.log`（临时调试日志）**已入库** | `.gitignore` 已加规则；**`git rm --cached _p.log` 未执行**（需一次显式索引操作，见 §5 R2） |

---

## 4 验证结果

| 检查 | 结果 |
|---|---|
| `python cli.py gates` | **22/22 PASS**（含修复长期 FAIL 的 `gate_hardcoded_paths`） |
| `pytest tests -m "not data"` | **340 通过**（与基线一致，无回归） |
| 全仓 `ruff check .` | 25 处**历史**报错（均在本次未触碰的文件，如 `gate_citations.py:62`、`graphify_sync.py` 等）；本次改动文件 **0 报错** |
| CLI 冒烟 | `ping` / `source list` / `relations status` / `analysis status` / `governance status` / `classify --dry-run` / `internal` / `timeliness` / `rfn` / `base` 均正常（改造后引导链路可用） |
| git | 提交 `16e3c4f` 已生成，`main...origin/main` 无 ahead/behind → **post-commit push 成功** |

---

## 5 遗留风险说明

### 5.1 高

| # | 风险 | 说明 | 建议 |
|---|---|---|---|
| **R1** | **路线图仅完成 7/22** | P1-2..P1-6、P2-1..P2-7、P3-1..P3-4 未执行；其中 **P1-2（水位 round/stage）** 与 **P1-6（worklist）** 是后续多项的前置 | 下一批做 P1-2 + P1-6（问题最痛、依赖最少） |
| **R2** | **`_p.log` 仍在版本库** | `.gitignore` 只影响未跟踪文件；已跟踪文件需 `git rm --cached` | 一次显式操作即可（本次未执行，避免未经批准改索引） |
| **R3** | **P1-1 的隔离机制尚未经真实全链验证** | `.quarantine.jsonl` 只在下次 `clean` 重跑时产生；本轮只验证了代码路径与门禁 | 下一批跑一次全链（或单源 `clean --project nfra`）确认隔离落盘与 `rc` 语义 |
| **R4** | **9 条历史脏记录仍在交付文件中** | `nfra` 5 + `supp` 4 条带 `validation_errors` 的记录已交付；`gate_clean_schema` 以基线冻结放行 | 全链重跑后归零；届时删除 `LEGACY_WITH_ERRORS` 并转严格 |

### 5.2 中

| # | 风险 | 说明 |
|---|---|---|
| **R5** | `gate_runtime_hygiene` **恒 PASS** | 三项 `_STRICT_* = False`，只披露：裸整数退出码 **79** 处、`except-pass` **138** 处、接口空壳 **3** 处（`theme_api`）。需按期转严格，否则该门禁无阻断力 |
| **R6** | `gate_import_bootstrap` 基线偏大 | `modules 127 / tests 35 / interfaces 15 / gates 17` 仅冻结未收敛；新增代码若再自注入会被拦（这是主要价值），但存量收敛仍需 P1-3 与后续批次 |
| **R7** | 25 处 ruff 历史报错 | 与本次改动无关，但意味着 `tools/ci_check.py` 的 ruff 闸当前为**红**——即本地 CI 等价物并不绿（该闸未被自动执行，长期无人发现） |
| **R8** | 22 项工作树未提交改动 | `BENCHMARK.md` + `docs/reports/` 17 份 + 其他，均为历史刷新产物（非本次改动）。既不提交也不清理，会让"commit ≈ 发布"的边界模糊 |
| **R9** | `gate_config_integrity` 的 D 组"待落地" | `config/triggers.yaml`、`config/schedule.yaml`、`worklist` 表均未落地，仅披露；方案 §3.5/§3.13 的"不依赖记忆"目标尚未具备实体 |

### 5.3 低

| # | 风险 |
|---|---|
| **R10** | J6（仓根未跟踪文件）为报告项：仓根仍可能有历史 untracked 残留（本次已处置 `file_list_watcher_bak.py`），`gate_repo_hygiene` 的严格化在 P2-7 |
| **R11** | N-5 的历史注释标记（24 个 `collectors/*.py` 中的 `migrate_collectors_p3b.py 注入` 注释）未清理，仅披露计数 |
| **R12** | `paths.py` 仍有 6 个"预留守位"常量（`CONFIG_DIR`/`INTERFACES_DIR`/`GATES_DIR`/`STD_LIB_DIR`/`ENUMS_FILE`/`SCHEMA_DIR`）零消费，已在 docstring 标注但未接入或删除 |

---

## 6 下一批建议（按依赖与风险排序）

1. **P1-2** 水位 `round/stage`（`_SCHEMA_INT` 2→3）+ `dependency_edges` 四态 —— 解除 `analysis:manifest`/`clauses:{src}` 的"只观察"豁免，是 P1-6/P2-1 的前置。
2. **P1-6** `worklist` 表（`_SCHEMA_INT` 3→4）+ `WORKLIST_KIND` + `cli.py worklist`，先接 D3/D4/D5（时效冲突 / 漂移 C2 / IPN 身份冲突——这三处**当前连处置入口都没有**）。
3. **P1-4** `gate_contract` 接入 `contract_manifest.json`（清单已漂移：`detail_table_fields` 10 vs 14）。
4. **P2-3a** 语料分层前置（`ingest_corpus` sha256 强制 + 历史清单回填 + 同卷 rename）—— 与 R4 的历史脏记录清理可一并做。
5. **R2/R7/R8 清理项**（`_p.log` 出库、ruff 25 处修复、工作树 22 项归类）—— 成本低、消除噪声。

**文档结束。**
