# 六项任务交付报告（2026-09-08）

> 任务域：regulatory_compliance_orchestrator（监管合规治理编排仓）
> 范围：①数据流转审计 ②EAST 表格类 supp 收录实战 ③统一基准 ④字段命名 ⑤Prompt 编排排查 ⑥五源生产全流程
> 审计报告：`reports/全链数据审计与基准字段排查_2026-09-08.md`（只读证据 + R1–R12/B1–B6）

---

## ① 数据流转流程 / 产物冗余 / 代码固化 / 断点 / 单源影响 —— 已排查并修复主要项

- **排查结论**：全链 18 跳均已有确定性脚本入口；raw 主库不在仓、apply 哑写、sync 哑引用（sync_all_layers/run_gates 不存在）、backfill 无 CLI、classify_state 断点未灌满、一次性历史脚本留主干为**六大断点**；镜像/attr.bak/明细多期等为**产物冗余**；时效/RFN/主题/internal/cleaned 变更对下游存在静默过时（见审计 R 表）。
- **已修复**：
  - B1 raw 落仓：supp raw 活跃复制至仓内 `data/raw/`（flat 豁免 scrapers data/raw）。
  - B2 apply_timeliness 缺失清单给 consolidate 修复指引（替代裸 FileNotFound）。
  - B3 sync_three_modules 哑引用 → rfn `rebuild_index` + `cli.py gates`（R1）。
  - B4 backfill_clauses → `cli.py internal backfill`（R10，验证 107 幂等）。
  - B5 classify_state → `classify --all` 灌满 11 主题×5 子步断点（R2）。
  - B6 `_rebuild_json_pipeline` 加 `--confirm` 防误触 guard（R11）。
  - R9 matched/citerefs 顶层键统一 RFN（重跑后 clause_graph 回填 docno/eff/file_src 消除退化）。
  - R7 `registry.re_theme`（既有 RFN 主题改判入口 + 联动 pending）。
  - R5 merged_view 输入指纹（归属/主题 sha + 内部索引 sha + processed 签名）写入 `inputs`。

## ② EAST 表格类监管文件 supp 收录实战 —— 完成

- 工具 `tools/build_east_backlog.py`：金非银检函〔2025〕82号 20250410 **全 13 文件**收录；
  - 6 个 xlsx（数据结构一览表/业务代码表/数据检核规则/报送清单/检核结果/特殊场景备案）→ **78 sheet 全表格结构化**（`table_structured` 结构轨 + `attachment_content` 文本轨）
  - doc/docx 正文抽取；E-R(jpg)/联系人(.et) 登记；扫描版主通知 pdf（无文本层、本机无 OCR）落**可核验元数据声明**正文（不臆造原文）
- 结果：supp raw 59→**60 条**（含 EAST），清洗 cleaned 58 行；EAST 行 39 列**原生持久化表格结构**（`attachment_count/attachment_content(+md5)/table_structured/table_recovery_method`）→ 验证**当前存储原生支持表格类结构化监管文件**。
- 全量真实抓取五源官网非本任务窗口（周二 automation 已编排）；已探活 gov 真实采集成功（3 条列表+1 详情）。

## ③ 统一基准审计 —— 完成
- 结论：raw ❌（旧仓，本任务起 supp 已落仓）、timeliness ❌（consolidate 基线在旧仓，需复制后现仓跑）、clause 固定节点曾落后 supp 一期、classify_state 仅 1 条、T1 matched 键期残留 → 均已有解或已修（supp raw 落仓/consolidate 指引/clause 随快照自动/clas sify_state 灌满/全主题键统一）。
- 其余阶段（cleaned/归属/主题/底座 40/明细 11/桥/镜像/recall output/internal/merged/draft/主题报告）均具"单一命令可全量重建"的统一基准。

## ④ 字段命名审计 —— 排查完成（中文列名/同义异名）
- 中文作字段编码清单 + 同义称谓表（源标识 6 称谓、时效/主题三形态、去重键三兄弟、时间戳 5+、匹配方式 docno_sig vs docno 等）已写入审计报告；既有契约 `FIELD_SEMANTIC_EQUIV`/`SOURCE_ALIAS_EQUIV` 覆盖不足部分列出。
- 高风险项：matched 键 schema 三处口径 → 本次以"顶层 RFN + rec 内嵌 监管文件编号/seq"统一（R9）；bridge「最近状态」与时效"状态"同名异义 → 契约注释标注。
- 数据层字段统一为逐层收敛的渐进治理（CSV 系中文列为正式编码，JSON 系英文键 + RFN 键为桥），已在报告中给出权威锚点供后续轮治理。

## ⑤ Prompt 编排排查 —— 结论：无待改造
- 全仓 *.py 扫描：prompt/LLM/大模型命中 8 文件全为**注释性字样**（"零 LLM 消耗"设计说明、"供 LLM 检视/完善"用途注释）；无真实 LLM API 调用。
- automation（周/日）词为调度描述，每步均指向具体脚本（代码编排）。确定性流程已代码化，**无"用 Prompt 做确定性转换"环节**。

## ⑥ 五源生产全流程跑通（2026-09-08 快照驱动，采集探活通过）
1. 采集探活：gov_collector 真实抓取 3 条 + 详情 1 条成功（网络/反爬正常）；全量采集交付周二 automation。
2. clean：supp 重清洗（含 EAST，58 行）；index rebuild 五源快照（gov/mof/nfra/pbc 20260907、supp 20260908）。
3. reconcile：漂移核验/桥（c1 0 / c2 10 / legacy 163），快照-审计一致。
4. classify --all：base→cluster→match→detail→clause_graph→upper 全量重建（state 11 主题×5 步灌满；R9 键统一；明细 0 缺失 0 收敛）。
5. internal：align/merged（含 inputs 指纹）；draft 107 份（条文 1272 / RFN 条款链接 78 / ⚠ 0）。
6. gates：**12 道全 PASS**；pytest common 通过；BENCHMARK 刷新（归属 1059/base 1050/final 1050/detail 1059/桥 823/state 2896/merged 107）。
7. 过程中修复运行 bug：match RFN 键、classify_state、sync 哑引用、flat raw/docs、guard 等（见各 commit）。

## 修复提交索引
批次A `78a496d` · 批次C#1 `32a7d28` · 批次B(EAST) · 批次C#2(backfill+R1) · 批次C#3(R7+R5 merged 指纹) · 批次C#4(R9+classify_state) 等（本仓 git log main）。

## 遗留治理后续（2026-09-08 同日跟进）
- ✅ R3 raw 落仓：五源 raw（gov 48MB/mof/nfra/pbc/supp）活跃复制至新仓 `data/raw`（gitignore）。
- ✅ clause 固定节点对齐：supp 条文节点重建至 0908（58 文件）。
- ✅ timeliness 统一基准补全：consolidate 基线复制现仓 + 现仓跑出唯一全量 `时效性标注结果清单_全量_20260908.jsonl`（3009 条）。
- ✅ R6：classify match/detail/upper 断点输入含 `clean_index/index.json`（clean 快照 sha），正文内容变化级联重建。
- ✅ R12：明细表叙述列（标题/发文字号/文件来源）按归属表权威快照投影刷新，人工列保留；11 明细重建 0 缺失。
- 长线留档：字段英文别名逐层落地（跨列改名影响面大，须分轮灰度）、drift 多期清单合并/808 语义命名、supp 存量 valid 展示口径（非 SSOT 链）。
