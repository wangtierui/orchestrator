# 重复实现台账（F-R06，2026-09-12）

> 用途：审查报告 F-R06 指出"重复实现群"（HTTP4/附件5/保存4/归一19/标题11/枚举5 → 维护点 50→12）。
> 本台账为**收敛进度与剩余路径的唯一登记**；新增相似实现须先查本表（防增量）。
> 纪律：归一/标题家族已完成 SSOT 化（A-10）；其余按"可见化 → 语义核对 → 合并"推进。

## 一、已完成收敛（2026-09-12）

| 群 | 原状 | 现状 | 证据 |
|---|---|---|---|
| **归一 19 处** | `norm_docno/norm_title` 私有多副本 | **SSOT 分层**（`std_lib/common_lib/norm.py`：norm_docno/norm_title/norm_title_strict）；12 处替换 import、10 处有意特化加 `# norm-specialization` 豁免；门禁"未豁免即 FAIL" | 提交 a32a150；gate_no_duplicate_libs.private_norm_unmarked=[] |
| **标题 11 处** | 标题归一散落 | 同上（strict/激进两层）+ `build_detail_tables/sync/verification_state` 等替换 | 同上 |
| **回写 3 份**（C-12） | apply / sync / classifier_pkulaw 各一套 | **单点** `apply_timeliness_to_cleaned.writeback_source`（双轨+原子+备份+status 派生）；两处改调 | 提交 a32a150 |
| **核验 4 脚本**（C-11） | supp_authority 专化重复 | 删除（并入 authority_backfill --source supp）；无命中处置三方统一 | 同上 |
| **契约字面量 4 处**（F-D11） | contract ↔ unified_schema/registry/recall 各写 | re-export 单源化 | 提交 0086f67 |
| **查表入口**（F-K03） | 各模块裸读底座 | `interfaces/base_api` + `cli base`（发布件唯一消费面） | 提交 1554d9c |

## 二、剩余重复面（收敛路径登记）

| # | 群 | 现值（约） | 收敛路径 | 风险 |
|---|---|---|---|---|
| 1 | **HTTP 客户端** | 4 套（各 collector 自封装重试/UA/限速） | 上收 `std_lib/scraper_std/http.py`（已存在）+ 各 collector 改调；需逐源回归（限速/冷却语义差异） | 中（网络行为差异） |
| 2 | **附件处理** | 5 套（下载/命名/哈希/登记） | 契约已备（`ATTACHMENT_FIELDS/attachment_view`，F-D07）；写侧按源归一验证后合并 | 中 |
| 3 | **保存/原子写** | 4 套 | `common_lib/io_atomic` 目标态；`_save_csv_rows` 等改调（部分已 re-export 契约字段） | 低-中 |
| 4 | **枚举映射** | 5 套（source/时效/正文状态/文件类型/附件事态） | `config/enums.py` 单源（已有）+ 各 mapper 改调；gate_enum_values 已校验一致性 | 低 |

**推进顺序建议**：4（枚举，低风险）→ 3（原子写，低-中）→ 2（附件，契约已备）→ 1（HTTP，最后做，逐源回归）。

## 三、防增量机制（已生效）

- `gate_no_duplicate_libs`：规范符号（公开名）非共享库重复 → FAIL；`_norm_*` 未豁免私有副本 → FAIL。
- `gate_contract` / `gate_enum_values`：契约与枚举一致性。
- 本台账：新增相似实现前先登记/评估（代码评审检查点）。

## 四、第二轮收敛判定（2026-09-12 续）

| 群 | 判定 |
|---|---|
| **枚举 5 套** | ✅ **已消解**——实测全仓仅 `unified_schema.SOURCE_SET`（config/enums 单源的 re-export）+ `ratelimit.RateLimitMode`（非五域枚举）；无本地枚举定义残留，且 `gate_enum_values` 全量校验在岗。 |
| 保存 4 套 | **读侧/模式已统一**（各 `_save_state` 均为 tmp+os.replace 原子写）；余为"跨模块私有函数同名复写"——抽公共 `io_atomic` 属长期优化（跨模块改动、收益低）→ **暂缓（登记）**。 |
| 附件 5 套 | **读侧已统一**（`contract.attachment_view`，F-D07；发布件 7 字段）；写侧按源保留（各采集器下载/命名语义差异）→ **暂缓（登记）**。 |
| HTTP 4 套 | `std_lib/scraper_std` 已具公共 HTTP/限速层；余为各 collector 的限速/冷却包装（WAF 语义差异，**逐源回归风险**）→ **暂缓（登记）**。 |

**结论**：F-R06 的"高杠杆项"（归一/标题/枚举/契约/查表）均已收敛；余 3 群为"读侧统一、写侧保留"的**有意并存**，不再计为本轮欠账，转长期优化清单。
