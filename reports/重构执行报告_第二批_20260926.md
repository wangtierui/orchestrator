# 重构执行报告 · 第二批（2026-09-26）

> 依据：`reports/全链路重构方案_修订版_v2_20260926.md`
> 提交：`8b4cfed refactor(v2 P1-2/P1-6/P1-4/P2-3a + R2/R7/R8)`（post-commit 已自动 push）
> 范围：**P1-2 · P1-6 · P1-4 · P2-3a + R2 · R7 · R8 共 7 项全部闭环**；工作树残留 **0**。
> 验证：`pytest -m "not data"` **372 通过** ｜ `cli.py gates` **22/22 PASS** ｜ `ruff check .` **All checks passed** ｜ `tools/ci_check.py` **3/3 PASS**

---

## 1 任务完成清单

| ID | 任务 | 交付物 | 验收证据 |
|---|---|---|---|
| **P1-2** | 水位 `round`/`stage`（`_SCHEMA_INT` 2→3）+ 声明式 inputs 扩充 + `dependency_edges` 四态 + `record_watermark` 校验 | `std_lib/common_lib/governance_store.py`（+`_OBSERVATION_MIGRATIONS` / `_migrate_observations`）、`interfaces/governance_api.py`、`tools/run_production_refresh.py`、`tests/test_governance_watermark.py`（新，10 用例） | 迁移实测：`user_version 2→3`、列已加、**22 行历史水位保留**（观测表未 DROP）；`within_round` 判据经单测覆盖 |
| **P1-6** | `worklist` 待办队列（`_SCHEMA_INT` 3→4）+ `WORKLIST_KIND`（8 类）+ CLI + 接 D3/D4/D5 | `governance_store.py`（表 + 4 API）、`config/enums.py`、`commands/worklist.py`（新）、`cli.py`、3 个产生方、`tests/test_governance_worklist.py`（新，9 用例） | `cli.py worklist stats/list` 可用；J7 实测 `wired=[internal_identity_conflict, rfn_clean_drift_c2, timeliness_conflict]`、`pending=5`（P2-5） |
| **P1-4** | `gate_contract` 接入 `contract_manifest.json`（file/symbol/count 三元断言）+ 清单加 `version` | `gates/gate_contract.py`（+`_manifest_checks` / `_dotted`）、`config/schema/contract_manifest.json`、`tests/test_contract_manifest.py`（新，12 用例含 4 负例） | 6 条契约全部断言通过（39/8/3/14/11 + 4 键集）；**两处历史漂移已修正** |
| **P2-3a** | 语料分层：同卷 rename + 域改名 + sha256 强制/回填 + 删双副本 + `gate_corpus_layering` | `data/corpus/{dept_policies,regulatory_stats}/`（迁移落点）、`paths.py`（`CORPUS_DIR`/`INBOX_DIR`）、`tools/ingest_corpus.py`（3 个新维护模式）、`tools/dedupe_original_storage.py`、`gates/gate_flat_layout.py`、`gates/gate_config_integrity.py`（J8）、`reports/corpus/*` | **硬链接 811/879 迁移前后不变**；清单与本体自洽（1027/313）；`hash_mode=sha256` 且空哈希 **0** |
| **R2** | `_p.log` 出库 | 删除该文件（`.gitignore` 规则上一批已就位） | 工作树残留 0；`git` 已记录删除 |
| **R7** | ruff 全仓归零 | 26 → **0**：`--fix` 22 处 + 手修 4 处 | `ruff check .` → All checks passed；`ci_check` ruff 闸由**红转绿** |
| **R8** | 归类并处置 22 项历史未提交改动 | 归类 = 19 份 `docs/reports/*.md`（09-22 再生成）+ `docs/reports/_manifest.json` + `BENCHMARK.md`（09-22 再生成）+ `modules/.../mof_collector.py`（09-22 P0 修复）；**全部归档提交**，`BENCHMARK.md` 另重跑至 22 门禁 | `git status --porcelain` → **0 条** |

### P2-3a 迁移实测（关键不变量）

| 项 | 迁移前 | 迁移后 |
|---|---|---|
| 本体落点 | `modules/regulatory_scrapers/data/corpus/<domain>/` | **`data/corpus/<domain>/`**（同卷 rename） |
| 域 | `dept_policies` / `east2_m20` | `dept_policies` / **`regulatory_stats`** |
| 本体文件数 | 1028 / 314（含域内清单） | **1027 / 313**（域内清单已删） |
| `originals` 硬链接 | 811 / 879 | **811 / 879（不变）** ← R9 风险未发生 |
| 清单 sha256 | 1019 + 313 条**全空** | **1027 + 313 条全有值**（`hash_mode=sha256`） |
| `_index.json` | 有 `east2_m20` 条目 | 2 域、`total_files=1340`、无幽灵域 |

---

## 2 变更摘要

**提交规模**：`8b4cfed` 覆盖约 60 个文件（含 R7 全仓 ruff 修复）。

### 2.1 行为级变更（需知晓）

| # | 变更 | 影响 |
|---|---|---|
| B1 | `record_watermark` **拒写空 `produced_by`** | 调用方若未声明产出方，水位不再入库（原允许任意字符串） |
| B2 | `dependency_edges` 新增第 4 态 `ok_within_round` | **同轮**版本差异不再阻断（`clauses:{src}` 得以声明 `cleaned:{src}` 上游） |
| B3 | `clauses:{src}` 现声明 `cleaned:{src}` | 下一轮刷新起生效；同轮 → 不触发 `gate_watermark` FAIL |
| B4 | `wm_status` 返回 detail 增 `within_round` / `within_round_deps` | 三态 API 不变（`ok/stale/unknown`），信息更细 |
| B5 | `worklist` 表 + `cli.py worklist` | 新增第 13 个命令；D3/D4/D5 检测点开始写队列 |
| B6 | 契约清单 `detail_table_fields.count` 10 → **14**；`base_json_keys.file` 改为 `interfaces/contract.py` | 清单与实现对齐；`gate_contract` 会持续断言 |
| B7 | 语料本体落点 `modules/.../corpus` → **`data/corpus/`**；域 `east2_m20` → `regulatory_stats` | 引用方已同步（`ingest_corpus`/`dedupe_original_storage`/`gate_flat_layout`/`paths`） |
| B8 | `ingest_corpus`：sha256 **默认强制**；`--no-hash` 记 `hash_mode=size_only` 并单列 `skipped_existing_unverified` | 幂等语义由"同名即跳过"收紧为"**内容一致才跳过**" |
| B9 | `ingest_corpus` 新增 `--reindex` / `--backfill-hash` / `--resync-manifest` | 归集本体的就地维护能力（域改名/退役/清单漂移收口） |
| B10 | 清单 `file_count` 语义变为**本体文件数**（`dept_policies` 1019 → 1027） | `source_dir` 保留作溯源；与源目录计数**不再相等**（见 §3 N-12） |

### 2.2 新增文件（6）

`commands/worklist.py`、`tests/test_governance_watermark.py`、`tests/test_governance_worklist.py`、`tests/test_contract_manifest.py`、`data/corpus/regulatory_stats/`（迁移）、`reports/corpus/regulatory_stats.manifest.json`

---

## 3 执行中发现并处置的问题项（9 项）

| # | 问题 | 处置 |
|---|---|---|
| **N-10** | `ingest_corpus` 的 `--src/--domain` 是 argparse 级 `required` → 新增的"就地维护"子命令（`--reindex`/`--backfill-hash`）**无法调用** | 改为模式内显式校验 |
| **N-11** | `_update_index` 只按域名"替换或追加"，**无法删除改名后的旧条目** → 域改名会留**幽灵域**（实测会出现 `east2_m20` + `regulatory_stats` 两条） | 新增 `--reindex`：由 `reports/corpus/*.manifest.json` **全量重建**索引（清单为唯一事实源） |
| **N-12** | **`dept_policies` 清单与本体布局长期不一致**：源侧含中间目录（`1.营销部/附件二-…/x.pdf`）而本体已拍平（`1.营销部/x.pdf`）、部门名亦有改名（`9.健康事业部` vs `9.健康保险事业部`）→ 1019 条声明中**仅 433 条**能对上，586 条为陈旧路径 | 新增 `--resync-manifest`：**以本体为准**重建 `files`（记 `resync.dropped/added` 便于追溯）；并加判据 **J8.3**（清单条数 == 本体文件数）防复发 |
| **N-13** | `analysis:manifest` **仍不能**声明 `inputs`：它产在阶段 6.8 而门禁在阶段 6 运行 → 门禁看到的是**上一轮**产物（artifact.round ≠ dep.round）→ `ok_within_round` **不适用**，声明即恒 FAIL。根因是"产/判顺序耦合"，与 `clauses:{src}` 情形**本质不同** | 保留"只观察"，根因与两种解法（gates 后移 / analysis 前移）登记为 **P3-5 待决项**（改动主链顺序需独立评估影响面） |
| **N-14** | J7 的"产生方"扫描把 **`tests/`** 纳入 → 单测**故意**写入未登记 kind（覆盖软校验负例）与待接 kind（覆盖队列行为）→ 判据被夹具**假命中** | 扫描排除 `tests/`（生产判据只看生产代码） |
| **N-15** | **本机编辑/同步链路会把 `new_str` 里的反斜杠二次转义**：正则 `\(` 落盘为 `\\(`（语义从"字面括号"变成"字面反斜杠 + 分组"→ 全部漏匹配）；字符集 `" \t\r\n"` 落盘为**字面四字符** → 跳过空白失效 | 记入代码注释；相关实现一律改**零转义**原语（`str.find` / `str.isspace()` / `os.sep`） |
| **N-16** | J7 的 kind 候选判定用逐字符 `islower()` → **含数字的 kind 被漏掉**（`rfn_clean_drift_c2` 的 `2` 不满足 `islower()`） | 改为 `cand.isidentifier() and cand == cand.lower()`（单测检出后定位） |
| **N-17** | `worklist_add` 的 `item_id` 在"同秒内处置后重新登记"时**主键冲突**（被单测 `test_can_reopen_after_resolve` 检出） | 时间戳加毫秒 + 冲突自增后缀 |
| **N-18** | `tests/test_governance_store.py::test_wm_status_three_states` 受环境遗留 `REG_ORCH_RUN_ID` 影响而**变脆**（P1-2 后同轮不再判 stale） | 测试显式控制轮次（`round_id="R1"/"R2"`）；新增同轮用例 `test_wm_status_ok_within_round` |

---

## 4 验证结果

| 检查 | 结果 |
|---|---|
| `pytest tests -m "not data"` | **372 通过**（上一批 340 + 新增 32） |
| `python cli.py gates` | **22/22 PASS** |
| `ruff check .` | **All checks passed**（上一批为 25 处红） |
| `tools/ci_check.py` | **CI: PASS 3/3**（ruff / pytest / gates）——该闸首次全绿 |
| SQLite 迁移 | `user_version 2→3→4` 幂等；观测表历史行零丢失 |
| 语料迁移 | 同卷 rename；**硬链接 811/879 不变**；清单/本体自洽 |
| `git status --porcelain` | **0 条**（工作树完全干净） |

---

## 5 遗留风险说明

### 5.1 高

| # | 风险 | 说明 | 建议 |
|---|---|---|---|
| **R1** | 路线图 **13/22 完成** | 未做：P1-3（interfaces 收口，15 处注入未收敛）、P1-5（日志，33 处 print）、P2-1/P2-2、P2-3b、P2-4、P2-5、P2-6、P2-7、P3-1..P3-4 | 下一批首选 **P1-3 + P1-5**（同属"分层收口与可观测性"，且为 P2-6 前置） |
| **R2** | **P3-5（N-13）未决**：`gates`（阶段 6）与 `analysis:gen`（阶段 6.8）顺序耦合 | 只要不解决，`analysis:manifest` 就**无法**声明依赖边，属水位覆盖度的真实缺口 | 需评估两种解法的连带影响（`gate_result` 的 run_id 归档、BENCHMARK 基线、人工处置习惯）后二选一 |
| **R3** | **清单语义变更需知晓**：`dept_policies.manifest.json` 的 `file_count` 由"源侧 1019"变为"本体 1027" | 与源目录直接比对的旧脚本/习惯会看到差异；`source_dir` 仍保留溯源 | 在 `_index.json` 的 policy 已注明；如需源侧口径，应另建"源快照"而非复用本体清单 |

### 5.2 中

| # | 风险 | 说明 |
|---|---|---|
| **R4** | `gate_runtime_hygiene` **恒 PASS** | 三项 `_STRICT_* = False`：裸整数退出码 79 处、`except-pass` 138 处、接口空壳 3 处（`theme_api`）。需 P1-3/P1-5 后按期转严格 |
| **R5** | `import_bootstrap` 基线仍大 | `modules 128 / tests 35 / interfaces 15 / gates 17 / std_lib 9 / tools 14`（`commands` 为硬零）。存量收敛依赖 P1-3 与后续批次 |
| **R6** | `worklist` 的 5 个 kind 待接 | `rfn_theme_uncertain`(D1) / `internal_unaligned`(D2) / `relevance_boundary`(D6) / `corpus_needs_review` / `ingest_quota_blocked` 已在 `_PENDING_PRODUCERS` 披露，J7 会在接入后**要求移除**（防"接了不改"） |
| **R7** | 交付件数据面未刷新 | `docs/reports/*`（19 份）的数值基于 **09-22** 数据快照（本批只重跑 `BENCHMARK.md` 基线，未重跑数据链）。`gate_clean_schema` 的 `LEGACY_WITH_ERRORS = {nfra:5, supp:4}` 仍在等一次全链 clean |
| **R8** | `data/corpus` 不与 `originals` 同生命周期 | 本体是"审计层 + 硬链接目标"，若被误删，`originals` 的硬链接仍有效（inode 保活）但审计凭证断裂 → J8 只断言自洽，不断言备份 |

### 5.3 低

| # | 风险 |
|---|---|
| **R9** | `paths.INBOX_DIR` 为预留守位（P2-3b 接入）；`paths.py` 另有 6 个预留守位常量 |
| **R10** | `retired_comment_mentions`：`migrate_collectors_p3b.py` 的历史注释标记仍散落在 35 处（J3 已只计可执行引用，注释提及仅披露） |
| **R11** | `gate_config_integrity` 的 D 组仍"待落地"：`config/triggers.yaml`、`config/schedule.yaml` 未创建 |
| **R12** | `_p.log` 已从版本库删除，但若本地再次产生同名文件，`.gitignore` 规则（上一批已加）会阻止其入库 ✓ |

---

## 6 下一批建议（按依赖与风险排序）

1. **P1-3** `interfaces` 收口（I-1~I-7）：补齐 `theme_api` 实装、切断 3 条"数据层跨模块直读"（`timeliness_review` 的 `".."` 路径、`base_publish` 直读两底座），并把 `gate_no_cross_module_import` 的**判据 D**（数据层跨模块读取）落地 → 同时把 `import_bootstrap` 的 `interfaces 15` 收敛到个位数。
2. **P1-5** 日志与失败语义：`std_lib/common_lib/logging.py` + 33 处 print → LOG + `_run` 增字段（`exit_code`），使 `gate_runtime_hygiene` 的 `_STRICT_EXITCODES` 具备转严条件。
3. **P2-5** 接入 D1/D2/D6 + `corpus_needs_review` + `ingest_quota_blocked`（worklist 机制已就绪，成本低）。
4. **P3-5** `gates`/`analysis:gen` 顺序解耦（解 R2，使 `analysis:manifest` 可声明依赖边）。
5. **R7（数据面）** 跑一次全链 clean + 刷新 `docs/reports/`，把 `LEGACY_WITH_ERRORS` 归零并删除该基线表。

**文档结束。**
