# 修复批次完成报告（2026-09-12）

> 依据：《最终版审查报告_20260912》§四 修复路线图 · **阶段 1（P0 静默与门禁）**
> 指令：EAST 其余目录/部门制度**等待修复完成、最后归集**；按路线图制定分批次任务并逐项执行（自主推进）。
> 提交：`39d37d1`（代码 28 项 + 数据/文档 + 对齐表/六件套迁入）

---

## 一、批次总览（10 批 / 41 项）

| 批次 | 修复项 | 内容摘要 | 状态 |
|---|---|---|---|
| A 门禁可信度 | F-S01 / F-S02 / F-S09 / F-C06 | recall rc 语义修正（四门禁 FAIL → exit 1，原恒 0）；N-3 lint **接线修复**（旧模块已迁 `gates.gate_hardcoded_snapshots`，原恒 ModuleNotFoundError 静默跳过；导入失败改 FAIL）；三道空跑门禁（citations/provenance/ssot）改 FAIL + rfn_drift 首次基线改 FAIL；**核验覆盖率门禁**（2975/4043=0.7358，<0.70 FAIL，1068 条透明化） | ✅ |
| B 时效默认值 | F-S03 | 三处"空→valid"清零：registry 登记置 `pending`；base/_rebuild 删 `or "valid"` 兜底（空保空）；gate_ssot 补"一边空一边非空即漂移"（原双向非空才比） | ✅ |
| C 登记链 | F-S04 / F-C01 | supp 登记 `organ=` TypeError 修复（去参 + failed 计数）；**新增 `cli.py rfn register/lookup`**（登记唯一入口，含部分键回退）——补登记 EAST 82 号函 → `RFN-9e135d3ac33a7823`（归属表 1059→1060） | ✅ |
| E 引用门禁 | F-S07 / F-L07 | 对齐表缺失不崩（空表→未实检明确报错）+**连带路径 bug**（`_MODULES` 多算一层，`import rfn` 恒失败）；**迁入对齐表（R-01~R-43）+ 六件套** 至新仓 docs（按拍平纪律重命名） | ✅ |
| F 索引与条款 | F-C02 / F-C04 / F-C05 / F-D01 | clauses 内容指纹（同日改写触发重建，原仅比日期）；classify 断点纳主题归属表（re_theme 可迁移）；apply 回写后重建 clean_index；indexer 白名单重建**保留主题列**（primary_theme 不再被冲） | ✅ |
| G 主题链 | F-D02 / F-D03 / F-D04 | upper_laws 主题统计断链修复（改「主题」列反解，实测恢复 8 主题引用）；cluster 值域统一（u_fix 短别名归一为完整组名）+ 明细子主题随 final 刷新 + citerefs 补 cluster；**未命中清单落盘**（`*_matched_miss.json`）+ Gate4 覆盖率检查 | ✅ |
| H 同日改写 | F-D05 ①~④ | ① clean 管道 `hash_files=True`（index.json 全源 sha 落地）；② validate sha 缺失降级 size 比对 + degraded 计数；③ recall 签名纳内容 sha；④ 断点经 index.json 级联（①修复后自动闭合） | ✅ |
| I 编排接线 | F-O01 / F-O05 / F-C07 / A-12 / A-14 | 生产刷新接入 `classify --all`（阶段 2.5，底座不再静默过时）；supp 本地摄取入编排（`--supp-batch` 可选）；删假步骤 `raw_snapshot`；`shlex.split` 修复参数拼接 | ✅ |
| J 结构清理 | H-07 / A-10 / A-11 / A-09 / A-08 / C-11 | status 三系清理（gov 死分支删、mof/pbc/supp 源特异 status 废弃、apply 派生英文）；私有归一副本**可见化**（清单入 gate detail）；`cli --help`；sync 门禁引用修复 + 镜像改走 `refresh()`（原路径恒 FileNotFoundError）；sync 标注辅助编排；核验"无命中写痕"统一 | ✅ |
| K 冗余清理 | F-R01 / R02 / R04 | backups 15→1（**释放 636MB**）；timeliness 旧清单 10 件；临时脚本 21 件 | ✅ |

---

## 二、关键实证数据

| 指标 | 修复前 | 修复后 |
|---|---|---|
| recall 退出码 | 恒 0（FAIL 不可感） | 四门禁 FAIL → 1（实测触发并暴露真实 Gate1 失败） |
| N-3 lint | 恒 ModuleNotFoundError 静默 | 实检（findings=0）+ 失败即 FAIL |
| index.json sha | 全源 `null` | 全源落地（如 gov `74c35af2…`）|
| recall 签名 | 无 sha（同日改写隐身） | 含内容 sha；`validate.degraded_no_sha=0` |
| 核验覆盖率 | 无门禁 | 2975/4043（0.7358）落 detail + 警告，<0.70 拦截 |
| upper `cited_by_themes` | 9/9 空（统计断链） | 恢复（保险法被 8 主题引用；空项经核实为真实无引用） |
| T1 cluster 值域 | 双值域（"S3" vs "S3银邮渠道"） | 统一（final 与明细子主题完全一致） |
| matched 覆盖率 | 缺口不可见（Gate4 只验超集） | 1011/1050（3.71%）可见 + miss 清单落盘 |
| 归属表登记 | supp 静默失败 | CLI 补登记 82 号函（pending/T10） |
| docs 引用核验 | 对齐表缺失即崩 | R-01~R-43 全有效；8 文件 0 未命中 |
| 磁盘 | — | 释放 636MB（backups） |

---

## 三、验证

- `ruff check .`：**All checks passed**
- `pytest tests`：**31 全过**
- `cli.py gates`：**13 道全 PASS**
- `run_retrieval_after_checks --check-only`：四门禁 PASS（rc=0）
- T1 全链重跑 6 步（base→cluster→match→detail→clause_graph→upper）全 ran

---

## 四、遗留（明确登记，下一步）

| # | 项 | 说明 |
|---|---|---|
| 1 | **F-L05 最后归集**（按指令推后） | EAST2.0 其余目录（~1.8GB）、保单登记三期（32.8MB）、部门制度（1019 文件/316.8MB）→ 监管统计域 / IPB，工具已就绪（`supp_ingest_local_dir`）|
| 2 | C-10 / C-12 收敛专项 | sync 双实现分叉、cleaned 回写 3 份——需语义逐处核对后合并（独立批次，避免草率重构破坏生产核验链）|
| 3 | A-10 副本清零 | 16 个文件私有 `_norm_docno/_norm_title`（SSOT=`common_lib.norm`，与 `rfn._norm_title` 存在标点/lower 差异需核对）——当前已可见化不阻断 |
| 4 | 存量复跑 | OCR 校正（F-S05 代码已修）与 status 派生（H-07）需**重刷 cleaned** 生效——建议并入归集后的生产复跑 |
| 5 | 阶段 2（P1 编排与契约） | Base Contract v1（published/ + SQLite/FTS5 + manifest）、llm_wiki 接入、契约单源化、交付补齐启动 |
| 6 | 阶段 3（P2） | 规划交付章节、view_active/时间切片、`cli query`、告警接入、状态文件 schema 统一 |

---

> 编制：CodeBuddy · 2026-09-12 · 批次依据《最终版审查报告_20260912》F 域台账 70 项
