# SSOT 收敛专项报告（F-D06 / F-D07 / F-D10，2026-09-13）

> 背景：《遗留项检视_20260913》——F 域台账最后 3 项 P1（SSOT 分散 / 附件 5 套 + pbc 缺轨 / 条款行缺维度）。
> 方法：先侦察实证（6 轮数据/代码扫描）→ 契约收敛（读取侧归一）→ 数据重建 → 回归。
> 提交：见 git log（本报告同批）｜验证：pytest 181 / gates 14 / ruff 0 / base 双底座刷新。

---

## 一、F-D06 归一收敛（文号 + 日期）

**实证**：文号归一已有 SSOT（`common_lib/norm.py`，A-10 收敛）；日期存在 4 套实现
（cleaner 解析层 / crawler_common 抽取型 / supp 摄取预处理 / pkulaw 比较键）。

**交付**（`std_lib/common_lib/norm.py` 分层登记扩展）：
- **文号豁免补全**：`pkulaw_cli.norm_docno`（法宝库比较专用形态）、`scanner/build_outputs._norm_docno`
  （空占位判定型，保持原语义防 recall 漂移）；
- **日期归一分层**（4 层）：解析/清洗层唯一实现 = `cleaner.normalize_date`；抽取型（crawler_common）、
  摄取预处理（supp）、比较键（pkulaw）显式豁免登记；
- **收敛基线测试**：`tests/test_ssot_convergence.py` 跨实现兼容断言（4 格式 + 混排抽取 + docno 基线），防再分叉。

## 二、F-D07 附件契约收敛（5 套命名 + pbc 轨）

**实证**：五源附件元素键各异（gov 16 键 / mof 15 / nfra 12 / supp 4 / **pbc 全量 571 条 0 附件**）；
pbc 为**扁平记录模型**（附件独立成行 link_type=="attachment"，非嵌套 attachments）。

**交付**（`interfaces/contract.py`）：
- **ALIASES 补漏**：`file_name` ← title（nfra）；`kind` ← attachment_kind（gov/mof 主键位）；
- **新增 `record_attachment_views(rec)`**：统一「记录 → 附件视图列表」单入口——
  嵌套模型（gov/mof/nfra/supp 及发布件）逐项 `attachment_view`；**pbc 扁平模型**单元素视图
  （file_name←title / kind←file_type / url←detail_url / text_len←len(content)）；
  **修正"附件消费按源分支"**（F-D07 影响面）；
- **pbc 附件数据缺口登记**：采集侧能力齐备（下载/落盘/解析全链），但当前数据 0 附件——
  需真实网络重采集补齐（运行期任务，非代码缺口）。

## 三、F-D10 条款行维度补全（核心）

**实证**：clause 行仅基础字段（dedup_key/source_url/document_number/title/章节条款），
时效/RFN/日期不可达（审查 L106 "clause_index:171-183"）。

**交付**：
- `clause_index._load_rfn_bridge()`：桥表（`rfn_clean_bridge.csv` 828 行）批量预载（缺表降级不阻断）；
- 行 schema **+4 维**：`rfn`（桥表投影 dedup_key→ / source_url→）、`timeliness_status`、`publish_date`、
  `effective_date`（cleaned 直取）；
- `contract.CLAUSE_LINE_FIELDS` 12 字段（8→12）；docstring 同步；
- **全量重建 + 契约校验**：

| 源 | 行数 | rfn | 时效 | 发布 | 生效 |
|---|---|---|---|---|---|
| gov | 605 | 6 | 588 | 605 | 357 |
| mof | 870 | 8 | 173 | 870 | 616 |
| nfra | 1939 | 767 | 1929 | 1939 | 1583 |
| pbc | 571 | 17 | 569 | 558 | 86 |
| supp | 58 | 30 | 58 | 57 | 51 |
| **合计** | **4043** | **828（20.5%；桥表 100% join）** | **3317（82.0%）** | **4029（99.7%）** | **2693（66.6%）** |

- `validate_schema: consistent=True`（4043 文件 / 61433 条款逐键一致）；
- **base 双底座刷新**：external records 4043 + clauses 61433；internal records 4043 + clauses 11626。

## 四、验证

| 项 | 结果 |
|---|---|
| 新增测试 `test_ssot_convergence.py` | 9 用例全过 |
| pytest 全量 | **181 用例全过**（含 e2e clause 契约断言） |
| gates | **14 道全 PASS** |
| ruff 全仓 | All checks passed |
| clause 重建 + validate | consistent=True |
| base publish 双底座 | 刷新成功 |

## 五、遗留（登记）

1. **pbc 附件重采集**（数据层，需真实网络——届时 `record_attachment_views` 直接消费）；
2. base_external 的 rfn join 与 clause 行内 rfn 为**双实现**——后续可让 base 透传 clause 行 rfn
   减少重复 join（F-D11 同类"双字面量"风险，低优先）；
3. 日期 4 套中 3 套为**显式豁免**（语义真实差异）——保持登记制，不强行合并。

> 编制：CodeBuddy · 2026-09-13 · 回链：《遗留项检视_20260913》/《最终版审查报告_20260912》F 域台账
