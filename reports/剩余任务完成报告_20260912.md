# 剩余任务完成报告（PDF Paddle 重扫核查 / 工具化 / F-R06 判定，2026-09-12）

> 依据用户指令：①核查"PDF 是否已完成 PaddleOCR 重新扫描"；②继续推进剩余任务（不含 EAST 报送文档结构化）。
> 提交：`557e945`（本轮）+ 前序 `7394893`/`90aa98f`/`b2edc11`/`c5013f8`。

---

## 一、PDF PaddleOCR 重扫 · 核查结论（附补遗）

### 1.1 核查实况（702 个 PDF）
| 类别 | 数量 | 状态 |
|---|---|---|
| 有文本层 PDF | 671 | **文本层直提**（不经 OCR；无需重扫）|
| 扫描件（首轮 46 目标）| 32 | ✅ 已 PaddleOCR 提质（ocr_engine=paddle）|
| 扫描件"边缘件"（文本层少量）| 14 | 首轮按判据未 OCR / 已标记 |

### 1.2 **发现判据盲区**（本轮补遗）
- **17 个 PDF 为零文本但未进 force 清单**：`fitz 首页读出≥30 字（水印/页眉）→ 判非扫描件`；
  而 `pypdf/pdfplumber 提取为空` → 实际是扫描件 → **遗漏**；
- **修复**：force 目标增强为「**fitz<30 字 或 文本层提取残量<100 字**」，新增 `ocr_force_engine`
  幂等标记（Paddle 后仍低质者一轮收敛）；
- **补扫执行**：30 个目标全量 Paddle 重扫 **进行中**（后台批处理，大文档长尾——最大 3.7MB 扫描件
  约 15-20 分钟/份；**当前进度：paddle 33 / 剩余待扫 27**）。

> 若批处理中断，**一键续跑**（幂等，可反复执行至 `total=0`）：
> ```bash
> python cli.py internal reocr --force --limit 3
> python cli.py internal index --source-dir "modules/regulatory_scrapers/data/corpus/dept_policies"
> python cli.py internal merged && python cli.py base publish --base internal
> python tools/sync_wiki_sources.py --out "D:\DeMon KB\监管法规库" --scope all --prune
> ```

## 二、sync --prune 工具化（`557e945`）

- `tools/sync_wiki_sources.py --prune`：清理"manifest 中属本 scope 但**不在当前发布件**的 md"
  （IPN 重命名等场景；本 scope 判据 = IPN- 前缀 vs 外部队列）；
- dry-run 实测 **清理 0**（vault 已对齐 4985）；后续 IPN/命名变更无需再手工清理。

## 三、F-R06 第二轮判定（登记台账）

| 群 | 判定 |
|---|---|
| **枚举 5 套** | ✅ **已消解**（实测全仓无本地枚举定义；config/enums 单源 + gate 在岗）|
| 保存 4 套 / 附件 5 套 / HTTP 4 套 | **读侧已统一**（原子写模式/attachment_view/公共 HTTP 层）；写侧为有意保留的源差异 → **转长期优化清单** |

## 四、终验

- 本轮已验：ruff 0（提交 `557e945`）；sync/prune/台账落地；
- **Paddle 补扫完成后**执行：`pytest / gates / recall` 终验 + 发布件链重跑（续跑命令见 §1.2）。

> 编制：CodeBuddy · 2026-09-12（PDF 补扫进行中，完成值以批处理日志 `logs/r_force_all.log` 为准）
