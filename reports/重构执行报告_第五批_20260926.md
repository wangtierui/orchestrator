# 重构执行报告 · 第五批（2026-09-26）—— 路线图收口

> 依据：`reports/全链路重构方案_修订版_v2_20260926.md` §3.7（P2-4）、§3.10（P2-3）、§3.15.5（P2-7）、
> §3.13.1/§3.13.7（P3-1/P3-4）、§3.12.4（P3-3）、§3.3.1（P3-5）
> 范围：**P2-3 / P2-4 / P2-7 / P3-1 / P3-3 / P3-4 / P3-5 全部闭环 + 判据 R/U/V 上线**。
> 至此 **路线图 P0–P3 全部条目闭环**（22/22）。
> 验证：`tools/ci_check.py --cov` **CI: PASS 5/5**（ruff 全绿 ｜ mypy 非阻断 ｜ pytest **456** ｜
> gates **22/22** ｜ coverage **45%**，`fail_under=20`）。

---

## 1 任务完成清单

| ID | 任务 | 优先级/依赖 | 交付物 | 验收证据 |
|---|---|---|---|---|
| **P2-4** | 测试与 CI 收口 | 中 | `pyproject` coverage 配置、`ci_check.py`（+mypy）、`.pre-commit-config.yaml`（新）、`tests/test_gates_matrix.py`（新，55 例） | 用例 **401 → 456**（≥450 ✅）；覆盖率 **45%**（`fail_under=20` ✅）；T3 脆弱断言改造（`==40/==11/==17/==18` → 派生）✅；mypy 非阻断（未装则仅告警）✅ |
| **P2-3** | 数据生命周期 | 中 | `tools/retention.py`（新）、`archive/` + 6 处门禁排除集 + `.gitignore` | dry-run 默认；**实测归档 55 项 / 17.7 MB**：`classifier/backups` **24 → 10**（方案目标 ✅）；台账 `reports/retention_20260926.json`；**只移动不删除** |
| **P2-7** | 退役收尾 | 中（依赖 P0-5 ✅） | J6 判据转严 | 仓根未跟踪且未忽略的文件 → **阻断**（三选一：入库/忽略/删除）；git 不可用才降级告警。实测正确拦下新文件 `.pre-commit-config.yaml`（提交后放行） |
| **判据 R** | 步骤清单一致性 | 高（收口 P2-6） | `gate_config_integrity._check_step_order()` | `STEP_ORDER` ↔ `_run("<step>")` 调用点**双向闭合**（17=17）；删步骤/漂移即 FAIL |
| **P3-4** | `.codebuddy` 引用清零 | 低 | 判据 **U1** | 引用数 **0**（排除集纯字符串条目豁免——那正是"忽略它"的正解） |
| **P3-1** | 助手状态与记忆权威 | 低 | `.codebuddy/README.md`（新）+ 判据 **U** | U2 权威口径声明（仓外为权威/仓内历史归档）；U3 调度触发事实源不回指 `.codebuddy`；U4 该目录不得新增 `*.yaml` 声明 |
| **P3-3** | 监管统计域下游消费 | 低 | `config/inbox_registry.yaml`（`consumer: pending_consumer`）+ 判据 **V** | 域 `consumer` 非空且已登记；`regulatory_stats` **显式标注 pending_consumer**（不假装已消费）；`dept_policies` → `internal_policy_base` ✅ |
| **P3-5** | gates 与 analysis:gen 顺序解耦 | 高（**真缺口收口**） | `gates` 移为**链尾** + `analysis:manifest` 声明 inputs | 原 N-13 的"声明即结构性恒 FAIL"消失；`gate_result` 仍记同一 run_id（无归档变化）；docstring 阶段编号与"18 道"旧口径同步修正为"22 道 / 链尾" |

**P3-5 定案理由**（原 N-13 方案 (a)，影响面已评估）：
① 门禁看到**本轮** `analysis:manifest` → 可声明 7 项 inputs（`clean_index/rfn_attr/rfn_theme/relations_index/merged_view/published:external/published:internal`）；② 与人工心智一致（"最后一道门"）；③ `gen_benchmark` 独立跑门禁，BENCHMARK 基线不受影响；④ 链尾失败时其前产物已落盘（与原顺序下"analysis 在门禁后"一致）。

---

## 2 变更摘要

### 2.1 行为级变更（B1–B6）

| # | 变更 | 影响 |
|---|---|---|
| B1 | `gates` 收束为链尾 | `analysis:manifest` 依赖声明**可落地**；交付门禁覆盖本轮全部产物 |
| B2 | `retention.py` 提供保留策略 | 备份/临时产物不再单调增长；归档走 `archive/`（可复核，不删除） |
| B3 | J6 转阻断 | 仓根"随手丢文件"从报告项升级为硬约束 |
| B4 | `ci_check` 增 mypy + coverage `--fail-under=20` | 覆盖率成为可判定门槛（45% 当前）；类型检查先"可观察" |
| B5 | 判据 R/U/V 上线 | 清单漂移、`.codebuddy` 回指、域无消费者三类"静默退化"全部变成可阻断断言 |
| B6 | `.pre-commit-config.yaml` | 提交即跑 ruff + 22 道门禁 + 调度一致性（本地、零外部依赖） |

### 2.2 新增文件（5）

`tools/retention.py`、`.pre-commit-config.yaml`、`.codebuddy/README.md`、`tests/test_gates_matrix.py`、
`reports/retention_20260926.json`（归档台账）。

---

## 3 执行中发现并处置的问题项（3 项）

| # | 问题 | 处置 |
|---|---|---|
| **N-32** | 判据 R 的清单解析被**注释里的同名提及**误伤：文件头 `_ARTIFACTS` 注释出现「现 `STEP_ORDER` 末位」，`_tuple_of_quotes` 用裸名查找命中注释 → 就近吞掉后面的 `(` → 清单解析为空（R 误报"未声明 STEP_ORDER"） | `_tuple_of_quotes` 的 marker 改为带赋值特征的 `STEP_ORDER:`（注释里不会以冒号紧跟）；并留注释说明"注释提及"陷阱 |
| **N-33** | `_check_domains()` 两处缺陷：① 忘初始化 `detail`（`NameError`）；② 键名 `domains` 与 P2-3a 的 `_check_corpus()`（其在 run() 中**之后**执行）**同名互相覆盖** | ① 补初始化；② 键名改 `inbox_domains` 并注释"同名覆盖"陷阱 |
| **N-34** | 冒烟暴露 3 处自身缺陷（与 N-28 同类）：`_QUOTES` 未定义（应复用 `_WL_QUOTES`）、`loop control variable dirpath not used`（B007）、`retention.py` 行尾空白 | 全部修复；ruff 全绿 |

---

## 4 验证结果

| 检查 | 结果 |
|---|---|
| `tools/ci_check.py --cov` | **CI: PASS 5/5**（ruff / pytest / gates / coverage 全绿；mypy 非阻断告警 = venv 未装 `mypy` 模块，`dev` 依赖已声明） |
| `ruff check .` | All checks passed |
| `pytest tests -m "not data"` | **456 通过**（483 collected，27 个 `data` 用例 deselect） |
| `python cli.py gates` | **22/22 PASS**（含判据 R/U/V/S/T/J7，全部阻断态） |
| `coverage report` | **TOTAL 45%**（`fail_under=20` ✅） |
| 判据 R | `declared=17 wired=17`，双向闭合 |
| `tools/retention.py --apply` | 归档 55 项 / 17.7 MB；`classifier/backups` 24→10；台账落盘 |
| `cli.py run --dry-run` | 步骤序为 `…analysis:gen → watch:baseline → gates`（gates 链尾）✓ |
| `git status --porcelain` | 0 条 |

---

## 5 遗留风险说明

### 5.1 高

| # | 风险 | 说明 | 建议 |
|---|---|---|---|
| **R1** | `gate_runtime_hygiene` 判据①②仍只披露 | 裸整数退出码、`except-pass` 存量未收敛（`_STRICT_* = False`）—— 这是**存量改造**（`tools/`、`modules/` 大量历史 `return N` 与 `except ...: pass`），非路线图条目 | 作为独立收尾批，一次性机械收敛后置 True |
| **R2** | **交付件数据面仍为 09-22 快照** | `docs/reports/*`（19 份）未重跑；`gate_clean_schema.LEGACY_WITH_ERRORS={nfra:5, supp:4}` 待全链 clean 归零 | 需一次真实 `cli.py run` 全链刷新（数据面，非重构） |

### 5.2 中

| # | 风险 | 说明 |
|---|---|---|
| **R3** | **D6（`relevance_boundary`）待接** | `filter_clean_relevance.py` 的 BOUNDARY 逐条登记会灌爆队列（数千条）；需先定"只登记人工已介入子集/抽样"口径；`_PENDING_PRODUCERS` 持续披露 |
| **R4** | `analysis:manifest` 的 inputs 声明需**实测背书** | 声明已落地且判据 R 通过，但尚未经一次真实全链运行由 `gate_watermark` 实测（需 R2 的数据面刷新） |
| **R5** | mypy 未真正跑过 | venv 未装 `mypy`（`dev` 依赖已声明）；装后首跑会报历史类型错误（非阻断，先看基线数字） |
| **R6** | `archive/` 目录本机存在但已 gitignore | 归档物不入库（设计如此：非源码）；跨机审计靠 `reports/retention_*.json` 台账而非目录本体 |

### 5.3 低

| # | 风险 |
|---|---|
| **R7** | `retention.py` 只覆盖已声明的策略类目；`reports/_tmp/logs` 等新目录的保留期随 `reports_tmp` 的 7 天一并覆盖（`exclude_names` 白名单内） |
| **R8** | `gate_config_integrity` 的 `run()` 有历史变量复用（`_check_corpus` 复用 `p4,d4`）—— 可读性瑕疵，不影响正确性；已在 N-33 同批注明 |
| **R9** | `.pre-commit-config.yaml` 未实际 `pre-commit install`（工具已交付，安装是使用侧动作） |

---

## 6 收口结论

路线图 **P0-1~P0-6、P1-1~P1-6、P2-1~P2-7、P3-1、P3-3、P3-4、P3-5** 全部闭环。重构目标达成：
- 唯一执行入口 `cli.py run`（+doctor/status/schedule/triggers/worklist，命令 12→18）；
- 门禁 18→22 道 + 6 组判据（S/T/J7/R/U/V），且其中 4 组已转阻断；
- 校验假成功、水位四态、worklist 队列、接口收口、日志语义化、调度/触发/投放区三份事实源、数据生命周期、测试与 CI —— 全部落地。

**真正的收尾（非重构）**：① R3 存量收敛（裸整数退出码/except-pass）；② R2 数据面全链刷新；
③ D6 口径定案。三者建议并入"运营收尾"而非继续留在重构路线图内。

**文档结束。**
